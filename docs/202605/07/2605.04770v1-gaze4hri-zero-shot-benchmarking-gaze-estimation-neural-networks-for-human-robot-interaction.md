---
title: "Gaze4HRI: Zero-shot Benchmarking Gaze Estimation Neural-Networks for Human-Robot Interaction"
title_zh: Gaze4HRI：面向人机交互的零样本视线估计神经网络基准测试
authors: "Berk Sezer, Ali Görkem Küçük, Erol Şahin, Sinan Kalkan"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04770v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 针对人机交互的3D视线估计基准测试
tldr: 本研究针对人机交互（HRI）场景中视线估计的可靠性问题，提出了大规模基准数据集Gaze4HRI。该数据集包含50多名受试者和60万帧图像，涵盖了光照变化、头眼冲突及动态相机/目标等关键变量。通过对现有SOTA模型的零样本评估，发现数据多样性比复杂模型架构更能提升鲁棒性，并指出向下视线是目前的通用失效点，为未来HRI视线估计研究提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04770v1/fig-001.webp\", \"caption\": \"Fig. 1: (a) We introduce Gaze4HRI, an extensive benchmark which includes high-quality gaze recordings for 50+ subjects, 3,000+ videos, 600,000+ frames for an HRI setting. (b) Using our dataset, we evaluated the performance of five state-ofthe-art gaze estimation architectures for HRI settings.\", \"page\": 2, \"index\": 1, \"width\": 844, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.04770v1/fig-002.webp\", \"caption\": \"TABLE VII: Exp. 4.2. Mutual-Gaze Setup: Error (◦) (subjectlevel mean ± SD) for each condition (H: Horizontal, V: Vertical). βpitch & βyaw: slope of change in error (◦) per change of absolute pitch & yaw eccentricity (◦) (as subjectlevel mean ± SD), as aggregated across all four conditions.\", \"page\": 8, \"index\": 2, \"width\": 446, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.04770v1/fig-003.webp\", \"caption\": \"Fig. 4: The four setups used in our analysis.\", \"page\": 4, \"index\": 3, \"width\": 506, \"height\": 1005}, {\"url\": \"assets/figures/arxiv/2605.04770v1/fig-004.webp\", \"caption\": \"Fig. 5: Gaze targets on the shared table, for the objectcentered setup. The black and red circles mark targets for head-pose free and head-pose fixed experiments respectively.\", \"page\": 5, \"index\": 4, \"width\": 512, \"height\": 289}]"
motivation: 现有的视线估计基准缺乏对动态相机、移动目标等实际人机交互复杂条件的评估，且跨数据集测试存在复杂度差异。
method: 构建了包含3000多个视频的大规模数据集Gaze4HRI，并对多种先进的视线估计神经网络在不同HRI变量下进行了零样本性能基准测试。
result: 实验显示所有方法在极端向下视线时均失效，但在ETH-X-Gaze上训练的PureGaze模型在其他条件下表现出最强的鲁棒性。
conclusion: 研究表明大规模数据多样性是实现零样本鲁棒性的核心驱动力，而自对抗损失等特征纯化框架能进一步提升模型在非受限环境下的表现。
---

## 摘要
虽然基于外观的零样本3D视线估计通过将RGB图像直接映射到视线向量提供了显著的成本效益，但其在人机交互（HRI）场景中的可靠性仍不确定。现有的基准测试经常忽略基本的HRI条件，例如动态摄像机视角和视频中的移动目标。此外，当前的跨数据集评估通常存在复杂度差距，即在多样化数据集上训练的方法在规模显著较小且变化较少的数据集上进行测试，未能评估真正的鲁棒性。为了弥补这些差距，我们推出了Gaze4HRI，这是一个大规模数据集（50多个受试者，3,000多个视频，600,000多个帧），旨在评估最先进方法在关键HRI变量下的性能：光照、头部-视线冲突，以及视频中摄像机和视线目标的运动。我们的基准测试显示，所有评估的方法在至少一种条件下都会失败，并将大幅度向下视线确定为普遍的失效点。值得注意的是，在ETH-X-Gaze数据集上训练的PureGaze在所有其他条件下都独特地保持了韧性。这些结果挑战了近期文献中对复杂时空建模和基于Transformer架构的关注。相反，我们的研究结果表明，以ETH-X-Gaze数据集为代表的广泛数据多样性是非受限环境下零样本鲁棒性的主要驱动力，而增强韧性的框架（如PureGaze用于视线特征纯化的自对抗损失）则提供了实质性的进一步改进。最终，本研究建立了一个严谨的基准，为从业者提供了实用指南，并重塑了未来的研究。数据集和代码可在 https://gazeforhri.github.io 获取。

## Abstract
While zero-shot appearance-based 3D gaze estimation offers significant cost-efficiency by directly mapping RGB images to gaze vectors, its reliability in Human-Robot Interaction (HRI) settings remains uncertain. Existing benchmarks frequently overlook fundamental HRI conditions, such as dynamic camera viewpoints and moving targets in video. Furthermore, current cross-dataset evaluations often suffer from a complexity gap, where methods trained on diverse datasets are tested on significantly smaller and less varied sets, failing to assess true robustness. To bridge these gaps, we introduce Gaze4HRI, a large-scale dataset (50+ subjects, 3,000+ videos, 600,000+ frames) designed to evaluate state-of-the-art performance against critical HRI variables: illumination, head-gaze conflict, as well as the motion of camera and gaze target in video. Our benchmark reveals that all evaluated methods fail in at least one condition, identifying steeply-downward gaze as a universal failure point. Notably, PureGaze trained on the ETH-X-Gaze dataset uniquely maintains resilience across all other conditions. These results challenge the recent focus in the literature on complex spatial-temporal modeling and Transformer-based architectures. Instead, our findings suggest that extensive data diversity, as exemplified by the ETH-X-Gaze dataset, serves as the primary driver of zero-shot robustness in unconstrained environments, while resilience-enhancing frameworks, such as PureGaze's self-adversarial loss for gaze feature purification, provide a substantial further improvement. Ultimately, this study establishes a rigorous benchmark that provides practical guidelines for practitioners as well as reshaping future research. The dataset and codes are available at https://gazeforhri.github.io.

---

## 论文详细总结（自动生成）

这篇论文对人机交互（HRI）场景下的视线估计神经网络进行了深入的零样本基准测试。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管基于外观的3D视线估计技术发展迅速，但在实际的人机交互（HRI）场景中（如机器人移动、相机安装在机械臂上、动态光照等），这些最先进（SOTA）模型的可靠性尚未得到充分验证。
*   **研究动机**：
    *   **复杂度差距**：现有基准测试通常在简单、静态的数据集上评估，而实际HRI涉及动态相机视角和移动目标。
    *   **零样本需求**：在实际应用中，用户倾向于直接部署预训练模型（零样本），而非进行昂贵的微调。
    *   **变量缺失**：现有研究缺乏对“头眼冲突”（头部指向与视线方向不一致）以及机器人运动引起的视角变化等关键变量的系统分析。

### 2. 论文提出的方法论
*   **核心思想**：构建一个专门模拟HRI真实条件的基准数据集 **Gaze4HRI**，并利用该数据集对五种主流SOTA模型进行多维度的零样本性能评估。
*   **关键技术细节**：
    *   **硬件设置**：使用UR5机器人手臂，在腕部安装Intel RealSense D435i相机，利用OptiTrack运动捕捉系统获取高精度（±0.5mm）的头部姿态作为真值。
    *   **数据处理**：采用数据校正（Rectification）和裁剪（Cropping）两种预处理方式，并对比了它们对不同架构模型的影响。
    *   **评估指标**：使用角误差（Angular Error），即真值视线向量与预测向量之间的夹角。
    *   **模型选择**：评估了 PureGaze、GazeTR、L2CS-Net、MCGaze 和 GaT，涵盖了CNN、Transformer及跨时空建模等不同架构。

### 3. 实验设计
*   **数据集/场景**：
    *   **Gaze4HRI数据集**：包含52名受试者、3258个视频、超过62万帧图像。
    *   **四大实验场景**：
        1.  **光照变化**：4种不同亮度的离散水平。
        2.  **相机视角**：机器人带动相机在60°弧线上移动。
        3.  **头眼冲突**：固定头部朝向（左/中/右），改变视线目标。
        4.  **视线方向**：包括“以物体为中心”（桌面上9个点）和“共同关注”（受试者追踪移动的机器人相机）。
*   **对比基准**：对比了在不同源数据集（ETH-X-Gaze vs. Gaze360）上训练的模型表现。

### 4. 资源与算力
*   **算力设备**：实验在配备 **NVIDIA RTX A4000 GPU** 的工作站上运行。
*   **训练时长**：
    *   为了公平对比，作者在Gaze360上重新训练了PureGaze（50个epoch）和GazeTR（80个epoch）。
    *   论文详细记录了各模型的推理速度（FPS）和显存占用（VRAM），例如PureGaze和GazeTR的FPS均超过500，而基于视频剪辑的模型（MCGaze）速度较慢（约19 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：52名受试者的样本量在视线估计领域属于中大规模，且视频总时长达5.7小时。
*   **客观性与公平性**：
    *   采用了**受试者级聚合（Subject-level aggregation）**进行统计推断，避免了伪重复实验导致的偏差。
    *   使用了Holm校正的t检验和Cohen's d效应量来确保统计结果的显著性和科学性。
    *   实验涵盖了从静态到动态、从受控到非受控的多种变量，设计非常全面。

### 6. 主要结论与发现
*   **数据多样性胜过模型复杂度**：在ETH-X-Gaze（包含极端姿态和多种光照）上训练的模型，其鲁棒性远超在Gaze360上训练的复杂时空模型。
*   **通用失效点**：所有评估的方法在处理**大幅度向下视线**（Steeply-downward gaze）时均表现糟糕，这主要是由于眼睑遮挡造成的。
*   **最佳模型**：在ETH-X-Gaze上训练的 **PureGaze** 表现最稳健，其自对抗损失函数能有效纯化视线特征，剔除身份和光照干扰。
*   **头眼冲突影响**：Gaze360训练的模型在头眼方向不一致时误差显著增大，而ETH-X-Gaze训练的模型对此具有很强的韧性。

### 7. 优点
*   **填补空白**：首次系统性地评估了机器人运动（动态相机）对视线估计的影响。
*   **实用指南**：为HRI从业者提供了明确的模型选择建议（优先选择在ETH-X-Gaze上训练的模型）。
*   **高质量标注**：利用运动捕捉系统和机器人运动学模型，提供了比现有“野外”数据集更精准的真值。

### 8. 不足与局限
*   **向下视线难题未解**：虽然识别出了向下视线的失效，但论文未提出算法层面的解决方案（仅建议增加下方摄像头）。
*   **环境局限**：虽然模拟了HRI变量，但数据采集仍主要在实验室环境下完成，对极端户外环境或复杂背景的覆盖可能仍有不足。
*   **单相机限制**：实验主要基于单RGB相机，未探讨深度信息或多相机融合在解决遮挡问题上的潜力。

（完）
