---
title: Proactive Dialogue Model with Intent Prediction
title_zh: 具有意图预测能力的主动对话模型
authors: Yang Luo
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27379v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 使用贝叶斯网络进行意图预测的前瞻性对话模型
tldr: 针对对话模型通常是被动响应且在多意图场景下效率低的问题，本文提出一种基于意图预测的主动对话模型。通过在推理时将从对话数据中提取的轻量级意图转移先验（时间贝叶斯网络 T-BN）注入系统提示词，引导模型预测用户后续意图。实验表明，该方法在不修改底层语言模型的情况下，显著提升了意图覆盖率并减少了对话轮数，实现了更高效的主动对话。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27379v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed pipeline. Stage 1 abstracts a corpus of MultiWOZ dialogues into a binary turn–intent matrix M. Stage 2 lifts M into consecutive USER-turn pairs and fits a Temporal Bayesian Network B with NOTEARS under a forward-only tabu-edge constraint. At runtime, each user utterance u is grounded in B via top-k embedding similarity; the posterior P (xt+1 | O(u)) is thresholded, formatted as a causal-insight block C, and injected into the LLM prompt so that the generated response y is both reactive to the current request and anticipatory of plausible next intents.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.27379v1/fig-002.webp\", \"caption\": \"Table 2: Next-intent prediction on 1,071 held-out USER-turn pairs from 200 dialogues (threshold τ=0.5). Recall@k measures whether the ground-truth next intent appears in the top-k predictions of each method. The Bigram baseline conditions on both the previous and current intent.\", \"page\": 5, \"index\": 2, \"width\": 662, \"height\": 199}]"
motivation: 现有的对话模型多为被动响应，缺乏对用户后续意图的预判，导致在处理复杂多意图任务时交互冗余。
method: 利用在 MultiWOZ 数据集上训练的时间贝叶斯网络（T-BN）捕捉意图转移规律，并将其作为先验知识注入推理时的系统提示词中。
result: "T-BN 在意图预测上表现优异，且在对话回放实验中将 Coverage AUC 从 0.742 提升至 0.856，并将达到 75% 意图覆盖所需的轮数从 3.95 降至 2.73。"
conclusion: 通过轻量级的意图转移引导，可以在不改变模型架构的前提下，有效提升对话系统的主动性和交互效率。
---

## 摘要
对话模型本质上是反应性的，仅响应当前的用户轮次而不预测即将出现的意图，这在多意图场景下会导致冗余交互。我们通过引入一种轻量级的意图转移先验来解决这一局限性，该先验源自对话数据，并在推理时注入到系统提示词中。我们使用在 MultiWOZ 2.2 逐轮意图标注上训练的时间贝叶斯网络（T-BN）来实例化这一先验。在 1,071 个预留的用户轮次对上，T-BN 实现了 Recall@5 = 0.787 和 MRR = 0.576。在超过 200 个对话的基准事实回放中，BN 引导的生成将覆盖率 AUC 从 0.742 提高到 0.856，并将达到 75% 意图覆盖率所需的轮次从 3.95 减少到 2.73。这些结果表明，轻量级的意图转移引导能够在不修改底层语言模型的情况下，实现更具主动性和效率的对话行为。

## Abstract
Dialogue models are inherently reactive, responding to the current user turn without anticipating upcoming intents, which leads to redundant interactions in multi-intent settings. We address this limitation by introducing a lightweight intent-transition prior derived from dialogue data and injected into the system prompt at inference time. We instantiate this prior using a Temporal Bayesian Network (T-BN) trained on per-turn intent annotations in MultiWOZ 2.2. The T-BN achieves Recall@5 = 0.787 and MRR = 0.576 on 1,071 held-out USER-turn pairs. In a ground-truth replay over 200 dialogues, BN-guided generation improves Coverage AUC from 0.742 to 0.856 and reduces the number of turns required to reach 75% intent coverage from 3.95 to 2.73. These results show that lightweight intent-transition guidance enables more proactive and efficient dialogue behavior without modifying the underlying language model.

---

## 论文详细总结（自动生成）

这是一份关于论文《Proactive Dialogue Model with Intent Prediction》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的任务型对话（TOD）系统通常是“被动响应式”的，即仅针对用户当前的输入进行回复，而缺乏对用户后续潜在意图的预判。在多意图场景下（如搜索、预订、后续咨询），这种被动性会导致交互轮次冗余，降低效率。
*   **研究背景**：虽然大语言模型（LLM）提升了对话理解能力，但它们仍未显式建模意图随时间演化的动态结构。本文旨在通过挖掘对话数据中的意图转移规律，赋予模型“前瞻性”，使其能主动引导对话。

### 2. 方法论
该方法分为两个阶段，旨在不修改底层 LLM 的情况下，通过外部知识注入实现主动性：

*   **阶段一：构建时间贝叶斯网络（T-BN）**
    *   **意图抽象**：从 MultiWOZ 2.2 数据集中提取 8 类核心意图（如 `find-restaurant`, `book-hotel`），构建二值化的“轮次-意图”矩阵。
    *   **结构学习**：利用 **NOTEARS 算法**（一种将 DAG 结构学习转化为连续优化问题的框架）学习意图间的有向无环图（DAG）。
    *   **时间建模**：引入滞后矩阵（Lagged Matrix）捕捉 $t$ 轮到 $t+1$ 轮的转移。为了保证因果逻辑，施加了“禁忌边（Tabu-edge）”约束，禁止时间倒流的连接。
    *   **参数估计**：使用贝叶斯估计器拟合条件概率分布（CPDs），得到转移概率 $P(x_{t+1} | x_t, \rho_t)$，其中 $\rho_t$ 是对话进度的特征。

*   **阶段二：推理时的知识注入（BN-Guided Generation）**
    *   **意图锚定**：将当前用户话语嵌入化，通过余弦相似度匹配到 T-BN 中的意图节点。
    *   **概率推理与门控**：利用变量消除法推导后续意图的后验概率。设置阈值（如 $P > 0.5$）过滤低置信度预测。
    *   **提示词注入**：将高概率的“主动洞察（Causal-insight）”格式化为文本块，注入到系统提示词（Prompt）中，引导 LLM 生成既响应当前需求又预判下一步的回复。

### 3. 实验设计
*   **数据集**：使用 **MultiWOZ 2.2**，这是多领域任务型对话的标准基准。
*   **实验场景**：
    *   **Track A（内在评估）**：在 1,071 个预留的用户轮次对上测试意图预测的准确性。
    *   **Track B（端到端评估）**：通过“基准事实回放（Ground-truth Replay）”模拟 200 场对话，评估效率提升。
*   **对比方法（Baselines）**：
    *   Random Transition（随机转移）
    *   Marginal（基于经验频率，无时间关联）
    *   Bigram（基于前一轮意图的二元模型）
    *   Plain-LLM（无引导的原始模型，使用 MiniMax-M2.5 后端）

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型后端**：提到了使用 **MiniMax-M2.5** 作为 LLM 生成底座。由于 T-BN 属于轻量级统计模型，其训练和推理开销相对于 LLM 极小。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 200 个完整对话进行回放测试，包含 1,071 个意图转移样本。
    *   进行了 **5 折交叉验证**来验证 T-BN 结构的稳定性。
*   **充分性评价**：实验设计较为客观。通过 Track A 验证了预测模型的有效性，通过 Track B 验证了下游任务的收益。对比了多种基准模型，并引入了 Recall@k 和 MRR 等标准指标，实验逻辑闭环。

### 6. 主要结论与发现
*   **预测性能**：T-BN 在意图预测上显著优于 Bigram 和 Marginal 模型，Recall@5 达到 **0.787**，MRR 达到 **0.576**。
*   **对话效率**：BN 引导的方法将意图覆盖率 AUC 从 0.742 提升至 **0.856**。
*   **轮次缩减**：达到 75% 意图覆盖所需的平均轮次从 3.95 降至 **2.73**（缩减了约 **30.9%**），证明了主动预判能有效减少冗余交互。
*   **结构稳定性**：通过禁忌边约束，成功实现了 100% 的前向因果逻辑，且核心意图的自持久性边（Self-persistence edges）表现稳健。

### 7. 优点
*   **轻量且即插即用**：无需对大模型进行微调（Fine-tuning），仅通过 Prompt 注入即可改变模型行为。
*   **可解释性强**：贝叶斯网络提供了显式的意图转移图谱，比纯黑盒的神经模型更易于分析和调试。
*   **因果约束**：引入 NOTEARS 和时间约束，确保了意图预测符合逻辑上的先后顺序。

### 8. 不足与局限
*   **数据稀疏性**：对于出现频率较低的意图（如 `book-train`），预测效果较差（Recall 为 0），说明模型对长尾分布的鲁棒性不足。
*   **意图规模有限**：目前仅在 8 个核心意图上验证，尚未测试在拥有成百上千个意图的大规模工业场景下的扩展性。
*   **接口瓶颈**：论文指出，BN 预测的增益在转化为最终对话质量时存在损耗，说明“概率引导到文本生成”的接口（Prompt 模板）仍有优化空间。

（完）
