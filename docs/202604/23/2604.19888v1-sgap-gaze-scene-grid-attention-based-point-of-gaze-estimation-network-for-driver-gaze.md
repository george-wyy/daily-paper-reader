---
title: "SGAP-Gaze: Scene Grid Attention Based Point-of-Gaze Estimation Network for Driver Gaze"
title_zh: SGAP-Gaze：基于场景网格注意力的驾驶员视线落点估计网络
authors: "Pavan Kumar Sharma, Pranamesh Chakraborty"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19888v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 驾驶员视线估计与视线落点估计网络
tldr: 本研究针对驾驶员视线估计问题，提出了一个新的基准数据集UD-FSG，包含同步的驾驶员面部和交通场景图像。同时提出了SGAP-Gaze网络，该模型融合了面部、眼睛、虹膜及场景上下文信息，利用Transformer注意力机制在场景网格上计算权重以预测注视点（PoG）。实验表明，该方法在多个数据集上显著优于现有模型，尤其在边缘区域表现稳健，为理解驾驶员态势感知提供了有效工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19888v1/fig-001.webp\", \"caption\": \"TABLE I: Comparison between benchmark driver gaze datasets.\", \"page\": 4, \"index\": 1, \"width\": 514, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.19888v1/fig-002.webp\", \"caption\": \"Fig. 3: Sample face–scene image pairs from the UD-FSG dataset, illustrating variations in traffic density and lighting conditions.\", \"page\": 4, \"index\": 2, \"width\": 488, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.19888v1/fig-003.webp\", \"caption\": \"Fig. 8: SGAP-Gaze results on test samples. The green and red circle (radius = 20 pixels) represent the ground-truth and predicted PoG.\", \"page\": 9, \"index\": 3, \"width\": 488, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2604.19888v1/fig-004.webp\", \"caption\": \"Fig. 6: Visualization of face features extracted from different layers of ResNet-18 for three drivers.\", \"page\": 5, \"index\": 4, \"width\": 488, \"height\": 327}]"
motivation: 现有的驾驶员视线估计模型主要依赖面部信息，忽视了交通场景提供的关键上下文线索。
method: 提出SGAP-Gaze网络，通过融合多模态面部特征生成视线意图向量，并利用Transformer注意力机制结合场景网格特征来预测注视点。
result: "在UD-FSG和LBW数据集上，该模型将平均像素误差降低了23.5%，且在场景边缘区域表现出更强的鲁棒性。"
conclusion: 结合多模态面部线索与场景感知注意力机制，能显著提升真实驾驶环境下注视点估计的准确性。
---

## 摘要
驾驶员视线估计对于理解驾驶员对周围交通的态势感知至关重要。现有的视线估计模型利用驾驶员面部信息来预测视线落点（PoG）或 3D 视线方向向量。我们提出了一个基准数据集——城市驾驶面部场景视线（UD-FSG），该数据集包含同步的驾驶员面部和交通场景图像。场景图像提供了有关周围交通的线索，与面部图像结合可以帮助改进视线估计模型。我们提出了 SGAP-Gaze，即基于场景网格注意力的视线落点估计网络，并在 UD-FSG 数据集上进行了训练和测试，该网络明确地将场景图像纳入视线估计建模中。该视线估计网络整合了驾驶员面部、眼睛、虹膜和场景上下文信息。首先，融合从面部模态提取的特征以形成视线意图向量。然后，利用基于 Transformer 的注意力机制融合面部和场景图像特征，在空间场景网格上计算注意力分数，从而获得视线落点。所提出的 SGAP-Gaze 模型在 UD-FSG 数据集上的平均像素误差为 104.73，在 LBW 数据集上为 63.48，与最先进的驾驶员视线估计模型相比，平均像素误差降低了 23.5%。空间像素分布分析表明，SGAP-Gaze 在所有空间范围内（包括场景的边缘区域，这些区域虽然罕见但对于理解驾驶员注意力至关重要）的平均像素误差始终低于现有方法。这些结果突显了在真实驾驶环境中，将多模态视线线索与场景感知注意力相结合，对于构建鲁棒的驾驶员视线落点估计模型的有效性。

## Abstract
Driver gaze estimation is essential for understanding the driver's situational awareness of surrounding traffic. Existing gaze estimation models use driver facial information to predict the Point-of-Gaze (PoG) or the 3D gaze direction vector. We propose a benchmark dataset, Urban Driving-Face Scene Gaze (UD-FSG), comprising synchronized driver-face and traffic-scene images. The scene images provide cues about surrounding traffic, which can help improve the gaze estimation model, along with the face images. We propose SGAP-Gaze, Scene-Grid Attention based Point-of-Gaze estimation network, trained and tested on our UD-FSG dataset, which explicitly incorporates the scene images into the gaze estimation modelling. The gaze estimation network integrates driver face, eye, iris, and scene contextual information. First, the extracted features from facial modalities are fused to form a gaze intent vector. Then, attention scores are computed over the spatial scene grid using a Transformer-based attention mechanism fusing face and scene image features to obtain the PoG. The proposed SGAP-Gaze model achieves a mean pixel error of 104.73 on the UD-FSG dataset and 63.48 on LBW dataset, achieving a 23.5% reduction in mean pixel error compared to state-of-the-art driver gaze estimation models. The spatial pixel distribution analysis shows that SGAP-Gaze consistently achieves lower mean pixel error than existing methods across all spatial ranges, including the outer regions of the scene, which are rare but critical for understanding driver attention. These results highlight the effectiveness of integrating multi-modal gaze cues with scene-aware attention for a robust driver PoG estimation model in real-world driving environments.

---

## 论文详细总结（自动生成）

### 论文总结：SGAP-Gaze 基于场景网格注意力的驾驶员视线落点估计网络

#### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂的真实驾驶环境（特别是异构交通流）中，准确估计驾驶员的视线落点（Point-of-Gaze, PoG）。
*   **研究动机**：
    *   现有的驾驶员监控系统（DMS）多关注车内分心（如玩手机、打瞌睡），而非驾驶员对外部交通环境的态势感知。
    *   大多数视线估计模型仅依赖面部特征，忽略了外部场景（如周围车辆、行人）对驾驶员视线的引导作用。
    *   缺乏包含同步面部和复杂交通场景的大规模真实世界数据集。

#### 2. 方法论
SGAP-Gaze 提出了一种融合面部多模态特征与场景上下文的深度学习框架：
*   **面部几何提取（FEI 模块）**：利用定制的 YOLOv8 模型检测面部、眼睛和虹膜区域，为后续特征提取提供精确裁剪图。
*   **多流特征提取**：
    *   **面部流**：使用 ResNet-18 提取四个层级的全局特征，捕捉从纹理到语义的多尺度信息。
    *   **高斯加权眼睛流**：在提取眼睛特征时，引入以虹膜中心为原点的二维高斯权重函数，强化虹膜位置对视线方向的贡献。
    *   **场景流**：通过 ResNet-18 将场景图像转化为 $7 \times 7$ 的空间特征网格。
*   **多模态特征融合与注意力机制**：
    *   将面部、眼睛和虹膜坐标特征融合为**视线意图向量（Gaze Intent Vector）**。
    *   **核心创新**：采用 Transformer 注意力机制，将视线意图向量作为查询（Query），场景网格特征作为键（Key），计算场景各区域的注意力权重。
*   **预测头**：通过注意力分布的加权平均获得初始 PoG，并辅以一个可学习的残差修正项来补偿网格离散化误差。

#### 3. 实验设计
*   **数据集**：
    *   **UD-FSG（新提出）**：包含 35 名驾驶员、37.3 万对同步图像，涵盖复杂的城市交通和多变的光照条件。
    *   **LBW (Look Both Ways)**：现有的包含面部和场景的基准数据集，用于对比验证。
*   **对比方法（Benchmark）**：
    *   **3D 视线方向**：对比了 Gaze360、ETH-XGaze、GazePTR 和 LBW 原型模型。
    *   **视线落点（PoG）**：重点对比了当前最先进的 Transformer 模型 GazePTR。
*   **评估指标**：平均角误差（度）和平均像素误差（MPE）。

#### 4. 资源与算力
*   **硬件环境**：使用了 GPU 服务器，配备 **4 张 NVIDIA GeForce RTX 3080 (10 GB VRAM)**。
*   **软件环境**：基于 PyTorch 框架和 CUDA 11.4。
*   **训练细节**：文中未明确给出具体的训练总时长，但提到了使用 5 FPS 的采样率处理 37.3 万帧数据，具有较大的计算规模。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨受试者（Subject-independent）的验证，确保了模型的泛化能力。
    *   针对不同输入模态（F、F+E、F+E+S）进行了消融实验，验证了场景信息和眼睛特征的有效性。
    *   进行了空间像素误差分布分析，将场景划分为 7 个区域，详细对比了模型在边缘和中心区域的表现。
*   **充分性评价**：实验设计较为充分且客观。通过在两个不同数据集（UD-FSG 和 LBW）上的测试，证明了模型在不同分辨率和交通复杂度下的稳健性。

#### 6. 主要结论与发现
*   **性能提升**：SGAP-Gaze 在 UD-FSG 数据集上比 SOTA 模型 GazePTR 的平均像素误差降低了 **23.5%**。
*   **场景信息的重要性**：引入场景特征后，像素误差从 108.63 降至 104.73，且标准差显著降低，说明场景上下文能提高预测的稳定性。
*   **边缘区域优势**：在场景边缘（驾驶员大幅度转头查看侧方时），SGAP-Gaze 的误差远低于传统模型，证明了注意力机制在处理极端视线角度时的有效性。

#### 7. 优点
*   **多模态深度融合**：不仅使用了面部图像，还精细化地利用了虹膜位置的高斯加权特征。
*   **场景感知注意力**：将视线估计从单纯的回归问题转化为“视线意图”与“场景目标”的匹配问题，更符合人类视觉注意力机制。
*   **高质量数据集**：UD-FSG 数据集的发布填补了异构交通环境下驾驶员视线研究的空白。

#### 8. 不足与局限
*   **极端环境下的失效风险**：在光照极差、运动模糊或驾驶员佩戴墨镜导致虹膜检测失败时，模型精度会下降，主要依赖头部姿态。
*   **计算复杂度**：由于涉及多流 ResNet 和 Transformer 注意力，实时部署在嵌入式车载设备上可能面临算力挑战。
*   **场景语义缺失**：目前的场景处理仅基于网格特征，未显式识别后视镜、侧视镜等关键语义目标，未来仍有提升空间。

（完）
