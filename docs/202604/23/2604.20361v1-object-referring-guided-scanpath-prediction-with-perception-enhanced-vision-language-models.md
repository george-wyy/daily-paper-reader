---
title: Object Referring-Guided Scanpath Prediction with Perception-Enhanced Vision-Language Models
title_zh: 基于感知增强视觉语言模型的目标指代引导扫描路径预测
authors: "Rong Quan, Yantao Lai, Dong Liang, Jie Qin"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20361v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 利用历史注视点预测视觉搜索过程中的人类注意力扫描路径
tldr: 本研究针对目标指代引导的扫描路径预测任务，提出ScanVLA模型。该模型利用视觉语言模型进行多模态特征融合，并引入历史增强解码器（HESD）和冻结的分割LoRA以增强细粒度位置感知。ScanVLA在模拟人类搜索特定目标的注意力轨迹方面表现卓越，显著提升了预测准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20361v1/fig-001.webp\", \"caption\": \"Figure 2: Overall architecture of ScanVLA . For each word in the referential expression, ScanVLA uses a Tokenizer, Image Encoder, and embedders (for image/text) to get joint embeddings. These embeddings are fed into an autoregressive LLM with causal mask for fusion, then forwarded to History Enhanced Scanpath Decoder (HESD) to produce fixation sequences.\", \"page\": 3, \"index\": 1, \"width\": 832, \"height\": 245}]"
motivation: 旨在解决现有扫描路径预测方法在多模态信息融合及细粒度目标定位感知方面的不足。
method: 提出ScanVLA模型，结合VLM提取对齐特征，并利用历史增强解码器和辅助分割LoRA提升位置预测精度。
result: 实验证明ScanVLA在目标指代引导的扫描路径预测任务上显著优于现有的先进方法。
conclusion: 通过整合视觉语言模型与专门的位置感知模块，能够更有效地模拟人类在复杂视觉场景中的搜索注意力轨迹。
---

## 摘要
目标指代引导的扫描路径预测（ORSP）旨在根据描述物体的语言描述，预测人类在视觉场景中搜索特定目标物体时的注意力扫描路径。多模态信息融合是 ORSP 的关键。因此，我们提出了一种新颖的模型 ScanVLA，首先利用视觉语言模型（VLM）从输入图像和指代性表达式中提取并融合内在对齐的视觉和语言特征表示。接着，为了增强 ScanVLA 对细粒度位置信息的感知，我们不仅提出了一种新颖的历史增强扫描路径解码器（HESD），该解码器直接将历史注视点的位置信息作为输入，以辅助预测当前注视点更合理的位置，还采用了一个冻结的分割 LoRA（Segmentation LoRA）作为辅助组件，以帮助更精确地定位被指代的对象，从而在不增加额外巨大计算和时间成本的情况下改进了扫描路径预测任务。大量的实验结果表明，ScanVLA 在目标指代下的表现显著优于现有的扫描路径预测方法。

## Abstract
Object Referring-guided Scanpath Prediction (ORSP) aims to predict the human attention scanpath when they search for a specific target object in a visual scene according to a linguistic description describing the object. Multimodal information fusion is a key point of ORSP. Therefore, we propose a novel model, ScanVLA, to first exploit a Vision-Language Model (VLM) to extract and fuse inherently aligned visual and linguistic feature representations from the input image and referring expression. Next, to enhance the ScanVLA's perception of fine-grained positional information, we not only propose a novel History Enhanced Scanpath Decoder (HESD) that directly takes historical fixations' position information as input to help predict a more reasonable position for the current fixation, but also adopt a frozen Segmentation LoRA as an auxiliary component to help localize the referred object more precisely, which improves the scanpath prediction task without incurring additional large computational and time costs. Extensive experimental results demonstrate that ScanVLA can significantly outperform existing scanpath prediction methods under object referring.

---

## 论文详细总结（自动生成）

以下是对论文《Object Referring-Guided Scanpath Prediction with Perception-Enhanced Vision-Language Models》的结构化深入总结：

### 1. 核心问题与整体含义
该论文关注的是**目标指代引导的扫描路径预测（Object Referring-guided Scanpath Prediction, ORSP）**。
*   **研究动机**：人类在根据语言描述寻找特定物体时，会增量式地整合视觉和语言信息。现有的方法（如 ART）通常使用独立的视觉和语义编码器，导致特征对齐困难，且在处理细粒度位置信息和多模态融合时存在局限，或者辅助任务计算成本过高。
*   **核心目标**：通过引入强大的视觉语言模型（VLM）和专门的位置感知增强模块，更准确地模拟人类在执行指代性搜索任务时的动态注意力转移过程。

### 2. 方法论
论文提出了 **ScanVLA** 模型，其核心架构包含以下关键部分：
*   **特征提取与融合（VLM 骨干）**：采用预训练的 VLM（如 InternVL 2.5 或 Qwen2.5-VL）作为统一的特征提取器。利用 LLM 的因果掩码（Causal Mask）特性，完美契合 ORSP 任务的增量处理需求（即预测当前词的注视点时只能看到前文）。
*   **双 LoRA 策略**：
    *   **Scanpath LoRA**：可训练模块，用于学习 ORSP 任务特定的扫描路径生成知识。
    *   **Segmentation LoRA**：引入预训练自分割任务（如 Sa2VA）的**冻结** LoRA 模块，作为“插件”增强模型对目标物体的细粒度定位和感知能力，且不增加额外训练负担。
*   **历史增强扫描路径解码器（HESD）**：
    *   不同于以往仅使用抽象隐状态的方法，HESD 直接将历史注视点的坐标（X, Y）、包索引（Pack Index）和顺序信息输入到一个单向 GRU 中。
    *   将 GRU 提取的历史全局信息与 VLM 的文本隐藏状态拼接，通过多注视点预测器（Multi-Fixs Predictor）输出当前词对应的注视点序列。
*   **损失函数**：结合了辅助文本生成损失（$L_{txt}$）、坐标回归损失（$L_{xy}$，采用 L1 损失）和注视点有效性损失（$L_{token}$，采用 Focal Loss 处理类别不平衡）。

### 3. 实验设计
*   **数据集**：**RefCOCO-Gaze**，这是目前 ORSP 领域唯一的大规模数据集，包含 2,094 张图像和 19,738 条人类扫描路径。
*   **评估指标**：
    *   扫描路径相似度：SS, $SS_{pack}$, FED, $FED_{pack}$。
    *   注视点显著性：$CC_{pack}$, $NSS_{pack}$。
*   **对比方法（Benchmarks）**：包括人类表现（Human）、随机基准（Random）、OFA、Chen et al.、Gazeformer 以及当前最先进的 ART 模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   Batch Size 为 1，梯度累积步数为 8。
    *   采用了 AdamW 优化器，配合线性预热（LinearLR）和余弦退火（CosineAnnealingLR）学习率调度。
    *   LoRA 的秩（Rank）设为 16，Alpha 设为 32。
    *   输入图像尺寸统一为 $312 \times 520$。

### 5. 实验数量与充分性
*   **实验规模**：论文在 RefCOCO-Gaze 测试集上进行了全面的定量对比，并提供了定性的可视化分析。
*   **消融实验**：共设计了 **6 组消融实验**，分别验证了分割 LoRA、扫描路径 LoRA、HESD 模块、文本损失函数、L1/L2 损失选择以及特征融合阶段（早期融合 vs 晚期融合）的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了从模型组件到超参数选择的验证。通过与人类表现的对比，证明了模型在某些指标上甚至超越了人类平均水平（更具客观性），实验结果具有说服力。

### 6. 主要结论与发现
*   **性能领先**：ScanVLA 在所有评估指标上均显著优于现有 SOTA 模型（如 ART），尤其是在 $NSS_{pack}$ 和 $CC_{pack}$ 等显著性指标上有大幅提升。
*   **VLM 的优势**：证明了 VLM 提取的内在对齐特征比独立编码器更适合多模态扫描路径预测任务。
*   **位置感知的重要性**：HESD 模块通过直接利用历史坐标信息，显著提升了预测注视点的合理性。
*   **插件化增强**：引入冻结的分割 LoRA 是一种低成本且高效的增强视觉定位能力的方法。

### 7. 优点
*   **首创性**：首次将视觉语言模型（VLM）应用于扫描路径预测任务，解决了特征对齐的痛点。
*   **架构优雅**：利用 LLM 的因果掩码自然地处理增量式任务，无需复杂的时序设计。
*   **高效性**：通过 LoRA 技术进行微调，并使用冻结的辅助模块，在保持高性能的同时控制了计算成本。
*   **感知增强**：HESD 模块对历史信息的显式建模提升了模型对空间关系的理解。

### 8. 不足与局限
*   **数据集单一**：由于 ORSP 是一个新兴任务，实验仅在 RefCOCO-Gaze 一个数据集上完成，跨场景的泛化能力有待进一步验证。
*   **实时性未提及**：虽然提到计算成本降低，但未具体给出推理延迟（Inference Latency）数据，对于机器人或 VR/AR 等实时应用场景的适用性尚不明确。
*   **主观性偏差**：扫描路径预测本身受人类主观因素影响极大，模型虽然在客观指标上表现优异，但在模拟不同个体搜索习惯方面的能力未做深入探讨。

（完）
