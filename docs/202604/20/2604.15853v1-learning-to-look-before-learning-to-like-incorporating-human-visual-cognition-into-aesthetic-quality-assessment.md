---
title: "Learning to Look before Learning to Like: Incorporating Human Visual Cognition into Aesthetic Quality Assessment"
title_zh: 先学观察再学欣赏：将人类视觉认知融入审美质量评估
authors: "Liwen Yu, Chi Liu, Xiaotong Han, Congcong Zhu, Minghao Wang, Sheng Shen"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15853v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 在眼动追踪数据上预训练的视线对齐视觉编码器，用于意图类评估
tldr: 针对传统审美质量评估（AQA）仅依赖语义感知而忽视人类视觉认知动态过程的问题，本文提出AestheticNet。该模型采用双路径架构，通过离线预训练的注视对齐视觉编码器（GAVE）模拟人类视觉注意力，并与CLIP等语义编码器融合。研究证明，引入视觉认知先验（如构图、光影）能显著提升审美评估性能，且该模块具有良好的通用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15853v1/fig-001.webp\", \"caption\": \"Figure 2: The Cognitive Architecture of AestheticNet. This framework achieves aesthetic perception through two stages and four steps. (A) Contrastive Gaze Alignment (CGA): The Gaze Encoder aligns raw pixels with eye-tracking sequences using contrastive loss (LCGA) to learn a general \\\"gaze grammar\\\". (B) Dual-Branch Extraction: A frozen Semantic Encoder and the Gaze-Aligned Visual Encoder (GAVE)—the visual backbone of the pre-trained Gaze Encoder—extract content and perceptual form. (C) Gaze-Guided Attention: Semantic representations (𝑄) actively query gaze-aligned features (𝐾,𝑉) via a directed attention mechanism. (D) Assessment: The synthesized representation is mapped to a scalar aesthetic score ?̂?. (MHSA: Multi-Head Self-Attention; LN: Layer Normalization; FFN: Feed-Forward Network).\", \"page\": 3, \"index\": 1, \"width\": 1044, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.15853v1/fig-002.webp\", \"caption\": \"Figure 1: Attention divergence between two AQA pathways. The semantic pathway (middle) exhibits object-centric bias, while the gaze attention pathway (right) prioritizes scene compositional properties and visual flow, implying the synergy and complementary effect.\", \"page\": 1, \"index\": 2, \"width\": 508, \"height\": 477}]"
motivation: 现有的审美评估模型大多将图像视为静态像素向量，缺乏对人类动态视觉探索过程（如注视路径和处理流利度）的模拟。
method: 提出双路径架构AestheticNet，利用在眼动数据上预训练的注视对齐视觉编码器（GAVE）捕捉视觉注意力，并与固定语义编码器进行交叉注意力融合。
result: 实验表明，该方法在多个基准测试中均优于纯语义基线，且注视模块可作为通用插件提升不同AQA骨干网络的性能。
conclusion: 将人类视觉认知（如注视先验）与语义感知相结合是提升审美质量评估准确性的关键，且该认知模块具有模型无关的修正能力。
---

## 摘要
自动审美质量评估（AQA）主要将图像视为静态像素向量，主要通过语义感知使预测结果与人类评分保持一致。然而，这种范式与人类的审美认知相背离，人类审美认知源于由扫描路径、处理流畅度以及自下而上的显著性与自上而下的意图之间的相互作用所构成的动态视觉探索。我们提出了 AestheticNet，这是一种受认知启发的新型 AQA 范式，通过双通路架构整合了类人视觉认知和语义感知。视觉注意力通路实现为注视对齐视觉编码器（GAVE），该编码器利用资源高效的对比注视对齐技术在眼动追踪数据上进行离线预训练，从而模拟人类视觉系统的注意力。该通路通过交叉注意力融合增强了使用固定语义编码器（如 CLIP）的语义通路。视觉注意力提供了一种反映前景/背景结构、色彩级联、亮度和光照的认知先验，这些都是超越语义的审美感知决定因素。经假设检验验证的实验表明，该方法相比仅使用语义的基准模型有持续提升，并证明了注视模块是与多种 AQA 骨干网络兼容的模型无关校正器，支持了类人视觉认知对于 AQA 的必要性和模块化特性。我们的代码可在 https://github.com/keepgallop/AestheticNet 获取。

## Abstract
Automated Aesthetic Quality Assessment (AQA) treats images primarily as static pixel vectors, aligning predictions with human-rating scores largely through semantic perception. However, this paradigm diverges from human aesthetic cognition, which arises from dynamic visual exploration shaped by scanning paths, processing fluency, and the interplay between bottom-up salience and top-down intention. We introduce AestheticNet, a novel cognitive-inspired AQA paradigm that integrates human-like visual cognition and semantic perception with a two-pathway architecture. The visual attention pathway, implemented as a gaze-aligned visual encoder (GAVE) pre-trained offline on eye-tracking data using resource-efficient contrast gaze alignment, models attention from human vision system. This pathway augments the semantic pathway, which uses a fixed semantic encoder such as CLIP, through cross-attention fusion. Visual attention provides a cognitive prior reflecting foreground/background structure, color cascade, brightness, and lighting, all of which are determinants of aesthetic perception beyond semantics. Experiments validated by hypothesis testing show a consistent improvement over the semantic-alone baselines, and demonstrate the gaze module as a model-agnostic corrector compatible with diverse AQA backbones, supporting the necessity and modularity of human-like visual cognition for AQA. Our code is available at https://github.com/keepgallop/AestheticNet.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **AestheticNet** 的新型审美质量评估（AQA）框架，旨在通过模拟人类视觉认知过程（“先看后赏”）来提升机器对图像美感的评价能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自动审美评估模型（AQA）大多将图像视为静态的像素向量，主要依赖语义识别（如识别出“日落”、“狗”）来预测分数。
*   **研究动机**：人类的审美认知并非瞬间完成，而是源于动态的视觉探索（注视路径、扫描顺序）。审美愉悦感往往来自于视觉信息被眼脑系统处理的“流利度”。
*   **核心假设**：将类人的视觉注意力（注视先验）引入机器感知，可以弥补纯语义模型的不足，更真实地还原人类审美逻辑。

### 2. 方法论：AestheticNet 架构
该模型采用双通路架构，模拟大脑的“双过程理论”：
*   **视觉注意力通路（Gaze Encoder）**：
    *   **核心思想**：学习“如何观察”。使用 **DINOv2** 作为视觉骨干，通过**对比注视对齐（CGA）**技术在眼动追踪数据上进行离线预训练。
    *   **技术细节**：利用 InfoNCE 损失函数，将图像特征与人类真实的眼动扫描序列（Scanpaths）进行对齐，从而捕获构图、光影、前景/背景关系等非语义的视觉先验。
*   **语义感知通路（Semantic Encoder）**：
    *   使用预训练且冻结的 **CLIP-ViT-L/14** 提取高层语义特征。
*   **融合机制（Gaze-Guided Attention）**：
    *   采用**交叉注意力机制**：以语义特征为查询（Query），注视特征为键（Key）和值（Value）。这模拟了人类自上而下的认知意图如何调制自下而上的视觉感知。
*   **优化目标**：结合了均方误差（MSE）和皮尔逊线性相关系数（PLCC）惩罚项的混合损失函数，以兼顾评分精度和排序一致性。

### 3. 实验设计
*   **数据集**：使用 **AVA 数据集** 的分类子集（包含风景、建筑、静物等 8 类，共 89,677 张图像）。
*   **Benchmark（评价指标）**：PLCC（相关性）、SROCC（等级相关性）、MSE（误差）。
*   **对比方法**：
    *   通用 CNN：ResNet-50。
    *   经典 AQA 模型：NIMA、HyperIQA。
    *   多模态模型：CLIP-L。
    *   大语言模型（Oracle）：Q-Align（13B）。

### 4. 资源与算力
*   **模型规模**：Gaze Encoder 基于 ViT-Small（约 21M 参数），语义端使用 CLIP-L。
*   **训练细节**：AdamW 优化器，Batch Size 为 64，采用余弦学习率调度，最高 200 轮训练，设有 50 轮早停机制。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和训练总时长，但从模型规模和数据量来看，属于中等算力需求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在全测试集和“低误差 Top-10k”样本上进行了对比。
    *   **消融实验**：通过推理时掩码（Masking）分别测试了纯注视（G-Only）和纯语义（S-Only）分支的贡献。
    *   **分类分析**：针对 8 个语义类别分别验证，证明了在“静物”和“建筑”等重构图领域提升最明显。
    *   **通用性测试**：将注视模块作为插件集成到 5 种不同的骨干网络中。
*   **充分性评价**：实验设计非常严谨，引入了**假设检验（Hypothesis Testing）**来验证必要性和独立性，统计学证据（p < .001）充分，结果具有说服力。

### 6. 主要结论与发现
*   **必要性**：引入人类注视先验显著提升了 AQA 性能，AestheticNet 在 PLCC 上比纯语义基线（CLIP-L）提升了 0.130。
*   **独立性**：注视特征是一种与语义正交的生物信号。实验证明 Gaze 模块可以作为**模型无关的校正器**，即插即用地提升从 ResNet 到 Q-Align 等各类模型的表现。
*   **认知协同**：审美判断是视觉流（动态）与语义内容（静态）共同作用的结果，缺一不可。

### 7. 优点与亮点
*   **认知启发**：不同于纯数据驱动的黑盒模型，该方法有坚实的心理学和神经科学理论支撑。
*   **高数据效率**：Gaze Encoder 仅需 109 张图像的眼动数据即可学习到通用的“观察语法”，体现了资源理性。
*   **模块化强**：提出的 GAVE 模块具有极强的通用性，可作为现有 AQA 模型的“认知补丁”。

### 8. 不足与局限
*   **数据集偏差**：AVA 数据集评分呈高斯分布，导致模型在极端审美（极美或极丑）样本上的表现仍受限于数据稀疏性。
*   **领域局限性**：目前主要针对摄影图像，对于抽象艺术等非写实领域，人类的注视策略可能完全不同，模型的泛化性有待验证。
*   **中心偏移风险**：人类注视天然存在“中心偏见”，模型可能在处理边缘构图时存在潜在偏差。

（完）
