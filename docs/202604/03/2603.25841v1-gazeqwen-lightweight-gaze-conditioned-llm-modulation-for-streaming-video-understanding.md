---
title: "GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding"
title_zh: GazeQwen：用于流式视频理解的轻量级视线引导大语言模型调制
authors: "Trong Thang Pham, Hien Nguyen, Ngan Le"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25841v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 基于注视调节的视频理解大语言模型
tldr: "针对多模态大模型在视频理解中难以有效利用视线信息的问题，本文提出 GazeQwen。该方法通过轻量级的视线重采样器（1-5M参数）将视线特征转化为残差，并注入到 LLM 解码器层中。在 StreamGaze 基准测试中，GazeQwen 表现优异，准确率达到 63.9%，显著超越了 Qwen2.5-VL-7B 原生模型及 GPT-4o，证明了隐层调制在视线感知视频理解中的高效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25841v1/fig-001.webp\", \"caption\": \"Figure 1. GazeQwen overview. A small gaze resampler (∼1– 5 M parameters) encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into four evenly-spaced LLM decoder layers via forward hooks.\", \"page\": 2, \"index\": 1, \"width\": 496, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.25841v1/fig-002.webp\", \"caption\": \"Table 1. Results on the StreamGaze benchmark. We report accuracy on all 10 task types. Baseline numbers are from [11]; GazeQwen is our evaluation on the Qwen2.5-VL-7B backbone. Overall is the mean over the 10 tasks shown. Best per-task (excluding Human) is bolded.\", \"page\": 4, \"index\": 2, \"width\": 1026, \"height\": 566}]"
motivation: 现有的多模态大模型即使通过视觉覆盖或文本描述也无法有效利用视线信息进行视频理解。
method: 提出一种轻量级视线重采样器，将视频特征与注视点位置编码结合，通过前向钩子将残差注入 LLM 解码器层。
result: "在 StreamGaze 基准测试的 10 项任务中达到 63.9% 的准确率，比 GPT-4o 高出 10.5 个百分点。"
conclusion: 实验证明，学习在 LLM 内部注入视线信息比单纯增加模型规模或优化提示词工程更为有效。
---

## 摘要
当前的多模态大语言模型（MLLMs）无法有效利用眼动视线信息进行视频理解，即使视线线索是通过视觉叠加或文本描述提供的。我们提出了 GazeQwen，这是一种参数高效的方法，通过隐藏状态调制赋予开源 MLLM 视线感知能力。其核心是一个紧凑的视线重采样器（约 1-5 M 可训练参数），它将 V-JEPA 2.1 视频特征与源自注视点的位置编码共同编码，并生成加性残差，通过前向钩子注入到选定的大语言模型解码器层中。可选的第二阶段训练向大语言模型添加了低秩自适应（LoRA），以实现更紧密的集成。在 StreamGaze 基准测试的所有 10 个任务上进行的评估显示，GazeQwen 达到了 63.9% 的准确率，相比使用视线作为视觉提示的相同 Qwen2.5-VL-7B 骨干网络提升了 16.1 个百分点，相比 GPT-4o 提升了 10.5 个百分点，在所有测试的开源和闭源模型中得分最高。这些结果表明，学习在大语言模型中的何处注入视线信息，比扩大模型规模或设计更好的提示词更有效。所有代码和检查点均可在 https://github.com/phamtrongthang123/gazeqwen 获取。

## Abstract
Current multimodal large language models (MLLMs) cannot effectively utilize eye-gaze information for video understanding, even when gaze cues are supplied via visual overlays or text descriptions. We introduce GazeQwen, a parameter efficient approach that equips an open-source MLLM with gaze awareness through hidden-state modulation. At its core is a compact gaze resampler (~1-5 M trainable parameters) that encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into selected LLM decoder layers via forward hooks. An optional second training stage adds low-rank adapters (LoRA) to the LLM for tighter integration. Evaluated on all 10 tasks of the StreamGaze benchmark, GazeQwen reaches 63.9% accuracy, a +16.1 point gain over the same Qwen2.5-VL-7B backbone with gaze as visual prompts and +10.5 points over GPT-4o, the highest score among all open-source and proprietary models tested. These results suggest that learning where to inject gaze within an LLM is more effective than scaling model size or engineering better prompts. All code and checkpoints are available at https://github.com/phamtrongthang123/gazeqwen .

---

## 论文详细总结（自动生成）

### GazeQwen：用于流式视频理解的轻量级视线引导大语言模型调制

#### 1. 论文的核心问题与整体含义
在第一视角（Egocentric）视频理解中，用户的眼动视线（Gaze）是揭示注意力焦点和意图的关键线索。然而，现有的多模态大语言模型（MLLMs）即使在输入中加入视线坐标文本或视觉覆盖标记，也难以有效利用这些信息。论文指出，这种“表面层级”的提示工程（Prompt Engineering）不足以让模型建立视线与视频语义之间的深度关联。因此，本研究旨在探索一种更高效的机制，将视线信息直接集成到 MLLM 的内部表示中，以提升流式视频的实时理解能力。

#### 2. 论文提出的方法论
GazeQwen 的核心思想是**隐藏状态调制（Hidden-state Modulation）**，即不改变输入格式，而是通过外部模块干预 LLM 的中间层特征。
*   **核心组件：** 一个紧凑的“视线重采样器”（Gaze Resampler，约 1-5M 参数）。
*   **输入处理：**
    *   **视频特征：** 使用冻结的 V-JEPA 2.1 编码器提取时空特征，保留比 LLM 原生视觉骨干更细粒度的空间细节。
    *   **视线编码：** 将注视点坐标通过 DETR 风格的正弦位置编码（Sinusoidal Positional Encoding）转化为高维向量。
*   **特征融合：** 采用交叉注意力机制（Cross-attention），以视线编码引导注意力分布，使学习到的潜在向量（Latents）聚焦于视线相关的视频区域。
*   **注入机制：** 利用 PyTorch 的**前向钩子（Forward Hooks）**，将重采样器生成的加性残差注入到 LLM（如 Qwen2.5-VL）的四个等间距解码器层中。
*   **训练策略：** 采用两阶段训练。第一阶段仅训练重采样器；第二阶段引入 LoRA 适配器对 LLM 进行微调，以实现视线信号与语言逻辑的深度对齐。

#### 3. 实验设计
*   **数据集与 Benchmark：** 使用 **StreamGaze** 基准测试，包含 285 个第一视角视频、8,521 个视线条件下的 QA 对，涵盖 10 种任务类型（分为过去、现在、预测三大类）。
*   **对比方法：**
    *   **闭源模型：** GPT-4o, Claude 4 Sonnet/Opus（使用文本坐标提示）。
    *   **开源模型：** Qwen2.5-VL, InternVL3.5, VITA 1.5 等。
    *   **流式专用模型：** ViSpeak, Dispider, Flash-VStream 等。
    *   **视线专用微调模型：** AssistGaze。

#### 4. 资源与算力
*   **参数量：** 极其轻量，可训练参数仅为 5-9M（重采样器 1-5M + LoRA 约 3.5M）。
*   **算力需求：** 论文明确提到该方法支持**单 GPU 训练**（Single-GPU training），具有极高的计算效率。
*   **未明确信息：** 文中未具体说明 GPU 的型号（如 A100 或 H100）以及具体的训练总时长。

#### 5. 实验数量与充分性
*   **实验规模：** 评估涵盖了 StreamGaze 的全部 10 个子任务，并进行了详细的性能拆解。
*   **消融实验：** 针对四个关键设计维度进行了充分的消融研究：(1) 视线编码方式（坐标 vs. 热力图）；(2) 视觉骨干网络（V-JEPA vs. DINOv2）；(3) 层共享策略（独立层 vs. 共享层）；(4) LLM 适配方式（LoRA 的影响）。
*   **客观性：** 实验采用了统一的测试集划分（70/15/15），避免了数据泄露，并与当前最先进的闭源和开源模型进行了直接对比，结果具有较强的说服力。

#### 6. 论文的主要结论与发现
*   **性能突破：** GazeQwen 达到了 **63.9%** 的总准确率，比 GPT-4o 高出 10.5%，比同骨干的 Qwen2.5-VL（使用视觉提示）高出 16.1%。
*   **调制优于提示：** 实验证明，在 LLM 内部学习如何注入视线信息，比单纯增加模型规模或优化 Prompt 更加有效。
*   **任务敏感性：** 视线信息在物体追踪（OTP）、属性识别（OAR）和主动提醒（OAA）等空间敏感型任务中提升最显著，但在简单物体识别（OI-Easy）中提升有限。

#### 7. 优点
*   **轻量且模块化：** 仅需极少量参数即可赋予大模型视线感知能力，且视线模块可随时拆卸，不影响原模型功能。
*   **非侵入式架构：** 通过 Hook 机制注入，无需修改 LLM 的原始代码或架构。
*   **高性能：** 在特定领域（视线引导理解）超越了规模大得多的闭源模型。
*   **时空对齐：** 成功利用 V-JEPA 的时空特征弥补了 LLM 视觉编码器在细粒度空间定位上的不足。

#### 8. 不足与局限
*   **依赖外部编码器：** 需要额外的 V-JEPA 骨干网络，增加了推理时的视觉处理开销。
*   **任务局限性：** 对于不需要空间消歧的任务（如全局场景回忆或简单动作预测），视线信息的引入带来的边际收益较小。
*   **应用场景：** 目前主要针对第一视角视频，在第三视角或非交互式视频中的泛化能力尚未验证。
*   **实时性细节：** 虽然声称适用于流式视频，但未详细给出端到端的推理延迟数据。

（完）
