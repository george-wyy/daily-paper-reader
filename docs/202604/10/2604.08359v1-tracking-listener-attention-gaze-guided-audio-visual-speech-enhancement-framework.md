---
title: "Tracking Listener Attention: Gaze-Guided Audio-Visual Speech Enhancement Framework"
title_zh: 追踪听者注意力：视线引导的音视频语音增强框架
authors: "Hsiang-Cheng Yang, You-Jin Li, Rong Chao, Yu Tsao, Borching Su, Shao-Yi Chien"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08359v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 将注视方向作为目标说话人选择意图的监督信号
tldr: 本研究提出了一种注视引导的音视频语音增强（GG-AVSE）框架，旨在解决多说话人环境下的“鸡尾酒会效应”。该框架利用听者的注视方向作为监督信号来确定目标说话人，通过GG-VM模块结合YOLO5Face提取特征，并与预训练的AVSEMamba模型集成。实验表明，该方法在PESQ、STOI和SI-SDR等指标上均有显著提升，有效解决了目标说话人歧义问题，展示了其在现实场景中的应用潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08359v1/fig-001.webp\", \"caption\": \"Fig. 3. AVSE with two simultaneous speakers under gaze guidance. From top to bottom: Ground Truth, Mixed, AVSEA, and GG-AVSE. Demo link: https://jinliyou1991.github.io/GG-AVSE-Demo/\", \"page\": 4, \"index\": 1, \"width\": 510, \"height\": 654}, {\"url\": \"assets/figures/arxiv/2604.08359v1/fig-002.webp\", \"caption\": \"Fig. 2. System architecture of the proposed GG-AVSE model.\", \"page\": 3, \"index\": 2, \"width\": 924, \"height\": 369}]"
motivation: 传统音视频语音增强技术在多说话人环境下难以准确识别听者真正想要关注的目标说话人。
method: 提出GG-VM模块，利用注视信号和人脸检测定位目标并提取视觉特征，通过零样本合并或局部微调与AVSEMamba模型结合。
result: "在AVSEC2-Gaze数据集上，该框架相比无注视基准在PESQ、STOI和SI-SDR指标上分别提升了10.08%、5.18%和23.69%。"
conclusion: 注视方向为解决目标说话人歧义提供了有效线索，证明了GG-AVSE框架在处理复杂音频环境时的优越性和可扩展性。
---

## 摘要
本文提出了一种视线引导的音视频语音增强（GG-AVSE）框架，旨在解决鸡尾酒会效应问题。传统音视频语音增强（AVSE）面临的一个主要挑战是在多说话人环境中识别听者意图关注的目标说话人。GG-AVSE 通过利用视线方向作为目标说话人选择的监督线索来解决这一问题。具体而言，我们提出了 GG-VM 模块，该模块将视线信号与 YOLO5Face 检测器相结合以提取目标说话人的面部特征，并通过零样本合并（zero-shot merging）和部分视觉微调（partial visual fine-tuning）两种策略将其与预训练的 AVSEMamba 模型集成。为了进行评估，我们引入了 AVSEC2-Gaze 数据集。实验结果表明，GG-AVSE 相比于无视线引导的基准模型取得了显著的性能提升：PESQ 提升了 10.08%（从 2.370 提升至 2.609），STOI 提升了 5.18%（从 0.8802 提升至 0.9258），SI-SDR 提升了 23.69%（从 9.16 提升至 11.33）。这些结果证实了视线为解决目标说话人歧义提供了有效的线索，并突显了 GG-AVSE 在实际应用中的可扩展性。

## Abstract
This paper presents a Gaze-Guided Audio-Visual Speech Enhancement (GG-AVSE) framework to address the cocktail party problem. A major challenge in conventional AVSE is identifying the listener's intended speaker in multi-talker environments. GG-AVSE addresses this issue by exploiting gaze direction as a supervisory cue for target-speaker selection. Specifically, we propose the GG-VM module, which combines gaze signals with a YOLO5Face detector to extract the target speaker's facial features and integrates them with the pretrained AVSEMamba model through two strategies: zero-shot merging and partial visual fine-tuning. For evaluation, we introduce the AVSEC2-Gaze dataset. Experimental results show that GG-AVSE achieves substantial performance gains over gaze-free baselines: a 10.08% improvement in PESQ (2.370 to 2.609), a 5.18% improvement in STOI (0.8802 to 0.9258), and a 23.69% improvement in SI-SDR (9.16 to 11.33). These results confirm that gaze provides an effective cue for resolving target-speaker ambiguity and highlight the scalability of GG-AVSE for real-world applications.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GG-AVSE** 的创新框架，旨在通过引入听者的视线（Gaze）信息来解决多说话人环境下的语音增强难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在复杂的“鸡尾酒会”环境（多说话人同时发声）中，传统的音视频语音增强（AVSE）系统虽然能利用视觉信息辅助降噪，但往往假设输入的视频流就是目标说话人。在现实场景中，视野内可能存在多个说话人，系统难以自动判断听者到底想听谁的声音。
*   **研究动机**：人类在嘈杂环境中会自然地注视感兴趣的说话人。论文提出利用听者的**视线方向**作为一种直观、实时的监督信号，来动态选择并增强目标说话人的语音。

### 2. 方法论
该框架由两个核心组件组成：**GG-VM（视线引导视觉模块）**和 **GG-AVSE 增强模型**。
*   **GG-VM 模块**：
    *   **视线追踪**：使用 Ganzin Sol 智能眼镜获取 120Hz 的实时视线坐标。
    *   **人脸捕获**：利用 **YOLO5Face** 检测器识别视频中的所有人脸。
    *   **目标关联**：设计了一个匹配评分公式：$Score(i) = \gamma \cdot D(i) + (1 - \gamma) \cdot IoU(i)$。其中 $D$ 是视线点与人脸中心的欧氏距离倒数，$IoU$ 是视线区域与人脸框的重叠度。通过该评分锁定目标说话人。
*   **GG-AVSE 框架**：
    *   **骨干网络**：采用 **AVSEMamba**，这是一种基于选择性状态空间模型（Mamba）的高效架构，具有线性计算复杂度，适合处理长序列。
    *   **集成策略**：
        1.  **零样本合并（Zero-Shot Merge）**：直接将 GG-VM 提取的特征输入预训练模型，无需重新训练。
        2.  **部分视觉微调（PVFT）**：使用 **SimCLR** 对比学习框架对视觉编码器进行微调，以弥补穿戴式设备视角与预训练数据集之间的领域差距（Domain Gap）。

### 3. 实验设计
*   **数据集**：构建了 **AVSEC2-Gaze** 数据集。
    *   基于 LRS3（语音）、CEC1、DEMAND 和 DNS（噪声）语料库合成。
    *   模拟双人混合场景，并记录人类参与者的视线坐标。
    *   包含 1000 个训练样本和 200 个测试样本。
*   **基准测试（Benchmark）**：
    *   **Noisy**：原始带噪语音。
    *   **AVSE**：传统的固定目标音视频增强模型。
    *   **AVSE NL**：无视线过滤、直接输入多说话人视觉特征的模型。
*   **评估指标**：PESQ（感知语音质量）、STOI（可懂度）、SI-SDR（尺度不变信号失真比）。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量及训练时长。
*   文中提到了使用 Ganzin Sol Glasses 硬件设备进行数据采集，并强调了 Mamba 架构在推理速度和内存扩展性上的优势，暗示了其对低算力穿戴设备的友好性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了不同数据集（AVSEC-2, AVSEC2-Gaze）下的表现。
    *   进行了消融实验，对比了零样本合并与微调（PVFT）的效果。
    *   专门设计了**说话人切换场景**实验（从目标 A 切换到目标 B），验证系统的动态追踪能力。
*   **充分性评价**：实验设计较为客观，涵盖了静态目标和动态切换两种关键场景。虽然数据集规模（1200 样本）相对较小，但对于验证“视线引导”这一概念的有效性是足够的。

### 6. 主要结论与发现
*   **性能显著提升**：在多说话人干扰下，GG-AVSE FT（微调版）相比无视线引导的基准，PESQ 提升 10.08%，STOI 提升 5.18%，SI-SDR 提升 23.69%。
*   **解决歧义性**：实验证明，如果没有视线引导（AVSE NL），模型在多说话人环境下性能会大幅下降，而视线信息能精准锁定目标。
*   **动态追踪有效**：在说话人切换实验中，GG-AVSE 能随视线转移迅速切换增强对象，而固定目标模型则会失效。

### 7. 优点
*   **交互自然**：利用视线作为 UI，符合人类自然的听觉注意力机制。
*   **架构高效**：结合了 Mamba 架构，解决了 Transformer 在处理长音频序列时的计算瓶颈。
*   **即插即用**：提出的零样本合并策略证明了该模块可以较好地适配现有的预训练 AVSE 模型。

### 8. 不足与局限
*   **硬件依赖**：系统高度依赖高精度的视线追踪硬件（如智能眼镜），这限制了其在普通智能手机或无穿戴设备场景下的应用。
*   **场景复杂度有限**：实验主要集中在双人混合（2-speaker mixtures）场景，对于更拥挤（如 3 人以上）或背景极其复杂的极端环境验证不足。
*   **数据集规模**：AVSEC2-Gaze 数据集规模较小，可能存在过拟合特定拍摄视角的风险。

（完）
