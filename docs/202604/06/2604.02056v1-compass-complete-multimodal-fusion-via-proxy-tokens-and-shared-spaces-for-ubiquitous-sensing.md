---
title: "COMPASS: Complete Multimodal Fusion via Proxy Tokens and Shared Spaces for Ubiquitous Sensing"
title_zh: COMPASS：通过代理令牌和共享空间实现泛在感知的完整多模态融合
authors: "Hao Wang, Yanyu Qian, Pengcheng Weng, Zixuan Xia, William Dan, Yangxin Xu, Fei Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02056v1"
tags: ["query:gaze-intent"]
score: 6.0
evidence: 针对缺失模态的泛在感知多模态融合框架
tldr: 针对多模态感知中模态缺失导致的融合性能下降，本文提出COMPASS框架。该框架基于“融合完整性”原则，通过在共享潜空间中利用观测模态合成缺失模态的代理Token，确保融合头始终接收固定结构的输入。通过代理对齐和任务监督，COMPASS在多个感知数据集上显著优于现有方法，证明了保持完整融合接口对提升系统鲁棒性的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02056v1/fig-001.webp\", \"caption\": \"Figure 3: Representation geometry on XRF55. (a) X-Fi embeddings colored by modality: three modalities remain separated, indicating weak shared-space alignment. (b) COMPASS embeddings: modality boundaries are substantially reduced, enabling reliable cross-modal proxy transfer. (c) Quantitative comparison: each point is one class; x-axis is cross-modal compactness (cosine similarity between same-class centroids from different modalities), y-axis is inter-class separation (minimum cosine distance to other class centroids). COMPASS improves both alignment and separation.\", \"page\": 8, \"index\": 1, \"width\": 1056, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.02056v1/fig-002.webp\", \"caption\": \"Figure 2: The COMPASS framework, shown with modality 𝑣 𝑗 missing at inference. Each observed modality 𝑣𝑖 passes through its encoder 𝐸𝑖 and projection 𝑃𝑖 , producing a token sequence z𝑖 ∈ R𝐿×𝑑 in a shared latent space; mean-pooling then gives a global token z̄𝑖 ∈ R𝑑 . When 𝑣 𝑗 is absent, each observed source feeds z𝑖 into a directed generator𝐺𝑖→𝑗 (a single-layer Transformer with a learnable proxy query), and the per-source outputs are mean-averaged (⊕) into one proxy token t̂𝑗 for the missing slot. Every slot receives exactly one token—a real global token or an aggregated proxy—so the fusion input is always 𝑁 tokens. These are summed (Σ), layer-normalized, and fed to the task head for prediction ŷ. Dashed lines mark training-only signals: Lalign pulls each proxy toward the real target z̄𝑗 (available via synthetic masking), while Lss (VICReg) regularizes pairs of observed global tokens.\", \"page\": 3, \"index\": 2, \"width\": 1056, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2604.02056v1/fig-003.webp\", \"caption\": \"Table 3: OctoNet HAR accuracy (%) for all 31 modality combinations under the in-distribution protocol. Modalities: IMU (I), UWB (U), WiFi (W), ToF (T), mmWave (M). Best per row in bold. ∗X-Fi collapses on ToF-only due to its 8×8 input producing only one post-ResNet-18 token.\", \"page\": 7, \"index\": 3, \"width\": 422, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2604.02056v1/fig-004.webp\", \"caption\": \"Table 1: HAR accuracy (%) on the MM-Fi. “Baseline” denotes the feature-level fusion baseline retrained per combination. X-Fi and COMPASS each use a singlemodel for all combinations. Modalities: Image (I), Depth (D), LiDAR (L), Radar (R).\", \"page\": 7, \"index\": 4, \"width\": 422, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.02056v1/fig-005.webp\", \"caption\": \"Figure 1: Comparison of fusion input layouts under different missing-modality strategies. (a) All modalities present: the fusion head receives the full token set. (b) Skip: absent modalities are dropped, changing the fusion input structure. (c) Imputation: a single observed modality generates a proxy token for the missing slot. (d) COMPASS: all observed modalities contribute directed proxy tokens, which are aggregated into each missing slot, preserving the fixed 𝑁 -token fusion interface.\", \"page\": 2, \"index\": 5, \"width\": 1056, \"height\": 182}]"
motivation: 现有方法在模态缺失时会改变融合头的输入结构，导致训练与推理不一致并损害跨模态交互。
method: 提出一种保持固定输入槽位的框架，利用共享空间内的成对生成器合成缺失模态的代理Token，并辅以对齐和判别性监督。
result: 在XRF55、MM-Fi和OctoNet等数据集的单模态及多模态缺失场景下，COMPASS均取得了领先的性能。
conclusion: 实验结果表明，维持模态完整的融合接口是实现鲁棒多模态感知的一种简单且有效的设计原则。
---

## 摘要
模态缺失仍然是多模态感知面临的主要挑战，因为大多数现有方法通过丢弃缺失分支、使用特定子集的融合或重建缺失特征，使融合过程适应观测到的子集。结果，融合头接收到的输入结构往往与训练时不同，导致融合不完整以及跨模态交互性能下降。我们提出了 COMPASS，这是一个基于融合完整性原则构建的缺失模态融合框架：融合头始终接收固定的 N 插槽多模态输入，每个模态插槽对应一个令牌。对于每个缺失模态，COMPASS 在共享潜空间中使用成对的源到目标生成器，从观测模态中合成特定目标的代理令牌，并将其聚合为单个替换令牌。为了使这些代理既具备表示兼容性又包含任务信息，我们结合了代理对齐、共享空间正则化以及针对每个代理的判别性监督。在 XRF55、MM-Fi 和 OctoNet 数据集上进行的多种单模态和多模态缺失设置下的实验表明，COMPASS 在绝大多数场景中均优于现有方法。我们的结果表明，保持模态完整的融合接口是实现鲁棒多模态感知的一种简单且有效的设计原则。

## Abstract
Missing modalities remain a major challenge for multimodal sensing, because most existing methods adapt the fusion process to the observed subset by dropping absent branches, using subset-specific fusion, or reconstructing missing features. As a result, the fusion head often receives an input structure different from the one seen during training, leading to incomplete fusion and degraded cross-modal interaction. We propose COMPASS, a missing-modality fusion framework built on the principle of fusion completeness: the fusion head always receives a fixed N-slot multimodal input, with one token per modality slot. For each missing modality, COMPASS synthesizes a target-specific proxy token from the observed modalities using pairwise source-to-target generators in a shared latent space, and aggregates them into a single replacement token. To make these proxies both representation-compatible and task-informative, we combine proxy alignment, shared-space regularization, and per-proxy discriminative supervision. Experiments on XRF55, MM-Fi, and OctoNet under diverse single- and multiple-missing settings show that COMPASS outperforms prior methods on the large majority of scenarios. Our results suggest that preserving a modality-complete fusion interface is a simple and effective design principle for robust multimodal sensing.