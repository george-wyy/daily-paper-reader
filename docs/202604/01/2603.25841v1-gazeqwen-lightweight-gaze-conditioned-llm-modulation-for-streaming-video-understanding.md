---
title: "GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding"
title_zh: GazeQwen：面向流式视频理解的轻量级视线条件 LLM 调制
authors: "Trong Thang Pham, Hien Nguyen, Ngan Le"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25841v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 利用注视点调节隐藏状态的多模态视频理解，适用于流式数据。
tldr: 针对多模态大模型在视频理解中难以有效利用视线信息的问题，本文提出 GazeQwen。该方法通过轻量级的视线重采样器（1-5M参数）将视线特征编码为残差，并注入到 LLM 解码器层中。实验表明，这种隐藏状态调制方式在 StreamGaze 基准测试上显著优于视觉提示工程和 GPT-4o，为流式视频理解提供了高效的视线感知方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25841v1/fig-001.webp\", \"caption\": \"Figure 1. GazeQwen overview. A small gaze resampler (∼1– 5 M parameters) encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into four evenly-spaced LLM decoder layers via forward hooks.\", \"page\": 2, \"index\": 1, \"width\": 496, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.25841v1/fig-002.webp\", \"caption\": \"Table 1. Results on the StreamGaze benchmark. We report accuracy on all 10 task types. Baseline numbers are from [11]; GazeQwen is our evaluation on the Qwen2.5-VL-7B backbone. Overall is the mean over the 10 tasks shown. Best per-task (excluding Human) is bolded.\", \"page\": 4, \"index\": 2, \"width\": 1026, \"height\": 566}]"
motivation: 现有的多模态大模型即使通过视觉覆盖或文本描述也无法有效利用视线信息来增强视频理解。
method: 提出一种轻量级的视线重采样器，将视频特征与视线位置编码结合，通过前向钩子将残差注入 LLM 解码器层。
result: "在 StreamGaze 基准测试中达到 63.9% 的准确率，比 Qwen2.5-VL-7B 基准提升 16.1 个百分点，且超越了 GPT-4o。"
conclusion: 学习在 LLM 内部何处注入视线信息比单纯扩大模型规模或优化提示词工程更为有效。
---

## 摘要
当前的多模态大语言模型 (MLLM) 无法有效地利用视线 (eye-gaze) 信息进行视频理解，即使视线线索是通过视觉叠加或文本描述提供的。我们介绍了 GazeQwen，这是一种参数高效的方法，通过隐藏状态调制 (hidden-state modulation) 使开源 MLLM 具备视线感知能力。其核心是一个紧凑的视线重采样器（约 1-5 M 可训练参数），它将 V-JEPA 2.1 视频特征与源自注视点的定位编码共同编码，并产生加性残差，通过前向钩子 (forward hooks) 注入到选定的 LLM 解码器层中。一个可选的第二训练阶段向 LLM 添加了低秩自适应器 (LoRA)，以实现更紧密的集成。在 StreamGaze 基准测试的所有 10 个任务中，GazeQwen 达到了 63.9% 的准确率，相比于使用视线作为视觉提示的相同 Qwen2.5-VL-7B 主干网络提升了 16.1 个百分点，相比 GPT-4o 提升了 10.5 个百分点，在所有测试的开源和闭源模型中得分最高。这些结果表明，学习在 LLM 的何处注入视线比扩大模型规模或设计更好的提示词更有效。所有代码和检查点均可在 https://github.com/phamtrongthang123/gazeqwen 获取。

## Abstract
Current multimodal large language models (MLLMs) cannot effectively utilize eye-gaze information for video understanding, even when gaze cues are supplied via visual overlays or text descriptions. We introduce GazeQwen, a parameter efficient approach that equips an open-source MLLM with gaze awareness through hidden-state modulation. At its core is a compact gaze resampler (~1-5 M trainable parameters) that encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into selected LLM decoder layers via forward hooks. An optional second training stage adds low-rank adapters (LoRA) to the LLM for tighter integration. Evaluated on all 10 tasks of the StreamGaze benchmark, GazeQwen reaches 63.9% accuracy, a +16.1 point gain over the same Qwen2.5-VL-7B backbone with gaze as visual prompts and +10.5 points over GPT-4o, the highest score among all open-source and proprietary models tested. These results suggest that learning where to inject gaze within an LLM is more effective than scaling model size or engineering better prompts. All code and checkpoints are available at https://github.com/phamtrongthang123/gazeqwen .

---

## 论文详细总结（自动生成）

这是一份关于论文《GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理第一视角（Egocentric）视频时，无法有效利用**人类视线（Eye-gaze）**信息。即使通过文本坐标、视觉覆盖（如红点）或显著性图进行提示，模型的表现仍远低于人类水平。
*   **研究背景**：视线反映了用户的注意力焦点和意图，对增强现实（AR）助手和具身智能至关重要。现有的 StreamGaze 基准测试显示，即使是 GPT-4o 等顶级模型，在视线引导的视频理解上也存在巨大鸿沟。
*   **整体含义**：本文认为视线信息不应仅作为“输入提示”，而应通过“内部调制”直接干预 LLM 的中间表示。

### 2. 方法论
GazeQwen 采用了一种轻量级的**隐藏状态调制（Hidden-state Modulation）**方案：
*   **核心思想**：不修改 LLM 架构，而是通过一个紧凑的“视线重采样器”生成残差，利用 PyTorch 的前向钩子（Forward Hooks）将其注入 LLM 的特定层。
*   **关键技术细节**：
    *   **特征提取**：使用冻结的 **V-JEPA 2.1** 编码器提取视频特征（相比 LLM 自带视觉编码器，它保留了更多细粒度的时空细节）。
    *   **视线编码**：将注视点坐标转换为 **DETR 风格的正弦位置编码**，以保持亚像素级的空间精度。
    *   **视线重采样器（Resampler）**：通过交叉注意力机制将视频特征与视线编码融合。特别之处在于将视线信息加入到 **Keys** 中，从而引导注意力权重向注视区域倾斜，而不改变原始视觉语义（Values）。
    *   **多层注入**：在 LLM 的 4 个等间距解码层（如第 6, 13, 20, 27 层）注入加性残差，使视线能同时影响低层空间布局和高层语义理解。
*   **训练流程**：
    *   **阶段 1**：仅训练重采样器（1-5M 参数），LLM 保持冻结，防止灾难性遗忘。
    *   **阶段 2**：引入 **LoRA** 适配器（约 3.5M 参数）与重采样器联合微调，使 LLM 更好地吸收注入的信号。

### 3. 实验设计
*   **数据集/基准**：**StreamGaze**，包含 285 个第一视角视频、8,521 个视线条件下的问答对。
*   **任务分类**：涵盖 10 类任务，分为“过去”（如物体追踪）、“现在”（如属性识别）和“前瞻性”（如物体出现预警）三大类。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 4 Sonnet/Opus。
    *   **开源模型**：Qwen2.5-VL, InternVL3.5, VITA 1.5 等。
    *   **流式/视线专用模型**：ViSpeak, AssistGaze 等。

### 4. 资源与算力
*   **算力需求**：论文强调该方法是“低成本”且“参数高效”的。
*   **具体配置**：明确提到支持**单 GPU 训练**。虽然未列出具体的 GPU 型号（如 A100 或 H100）和总训练时长，但其可训练参数量极小（总计约 5-9M），远低于全量微调的需求。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同的视频理解任务上进行了全面评估。
*   **消融实验**：针对四个关键设计维度进行了充分的消融研究：
    1.  **视线编码方式**（坐标编码 vs. 热力图）。
    2.  **视觉主干网络**（V-JEPA vs. DINOv2）。
    3.  **层共享策略**（独立层重采样器 vs. 共享重采样器）。
    4.  **LLM 适配方式**（有无 LoRA）。
*   **客观性**：通过与 10 余种主流模型对比，并使用统一的 StreamGaze 划分标准，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能突破**：GazeQwen 达到了 **63.9%** 的准确率，比 Qwen2.5-VL 基准提升了 **16.1%**，甚至超越了 GPT-4o（53.4%）。
*   **关键发现**：
    *   **内部调制优于提示工程**：在 LLM 内部注入信号比在输入端提供视觉标记更有效。
    *   **视线对空间去歧义贡献最大**：在“物体出现预警”（+34.1%）和“物体追踪”（+29.4%）等任务中提升最显著。
    *   **V-JEPA 的优越性**：其时空特征比 DINOv2 更适合视线对齐任务。

### 7. 优点
*   **轻量且模块化**：仅需极少量参数，且视线模块可以随时“挂载”或“卸载”，无需重新加载 LLM 权重。
*   **非侵入式**：通过 Forward Hooks 实现，无需修改 LLM 的源代码架构。
*   **精度高**：正弦位置编码保留了比热力图更精准的视线位置信息。

### 8. 不足与局限
*   **人类差距依然存在**：尽管表现优异，但 63.9% 的准确率仍远低于人类的 82.7%，说明模型在复杂推理上仍有提升空间。
*   **高层逻辑任务提升有限**：在“未来动作预测”（FAP）等需要深度时间推理的任务中，视线信息的加入带来的提升较小（仅 +1.7%）。
*   **依赖外部编码器**：需要额外运行一个 V-JEPA 编码器，虽然提升了精度，但也增加了推理时的计算链路。

（完）
