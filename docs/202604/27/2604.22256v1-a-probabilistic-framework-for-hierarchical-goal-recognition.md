---
title: A Probabilistic Framework for Hierarchical Goal Recognition
title_zh: 一种分层目标识别的概率框架
authors: "Chenyuan Zhang, Katherine Ip, Hamid Rezatofighi, Buser Say, Mor Vered"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22256v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 分层目标识别的概率框架
tldr: 本研究针对目标识别中缺乏结合层次化任务结构与概率推理的问题，提出了首个基于分层任务网络（HTN）的概率规划框架。该框架利用HTN规划器和三阶段生成模型进行似然估计，从而计算目标假设的后验分布。实验证明，该方法在HTN基准测试上的表现优于现有识别器，为处理复杂、不确定环境下的目标识别奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22256v1/fig-001.webp\", \"caption\": \"Table 1: top-k accuracy. For top-5 accuracy, Full-Simplified and Partial-Simplified coincide with Full-Three-Stage and PartialThree-Stage, respectively. The average number of valid hypotheses is identical within each setting and reported in the last two rows.\", \"page\": 8, \"index\": 1, \"width\": 500, \"height\": 431}]"
motivation: 现有的目标识别方法未能有效结合层次化任务结构与概率推理，难以应对现实场景中的复杂性和不确定性。
method: 提出一种基于HTN的概率框架，通过三阶段生成模型和HTN规划器来估计似然度并推导目标的后验分布。
result: 在HTN基准测试中，该框架的识别性能显著优于现有的基于HTN的非概率目标识别方法。
conclusion: 该框架为基于层次化规划结构的概率目标识别提供了理论基础，推动了目标识别技术向更具实用性的方向发展。
---

## 摘要
目标识别旨在从智能体行为的观测中推断其目标。在现实场景中，识别过程可以受益于利用分层任务结构和在不确定性下进行推理。基于规划的目标识别在过去十年中取得了实质性进展，但据我们所知，目前尚无方法能将分层任务结构与概率推理结合起来。在本文中，我们提出了首个基于规划的概率框架，用于分层任务网络（HTN）上的分层目标识别。我们通过利用一个带有三阶段生成模型（用于似然估计）的 HTN 规划器来实例化该框架，从而产生目标假设的后验分布。实验结果表明，在 HTN 基准测试中，该框架的识别性能优于现有的基于 HTN 的识别器。总的来说，该框架为基于分层规划结构的概率目标识别奠定了基础，推动了目标识别向更实际的应用场景发展。

## Abstract
Goal recognition aims to infer an agent's goal from observations of its behaviour. In realistic settings, recognition can benefit from exploiting hierarchical task structure and reasoning under uncertainty. Planning-based goal recognition has made substantial progress over the past decade, but to the best of our knowledge no existing approach jointly integrates hierarchical task structure with probabilistic inference. In this paper, we introduce the first planning-based probabilistic framework for hierarchical goal recognition over Hierarchical Task Networks (HTNs). We instantiate the framework by exploiting an HTN planner with a three-stage generative model for likelihood estimation, yielding posterior distributions over goal hypotheses. Empirical results show improved recognition performance over the existing HTN-based recognizer on HTN benchmarks. Overall, the framework lays a foundation for probabilistic goal recognition grounded in hierarchical planning structure, moving goal recognition toward more practical settings.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个用于分层目标识别（Hierarchical Goal Recognition, HGR）的概率框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在目标识别中同时结合**分层任务结构**（Hierarchical Task Structure）与**概率推理**（Probabilistic Inference）。
*   **研究背景**：
    *   人类在识别他人目标时擅长利用行为的分层特性（如“做汤”包含“切菜”等子任务）。
    *   现有的基于规划的目标识别方法要么是概率性的但仅支持扁平化动作模型，要么是支持分层任务网络（HTN）但仅能给出确定性的（是/否）判断，无法处理观测中的噪声、不确定性或衡量不同假设的可能性。
*   **整体含义**：该论文填补了分层规划与贝叶斯推理结合的空白，提出了首个基于 HTN 的概率目标识别框架。

### 2. 方法论
该论文将 HTN 目标识别建模为贝叶斯推理问题，核心在于计算后验概率 $P(N_g | \hat{o}, s_0)$。
*   **三阶段生成模型**：用于估计似然度 $P(\hat{o} | N_g, s_0)$：
    1.  **网络分解（Stage I）**：使用玻尔兹曼分布（Softmax）根据方法成本对任务分解路径进行建模，倾向于选择成本较低的分解方式。
    2.  **可执行线性化（Stage II）**：在给定分解后的原始任务网络下，通过均匀采样可执行的动作序列来生成规划。
    3.  **观测模型（Stage III）**：考虑观测的进度先验和对齐似然，允许观测序列是执行序列的子序列。
*   **似然度近似估计**：由于精确计算所有可能的分解和执行路径是不可行的，论文采用了一种比率近似法：
    *   计算“观测一致的最优执行”与“无约束的最优执行”的概率比值。
*   **Top-K 假设选择**：为了应对大规模假设空间和规划器的不完备性，先利用 HTN 规划器筛选出 K 个最有潜力的假设，再对这些假设进行详细的后验概率计算。
*   **外生动作处理**：通过引入“任务插入”（Task Insertion）语义，允许框架处理那些不属于目标分解路径的干扰动作（噪声）。

### 3. 实验设计
*   **数据集/场景**：
    1.  **Kitchen Domain（厨房领域）**：包含 1000 多种目标组合（前菜、主菜、甜点），具有高度的子任务重叠。
    2.  **Monroe Domain（门罗领域）**：经典的灾难救援规划基准测试。
*   **Benchmark 与对比方法**：
    *   **Baseline**：Höller 等人 (2018a) 提出的基于 HTN 的确定性识别器（目前唯一的 HTN 目标识别参照）。
    *   **对比变体**：论文对比了“完整三阶段模型”与“简化的基于成本的玻尔兹曼分布模型”。
*   **评估指标**：Top-1, Top-3, Top-5 准确率，涵盖了从 20% 到 80% 的不同观测比例。

### 4. 资源与算力
*   **硬件环境**：MacBook Pro，配备 10 核 CPU，16 GB 内存。
*   **算力消耗**：
    *   实验主要依赖 CPU 计算（HTN 规划通常是搜索密集型而非显存密集型）。
    *   每个实例设置了 3 分钟的时间限制。
    *   在 Kitchen 领域，概率模型的平均运行时间为 23.9 秒/实例，而基准方法为 5.1 秒/实例。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的领域（厨房、救援）进行了测试，涵盖了全观测和部分观测（随机移除 20% 动作）场景。
*   **充分性评价**：
    *   **较为充分**：通过不同观测比例的曲线展示了模型在识别早期的优势。
    *   **客观性**：论文诚实地指出了模型在观测后期（当智能体行为高度符合成本最优假设时）简化模型的表现可能反超三阶段模型。
    *   **局限性**：外生动作的实验仅作为“健全性检查”（Sanity Check）在一个修改后的实例上进行，缺乏大规模的量化评估（受限于目前缺乏支持任务插入的成熟 HTN 规划器）。

### 6. 主要结论与发现
*   **性能提升**：概率框架在 Top-k 准确率上显著优于确定性基准方法，尤其是在观测数据较少的早期阶段。
*   **鲁棒性**：三阶段生成模型对非成本最优的行为具有更好的容错性。
*   **排序价值**：在存在多个有效假设的情况下，概率框架能给出合理的可能性排序，而基准方法只能随机返回一个。
*   **外生动作**：该框架是首个在理论和实践上能处理分层结构中外生动作（噪声）的识别框架。

### 7. 优点
*   **理论完备**：首次将贝叶斯推理与 HTN 规划深度融合，提供了严谨的数学定义。
*   **实用性强**：通过 Top-K 策略和现成的 HTN 规划器实现了可扩展的推理。
*   **直觉对齐**：模型能够捕捉“越不令人惊讶的解释概率越高”这一人类直觉。

### 8. 不足与局限
*   **计算开销**：由于需要多次调用 HTN 规划器（每个假设至少两次），计算成本比确定性方法高出数倍。
*   **规划器依赖**：识别性能受限于底层 HTN 规划器的完备性和效率；若规划器超时，则无法获得准确的后验分布。
*   **模型偏差**：生成模型假设智能体按某种概率分布行动，如果真实智能体的行为模式（如极度追求成本最优）与假设不符，可能会出现模型失配。
*   **外生动作实验不足**：由于缺乏高效的任务插入规划器，对外生动作的处理能力尚未在复杂大规模场景下得到充分验证。

（完）
