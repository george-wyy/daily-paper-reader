---
title: "GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding"
title_zh: GazeQwen：用于流式视频理解的轻量级注视条件 LLM 调制
authors: "Trong Thang Pham, Hien Nguyen, Ngan Le"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25841v1"
tags: ["query:gaze-intent"]
score: 10.0
evidence: 用于流式视频理解和注视感知的注视调节LLM方法
tldr: 针对多模态大模型在视频理解中难以有效利用视线（gaze）信息的问题，本文提出 GazeQwen。该方法通过轻量级的视线重采样器将视线特征编码为残差，并注入到 LLM 解码器层中。实验表明，这种隐藏状态调制方式在 StreamGaze 基准测试上显著优于视觉提示工程和 GPT-4o 等闭源模型，证明了精准注入视线信息比单纯增加模型规模更有效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25841v1/fig-001.webp\", \"caption\": \"Figure 1. GazeQwen overview. A small gaze resampler (∼1– 5 M parameters) encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into four evenly-spaced LLM decoder layers via forward hooks.\", \"page\": 2, \"index\": 1, \"width\": 496, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.25841v1/fig-002.webp\", \"caption\": \"Table 1. Results on the StreamGaze benchmark. We report accuracy on all 10 task types. Baseline numbers are from [11]; GazeQwen is our evaluation on the Qwen2.5-VL-7B backbone. Overall is the mean over the 10 tasks shown. Best per-task (excluding Human) is bolded.\", \"page\": 4, \"index\": 2, \"width\": 1026, \"height\": 566}]"
motivation: 现有的多模态大模型即使通过视觉覆盖或文本描述也无法有效利用视线信息来增强视频理解。
method: 提出一种轻量级的视线重采样器，将视频特征与注视点位置编码结合，通过前向钩子将残差注入 LLM 解码器层。
result: "在 StreamGaze 基准测试中达到 63.9% 的准确率，比 Qwen2.5-VL-7B 基准提升 16.1 个百分点，并超越了 GPT-4o。"
conclusion: 学习在 LLM 内部何处注入视线信息比扩大模型规模或设计更好的提示词更为有效。
---

## 摘要
当前的多模态大语言模型（MLLM）无法有效地利用眼动注视信息进行视频理解，即使注视线索是通过视觉叠加或文本描述提供的。我们介绍了 GazeQwen，这是一种参数高效的方法，通过隐藏状态调制使开源 MLLM 具备注视感知能力。其核心是一个紧凑的注视重采样器（约 1-5 M 可训练参数），它将 V-JEPA 2.1 视频特征与源自注视点的点位编码共同编码，并产生加性残差，通过前向钩子（forward hooks）注入到选定的 LLM 解码器层中。一个可选的第二训练阶段向 LLM 添加了低秩自适应器（LoRA），以实现更紧密的集成。在 StreamGaze 基准测试的所有 10 个任务上进行的评估显示，GazeQwen 达到了 63.9% 的准确率，相比使用注视作为视觉提示的相同 Qwen2.5-VL-7B 主干网络提升了 16.1 个百分点，相比 GPT-4o 提升了 10.5 个百分点，在所有测试的开源和私有模型中得分最高。这些结果表明，学习在 LLM 中的何处注入注视信息比扩展模型规模或设计更好的提示词更有效。所有代码和检查点均可在 https://github.com/phamtrongthang123/gazeqwen 获取。

## Abstract
Current multimodal large language models (MLLMs) cannot effectively utilize eye-gaze information for video understanding, even when gaze cues are supplied via visual overlays or text descriptions. We introduce GazeQwen, a parameter efficient approach that equips an open-source MLLM with gaze awareness through hidden-state modulation. At its core is a compact gaze resampler (~1-5 M trainable parameters) that encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into selected LLM decoder layers via forward hooks. An optional second training stage adds low-rank adapters (LoRA) to the LLM for tighter integration. Evaluated on all 10 tasks of the StreamGaze benchmark, GazeQwen reaches 63.9% accuracy, a +16.1 point gain over the same Qwen2.5-VL-7B backbone with gaze as visual prompts and +10.5 points over GPT-4o, the highest score among all open-source and proprietary models tested. These results suggest that learning where to inject gaze within an LLM is more effective than scaling model size or engineering better prompts. All code and checkpoints are available at https://github.com/phamtrongthang123/gazeqwen .

---

## 论文详细总结（自动生成）

以下是对论文《GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理流式视频（如 AR 眼镜、具身智能场景）时，无法有效利用用户的**眼动注视（Gaze）**信息。
*   **研究背景**：注视点能揭示用户的注意力焦点和意图。然而，现有的方法（如将注视坐标转为文本、在图像上覆盖标记或生成显著图）在 StreamGaze 等基准测试中表现不佳，甚至无法超越不带注视信息的基准模型。
*   **整体含义**：本文认为问题不在于模型规模，而在于**输入形式**。论文提出通过“隐藏状态调制”直接将注视信号注入 LLM 内部，而非仅仅作为表面提示词。

### 2. 论文提出的方法论
GazeQwen 采用了一种轻量级的模块化设计，核心思想是**隐藏状态调制（Hidden-state Modulation）**：
*   **输入处理**：
    *   **视频特征**：使用冻结的 **V-JEPA 2.1** 编码器提取时空特征，因为它比 LLM 自带的视觉编码器更能保留细粒度的空间细节。
    *   **注视编码**：将注视点坐标通过 DETR 风格的正弦位置编码（Sinusoidal Positional Encoding）转换为高维向量。
*   **注视条件重采样器（Gaze-Conditioned Resampler）**：
    *   这是一个紧凑的模块（1-5M 参数），利用交叉注意力机制将视频特征与注视向量融合。
    *   **关键细节**：注视编码被添加到 Attention 的 **Key** 中，从而引导模型关注注视点附近的视觉 Token，而不改变 Value 中的语义内容。
*   **钩子注入（Hook-Based Injection）**：
    *   利用 PyTorch 的 **Forward Hooks**，将重采样器生成的加性残差注入到 LLM（如 Qwen2.5-VL）的 4 个等间距解码层中。这种方式无需修改 LLM 源码。
*   **两阶段训练**：
    *   **第一阶段**：仅训练重采样器，LLM 完全冻结，学习产生有用的注视残差。
    *   **第二阶段**：引入 **LoRA** 适配器（约 3.5M 参数）对 LLM 进行微调，使模型内部注意力模式更好地适应注入的信号。

### 3. 实验设计
*   **数据集与基准**：使用 **StreamGaze** 基准测试，包含 285 个第一视角视频和 8,521 个注视条件下的 QA 对。
*   **任务分类**：涵盖 10 类任务，分为“过去”（如物体跟踪）、“现在”（如属性识别）和“主动预测”（如物体出现预警）三大类。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 4 (Sonnet/Opus)。
    *   **开源模型**：Qwen2.5-VL, InternVL3.5, VITA 1.5, MiniCPM-V 等。
    *   **流式专用模型**：ViSpeak, Dispider, Flash-VStream 等。
    *   **注视专用模型**：AssistGaze。

### 4. 资源与算力
*   **算力需求**：论文明确提到该方法是“低成本”的，支持**单 GPU 训练（Single-GPU training）**。
*   **参数量**：可训练参数极少，重采样器约 1-5M，LoRA 约 3.5M，总计约 5-9M 参数。
*   **训练配置**：使用 AdamW 优化器，学习率 $3 \times 10^{-4}$，训练最多 20 个 Epoch。具体的 GPU 型号（如 A100 或 H100）文中未明确标出，但从参数量和单卡描述看，消费级显卡亦可胜任。

### 5. 实验数量与充分性
*   **实验规模**：在 StreamGaze 的全部 10 个任务上进行了完整评估，并提供了与十余种主流模型的对比。
*   **消融实验**：设计了 4 个维度的消融实验，验证了：
    1.  **注视编码**：正弦编码优于热力图。
    2.  **视觉主干**：V-JEPA 2.1 优于 DINOv2。
    3.  **层共享**：独立层重采样器优于共享参数。
    4.  **LLM 适配**：LoRA 进一步提升了性能。
*   **客观性**：采用了严格的视频级数据划分（70/15/15），防止了数据泄露，实验设计较为客观公平。

### 6. 论文的主要结论与发现
*   **性能突破**：GazeQwen 总体准确率达到 **63.9%**，比 GPT-4o 高出 10.5 个百分点，比同底座的 Qwen2.5-VL（使用视觉提示）高出 16.1 个百分点。
*   **关键发现**：
    *   在**物体出现预警（OAA）**和**物体跟踪（OTP）**等空间敏感任务上提升最显著（分别提升 34.1% 和 29.4%）。
    *   **内部调制优于外部提示**：证明了在 LLM 内部学习“如何注入注视信息”比单纯增加模型规模或优化 Prompt 更有效。
    *   **层级差异**：不同深度的 LLM 层需要不同的注视表示（早期层关注空间布局，后期层关注语义）。

### 7. 优点
*   **参数高效**：仅需极少量参数即可实现大幅性能提升，适合端侧部署。
*   **模块化与可插拔**：通过 Hook 注入，不破坏原始模型权重，注视模块可以随时拆卸，实现“注视无关”与“注视感知”模式的无缝切换。
*   **通用性**：该方法论可以推广到其他开源 MLLM 底座上。

### 8. 不足与局限
*   **任务局限性**：在一些不需要精细空间定位的任务（如 OI-Easy 简单物体识别、FAP 未来动作预测）中，注视信息的引入提升微乎其微。
*   **依赖外部编码器**：需要额外的 V-JEPA 视觉主干，增加了推理时的计算链路复杂性。
*   **实时性验证**：虽然针对流式视频设计，但论文主要在 QA 离线基准上测试，在真实的超低延迟实时交互系统中的表现仍需进一步验证。

（完）
