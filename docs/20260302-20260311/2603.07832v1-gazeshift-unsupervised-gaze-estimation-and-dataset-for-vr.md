---
title: "GazeShift: Unsupervised Gaze Estimation and Dataset for VR"
title_zh: GazeShift：面向虚拟现实（VR）的无监督视线估计与数据集
authors: "Gil Shapira, Ishay Goldin, Evgeny Artyomov, Donghoon Kim, Yosi Keller, Niv Zehngut"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07832v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 用于VR交互的无监督视线估计和数据集
tldr: 针对VR环境下离轴相机视线估计数据匮乏及标注困难的问题，本文推出了包含210万张红外图像的大规模数据集VRGaze。同时提出GazeShift无监督框架，通过注意力引导实现视线与外观特征解耦，无需标签即可学习视线表示。该模型轻量高效，在VR设备上推理仅需5ms，在多个数据集上表现优异，为实时VR视线追踪提供了高效方案。
selection_source: fresh_fetch
motivation: 现代VR头显缺乏大规模、准确标注的离轴相机视线估计数据集，且传统标注方法难以保证用户注视目标的准确性。
method: 提出一种名为GazeShift的注意力引导无监督框架，通过视线重定向实现特征解耦，无需多视图或3D几何信息即可学习视线表示。
result: 在VRGaze数据集上实现1.84度误差，在MPIIGaze上以极低计算开销达到7.15度误差，且在VR GPU上推理仅需5毫秒。
conclusion: GazeShift是一个高效、鲁棒且无需大量标签的实时VR视线追踪解决方案，显著降低了计算成本并提升了跨场景适用性。
---

## 摘要
视线估计在现代虚拟现实（VR）系统中起着至关重要的作用。尽管远程摄像头视线估计已取得显著进展，但 VR 视线研究仍受限于数据稀缺——特别是缺乏使用现代头显典型的离轴（off-axis）摄像头配置捕获的大规模、精确标注的数据集。由于无法保证用户注视在预定目标上，视线标注非常困难。为了应对这些挑战，我们推出了 VRGaze——首个面向 VR 的大规模离轴视线估计数据集——包含从 68 名参与者中收集的 210 万张近眼红外图像。我们进一步提出了 GazeShift，这是一个注意力引导的无监督框架，用于在没有标注数据的情况下学习视线表示。与以往依赖多视图或 3D 几何的重定向（redirection）方法不同，GazeShift 专门针对近眼红外图像量身定制，在紧凑的实时模型中实现了有效的视线-外观解耦。GazeShift 嵌入可以通过轻量级少样本校准选择性地适配个体用户，在 VRGaze 上实现了 1.84 度的平均误差。在远程摄像头 MPIIGaze 数据集上，该模型实现了 7.15 度的跨用户（person-agnostic）误差，且参数量和 FLOPs 分别比基准方法少 10 倍和 35 倍。原生部署在 VR 头显 GPU 上时，推理仅需 5 毫秒。结合其对光照变化的鲁棒性，这些结果凸显了 GazeShift 作为一种标签高效、实时的 VR 视线追踪解决方案的优势。项目代码和 VRGaze 数据集已发布在 https://github.com/gazeshift3/gazeshift。

## Abstract
Gaze estimation is instrumental in modern virtual reality (VR) systems. Despite significant progress in remote-camera gaze estimation, VR gaze research remains constrained by data scarcity - particularly the lack of large-scale, accurately labeled datasets captured with the off-axis camera configurations typical of modern headsets. Gaze annotation is difficult since fixation on intended targets cannot be guaranteed. To address these challenges, we introduce VRGaze - the first large-scale off-axis gaze estimation dataset for VR - comprising 2.1 million near-eye infrared images collected from 68 participants. We further propose GazeShift, an attention-guided unsupervised framework for learning gaze representations without labeled data. Unlike prior redirection-based methods that rely on multi-view or 3D geometry, GazeShift is tailored to near-eye infrared imagery, achieving effective gaze-appearance disentanglement in a compact, real-time model. GazeShift embeddings can be optionally adapted to individual users via lightweight few-shot calibration, achieving a 1.84-degree mean error on VRGaze. On the remote-camera MPIIGaze dataset, the model achieves a 7.15-degree person-agnostic error, doing so with 10x fewer parameters and 35x fewer FLOPs than baseline methods. Deployed natively on a VR headset GPU, inference takes only 5 ms. Combined with demonstrated robustness to illumination changes, these results highlight GazeShift as a label-efficient, real-time solution for VR gaze tracking. Project code and the VRGaze dataset are released at https://github.com/gazeshift3/gazeshift.

---

## 论文详细总结（自动生成）

以下是对论文《GazeShift: Unsupervised Gaze Estimation and Dataset for VR》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：现代 VR 头显（如 Apple Vision Pro、Meta Quest Pro）普遍采用**离轴（Off-axis）**红外相机配置以减少视觉遮挡，但学术界缺乏针对此类几何结构的大规模标注数据集。此外，视线标注（Gaze Annotation）极度依赖用户配合，存在注视点不确定、标注成本高昂的问题。
*   **整体含义**：本文旨在通过提供首个大规模离轴 VR 视线数据集（VRGaze），并提出一种无需标签的无监督学习框架（GazeShift），解决 VR 环境下视线估计的数据稀缺与标注难题，实现高效、实时的端侧部署。

### 2. 方法论：核心思想与技术细节
GazeShift 的核心思想是将“视线重定向”作为代理任务进行无监督学习：
*   **双编码器架构**：
    *   **视线编码器（Gaze Encoder）**：从目标图像中提取抽象的视线嵌入（Embedding）。
    *   **外观编码器（Appearance Encoder）**：从源图像中提取空间外观特征。这种分离设计防止了外观信息泄露到视线表示中，实现了有效的特征解耦。
*   **注意力引导的重定向**：
    *   利用**自注意力（Self-Attention）**捕捉源图像的空间结构。
    *   通过**交叉注意力（Cross-Attention）**将目标视线嵌入作为 Query，对源外观特征进行全局调制，从而生成重定向后的图像特征。
*   **视线感知重构损失（Gaze-Focused Loss）**：
    *   公式逻辑：$L_{focus} = \sum w^\gamma (x - \hat{x})^2$。
    *   利用模型内部生成的注意力图（Attention Map）作为权重，自动聚焦于虹膜、瞳孔等视线相关区域，抑制背景噪声对学习的干扰。
*   **校准机制**：在无监督预训练后，通过极少量的标注样本（Few-shot）进行线性回归校准，以消除个体差异（如 Kappa 角）。

### 3. 实验设计
*   **数据集**：
    *   **VRGaze（本文贡献）**：210 万张图像，68 名受试者，涵盖不同年龄、性别和族裔，采用离轴红外相机拍摄。
    *   **对比数据集**：OpenEDS2020（轴向 VR 数据）、MPIIGaze（远程 RGB 数据）、Columbia Gaze（受控环境数据）。
*   **Benchmark 与对比方法**：
    *   **无监督基准**：Cross-Encoder（目前最强的眼部无监督模型）、VAE、SimCLR、BYOL 等。
    *   **监督基准**：基于外观的 Siamese 网络、基于特征（瞳孔/闪烁点）的方法。

### 4. 资源与算力
*   **训练算力**：所有实验均在单张 **NVIDIA RTX A5000 GPU** 上完成。
*   **训练配置**：使用 AdamW 优化器，学习率 0.0001，Batch Size 为 30，训练 20 个 Epoch。
*   **端侧部署**：在 **Samsung Exynos 2200** 芯片（含 Xclipse 920 GPU）上进行推理测试。
*   **推理耗时**：双眼推理总耗时仅为 **5 毫秒**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 VRGaze 上进行了不同校准样本数（K=17 到 60）的性能测试。
    *   进行了详尽的**消融实验**（验证双编码器、注意力机制、加权损失的各自贡献）。
    *   进行了**跨数据集泛化实验**（证明了轴向数据训练的模型无法直接用于离轴场景）。
    *   进行了**解耦分析**（通过扰动实验验证视线与外观嵌入的独立性）。
*   **充分性评价**：实验设计非常充分且客观。不仅涵盖了 VR 场景，还扩展到了远程相机场景（MPIIGaze），证明了方法的通用性。通过 10 次随机实验取平均值，保证了结果的公平性。

### 6. 主要结论与发现
*   **高精度**：在 VRGaze 上，GazeShift 达到了 **1.84°** 的平均误差，接近监督学习水平（1.54°）。
*   **极轻量**：视线编码器仅有 **34.2万（0.34M）** 参数，计算量仅 55 MFLOPs，比主流基准（如 ResNet-18）参数量少 10 倍，计算量少 35 倍。
*   **离轴必要性**：实验证明，在轴向数据（OpenEDS）上训练的模型在离轴数据上误差高达 5.2°，证明了 VRGaze 数据集的独特价值。
*   **鲁棒性**：模型对光照变化和瞳孔缩放具有很强的鲁棒性。

### 7. 优点（亮点）
*   **完全无监督**：摆脱了对大规模精确标注的依赖，通过重定向任务自发学习视线特征。
*   **端侧友好**：极低的计算开销和极快的推理速度，非常适合集成到移动端 VR 头显中。
*   **自适应聚焦**：创新的视线感知损失函数，无需外部掩码（Mask）即可让模型“知道”该看哪里。
*   **数据集贡献**：填补了开源领域大规模离轴 VR 视线数据的空白。

### 8. 不足与局限
*   **假设限制**：模型假设帧间主要变化源于视线移动。在光照剧烈变化或存在频繁眨眼、遮挡的极端环境下（如 AR 户外场景），该假设可能受到挑战。
*   **校准依赖**：虽然是无监督学习，但为了达到高精度，仍需要少量的用户校准步骤（尽管这是目前 VR 行业的通用做法）。
*   **应用范围**：目前主要针对单眼/双眼图像，尚未探讨全脸信息在 VR 场景下的辅助作用。

（完）
