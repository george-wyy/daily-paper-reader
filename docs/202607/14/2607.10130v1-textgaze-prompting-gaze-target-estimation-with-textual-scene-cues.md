---
title: "TextGaze: Prompting Gaze Target Estimation with Textual Scene Cues"
title_zh: TextGaze：利用文本场景线索引导注视目标估计
authors: "Junhui She, Fei Wang, Kun Li, Yiqi Nie, Yuxin Liu, Zhangling Duan, Xun Yang"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10130v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 用文本线索进行注视目标估计以理解人类目标
tldr: 注视目标估计任务中，现有多分支方法依赖额外标注，简化设计则忽略真实意图。TextGaze利用大视觉语言模型（LVLM）获取与注视对齐的文本线索，通过Transformer融合模块联合预测注视热图和视野状态。在四个数据集上取得竞争性能，并展现出无需微调的跨数据集泛化能力。该方法平衡了标注负担与意图对齐，展示了LVLM作为辅助指导的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10130v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 809, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1824, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1838, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 903, \"height\": 241, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 892, \"height\": 626, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10130v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1834, \"height\": 856, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10130v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10130v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 896, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10130v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 894, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10130v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 898, \"height\": 232, \"label\": \"Table\"}]"
motivation: 现有方法多分支需额外标注，简化设计忽视视线意图，导致高标注负担或预测与真实目标错位。
method: 提出TextGaze，使用冻结视觉编码器和LVLM获取文本线索，设计Transformer融合模块带分层文本监督，轻量解码头联合预测热图及状态。
result: 在四个主流数据集上取得竞争性能，关键指标表现好，无需额外微调即可跨数据集泛化。
conclusion: TextGaze提供简化替代方案，展示了LVLM作为注视估计中可访问辅助指导的潜力。
---

## 摘要
注视目标估计旨在推断一个人在场景中的注视位置。在主流的逻辑设计中，多分支方法需要额外的监督和标注，而简化设计则优先考虑低层级的视觉显著性而非真实的注视意图。前者导致标注负担重并阻碍领域迁移，后者则导致预测的注意力与实际注视目标之间的错位。为解决这一问题，我们提出TextGaze，一种统一的跨模态架构，利用大型视觉-语言模型（LVLM）作为可扩展的语义指导来平衡两种设计范式。该模型从冻结的编码器中提取视觉特征，并利用LVLM获取对齐注视的文本线索。我们设计了一个基于Transformer的融合模块，带有层级文本监督以保留任务语义。轻量级的解码头可联合预测注视热图和帧内/帧外状态。我们在四个主流数据集上评估了该方法，结果显示在关键指标上具有竞争力，并且在无需额外微调的情况下具有鲁棒的跨数据集泛化能力。总体而言，我们为传统设计提供了一种简化的替代方案，并凸显了LVLM作为注视估计可访问辅助指导的潜力。

## Abstract
Gaze target estimation aims to infer the position of a person's gaze within a scene. Within mainstream design logic, multi-branch methods require extra supervision and annotations, while streamlined designs prioritize low-level visual saliency over true gaze intent. The former leads to a high annotation burden and hinders domain transfer, whereas the latter causes misalignment between predicted attention and actual gaze targets. To address this issue, we propose TextGaze, a unified cross-modal architecture that leverages a Large Vision-Language Model (LVLM) as scalable semantic guidance to balance the two design paradigms. The model extracts visual features from a frozen encoder and utilizes an LVLM to obtain gaze-aligned textual cues. We design a transformer-based fusion module with hierarchical text supervision to preserve task semantics. Lightweight decoding heads enable the joint prediction of gaze heatmaps and in-/out-of-frame status. We evaluate our method on four mainstream datasets, and the results show competitive performance across key metrics with robust cross-dataset generalisation without extra fine-tuning. Overall, we provide a streamlined alternative to traditional designs and highlight the potential of LVLMs as accessible auxiliary guidance for gaze estimation.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，以下是对给定论文《TextGaze: Prompting Gaze Target Estimation with Textual Scene Cues》的结构化、深入且客观的总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：现有的注视目标估计方法存在两种设计范式的矛盾：
    -   **多分支方法**：通过引入深度、姿态等辅助线索提升性能，但这需要大量的额外标注，增加了数据收集成本和架构复杂度，且阻碍了跨领域泛化。
    -   **简化设计（如提示调优方法）**：架构简单、收敛快，但过度依赖低层级的视觉显著性（如空间邻近性），忽视了高层语义的注视意图，导致预测的注意力与真实注视目标错位（即语义聚焦不足）。
-   **研究动机**：需要一种既能利用语义先验知识，又能避免多分支复杂性的新范式。论文提出利用大型视觉-语言模型（LVLM）作为可访问的、可扩展的语义指导来源。
-   **整体含义**：这项工作旨在将注视估计从纯粹的几何回归问题，转变为一种通过语义层推理的意图识别任务，从而在模型简洁性和性能之间取得更好的平衡。

### 2. 方法论：核心思想、关键技术细节

-   **核心思想**：提出一个名为 **TextGaze** 的统一跨模态框架。该框架采用双流设计，核心是将从LVLM生成的文本场景线索（语义意图）与冻结的视觉特征（像素级信息）进行深度融合，引导注视预测。
-   **关键技术细节**：
    1.  **LVLM文本线索模块**：
        -   使用一个**冻结的LVLM**（论文中采用Qwen2.5-VL-7B），通过设计好的提示词，生成关于目标人物的**外观描述、动作状态和粗略的注视方向**的文本。
        -   生成的文本通过一个**冻结的文本编码器**（BERT）转化为特征嵌入。
    2.  **特征令牌序列处理模块**：
        -   **视觉流**：使用**冻结的DINOv2**提取视觉特征图，并加入**可学习的头部令牌嵌入**以增强对头部信息的敏感性，最后加上2D正弦位置编码。
        -   **文本流**：为文本特征添加**可学习的位置编码**。
        -   **融合序列**：将处理后的视觉、文本令牌序列（以及一个可选的帧内/外分类令牌）拼接起来，形成双模态融合序列。
    3.  **跨模态融合与预测模块**：
        -   **融合模块**：使用一个3层的堆叠融合Transformer。它执行统一的多头自注意力，允许视觉和文本令牌之间进行全局、对称的交互。
        -   **分层文本监督**：为解决深层融合中文本语义漂移的问题，提出在每一层Transformer后，对文本令牌部分的输出进行投影，并与初始的BERT文本特征计算**均方误差损失**，强制中间层保留任务相关的文本语义。
        -   **预测头**：
            -   从融合后的特征中提取视觉令牌部分，重塑并通过一个两层的卷积解码器，输出注视概率热图（受像素级二元交叉熵损失监督）。
            -   （可选）将分类令牌输入一个两层的MLP，预测注视目标是否在画面内。
-   **总损失函数**：
    -   `L_all = L_heat (热图损失) + λ1 * L_in/out (帧内/外损失) + λ2 * L_text (文本一致性损失)`

### 3. 实验设计

-   **数据集与场景**：
    -   **主要基准**：**GazeFollow**（静态图像）和 **VideoAttentionTarget**（视频帧），用于标准的注视目标预测评估。
    -   **跨数据集泛化评估**：**ChildPlay**（儿童注视行为）和 **GOO-Real**（零售场景），在不进行任何微调的情况下直接测试模型的泛化能力。
-   **基准测试（Benchmark）**：
    -   **GazeFollow**：AUC（面积），Avg L2（平均欧式距离），Min L2（最小欧式距离）。
    -   **VideoAttentionTarget**：AUC，L2，AP_in/out（帧内/外平均精度）。
    -   **ChildPlay/GOO-Real**：AUC，L2，AP。
-   **对比方法**：
    -   与大量的现有方法对比，包括传统的多分支方法（如Chong et al., Bao et al.）、以及最新的提示调优方法（如Ryan et al. / GazeLLE）。对比时确保了与最先进方法（GazeLLE）使用相同的视觉编码器（DINOv2 ViT-B/L）和实验设置。

### 4. 资源与算力

-   论文中**未明确提及**具体的GPU型号、数量以及训练时长。仅提及模型使用AdamW优化器，且在NVIDIA A100 GPU上进行训练。这表明了对算力的基本要求，但缺乏具体的量化数据，不利于进行公平的效率对比。

### 5. 实验数量与充分性

-   **实验数量**：较为充分。
    -   **主实验**：在两个主要基准（GazeFollow, VideoAttentionTarget）上对比了数十种方法。
    -   **跨数据集泛化实验**：在两个零样本场景（ChildPlay, GOO-Real）上进行了评估。
    -   **消融实验**：
        1.  **融合策略消融**：对比了三种融合方式（加法融合、交叉注意力融合、Transformer融合）。
        2.  **关键组件消融**：逐步验证了文本监督、视觉残差、混合位置编码三个核心组件的有效性。
    -   **定性分析**：提供了多张热力图可视化结果，直观展示了模型在多目标、遮挡、复杂场景下的表现。
-   **实验充分性与公平性**：
    -   **优点**：实验设计较为全面，涵盖了标准评估、跨领域泛化和核心组件分析。比较公平，特别是与最先进的方法(GazeLLE)在相同骨干网络下进行了对比。
    -   **局限**：消融实验仅在GazeFollow数据集上进行，未在其他数据集（如VideoAttentionTarget）上验证组件的一般性。跨数据集泛化的分析（AP指标解释）略显简略。

### 6. 主要结论与发现

1.  **文本线索有效**：引入LVLM生成的文本场景线索，能够有效缓解简化设计中的“语义聚焦不足”问题，为注视估计提供了高层语义先验。
2.  **双流融合成功**：所提出的双流跨模态融合框架，有效地将文本嵌入与精细视觉特征结合，提升了空间推理和场景适应性。
3.  **性能领先**：在GazeFollow、VideoAttentionTarget和GOO-Real数据集上取得了具有竞争力的性能，特别是在跨数据集泛化方面表现突出。
4.  **关键组件有效**：分层文本监督、视觉残差连接和混合位置编码这三个设计对模型性能均有贡献，且组合使用时效果最佳。
5.  **LVLM作为辅助工具的价值**：论文成功地将LVLM作为可访问的辅助指导工具，为简化注视估计模型设计提供了一条新路径，而无需引入复杂的多分支结构或昂贵的额外标注。

### 7. 优点

1.  **问题定义创新**：明确指出了现有方法“语义聚焦不足”的根本问题，并从认知科学的视角提供了理论支撑。
2.  **方法设计精巧**：
    -   将LVLM作为辅助工具而非任务求解器，避免了LVLM的幻觉问题。
    -   提出的分层文本监督损失，是一个巧妙的设计，有效解决了多模态融合中语义退化的常见问题。
    -   双流架构简洁清晰，在引入语义信息的同时保持了架构的轻量级。
3.  **实验全面且公平**：在主数据集、泛化能力和消融分析上做了大量工作，并特别选择了与最先进方法在相同骨干下对比，结论更可靠。
4.  **泛化能力强**：在零样本跨数据集测试中，特别是在帧内/外分类任务上表现优异，证明了其捕获高层意图的能力优于依赖低层视觉特征的模型。
5.  **工程友好**：提示词设计考虑了帧外场景，避免LVLM产生错误的物体级推断。代码开源，有利于社区复现和后续研究。

### 8. 不足与局限

1.  **算力报告缺失**：未提供完整的资源消耗报告，使得在效率方面难以与其他方法进行公平比较。
2.  **消融实验的局限性**：消融实验仅在一个数据集上进行（GazeFollow），其结论（如组件重要性排序）在其他数据集（如VideoAttentionTarget）上是否成立，有待进一步验证。
3.  **跨数据集分析深度不足**：虽然在ChildPlay和GOO-Real上展示了结果，但对性能差异背后的原因（例如，为什么在ChildPlay的AP指标上领先但在AUC上略逊）缺乏深入分析。
4.  **对LVLM的依赖**：模型的性能一定程度上依赖于所使用的LVLM（Qwen2.5-VL-7B）的质量。不同LVLM生成的文本质量差异，可能对最终效果产生影响（论文承认框架是模型无关的，但未进行LVLM模型的横向对比）。
5.  **实际部署的鲁棒性**：对LVLM和BERT的生成和编码过程存在一次前向推理开销，在实时性要求极高的应用中（如人机交互）可能存在延迟。论文未讨论此问题。
6.  **新兴领域验证**：实验场景覆盖了日常活动、零售、儿童行为，但在如XR、教育、医疗等专业且对注视意图要求极高的细分领域，其泛化能力和实用性有待进一步验证。

（完）
