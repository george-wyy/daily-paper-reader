---
title: "Alfa: Attentive Low-Rank Filter Adaptation for Structure-Aware Cross-Domain Personalized Gaze Estimation"
title_zh: Alfa：用于结构感知的跨域个性化视线估计的注意力低秩滤波器自适应
authors: "He-Yen Hsieh, Wei-Te Mark Ting, H. T. Kung"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08445v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 个性化视线估计与用户特定差异的自适应
tldr: 针对视线估计中用户个体差异导致的性能下降，本文提出Alfa方法。该方法通过注意力机制和低秩分解（SVD）重新加权预训练模型中的语义模式，而非学习新权重。在仅需少量无标签样本的情况下，Alfa在四个跨数据集基准测试中均取得最低误差，优于现有TTP和LoRA变体，且具有跨领域适用性。
selection_source: fresh_fetch
motivation: 预训练视线模型难以应对不同用户的眼部及面部结构差异，且现有参数高效微调方法未能充分利用预训练滤波器中已有的结构信息。
method: 利用奇异值分解提取预训练滤波器的主要空间分量，并通过注意力机制对这些分量进行重加权，以选择性地放大与目标用户相关的特征。
result: 在四个跨数据集视线估计基准测试中，Alfa均取得了最低的平均误差，性能显著优于现有的测试时个性化方法和LoRA变体。
conclusion: Alfa通过重加权现有特征实现了高效的跨域个性化适配，并展示了其在视觉任务及扩散语言模型等其他领域的广泛应用潜力。
---

## 摘要
预训练的视线模型学习识别用户间通用的有用模式，但细微的用户特定差异（如眼睑形状或面部结构）可能会降低模型性能。测试时个性化（TTP）仅使用少量无标签样本，使预训练模型适应这些用户特定的领域偏移。高效微调对于执行这种领域自适应至关重要：数据和计算资源可能受限，尤其是在设备端定制方面。虽然流行的参数高效微调（PEFT）方法通过仅更新一小部分权重来解决自适应成本问题，但它们可能没有充分利用预训练滤波器中编码的结构。为了更有效地利用预训练期间学习到的现有结构，我们将个性化重新定义为对现有特征进行重新加权的过程，而不是学习全新的特征。我们提出了注意力低秩滤波器自适应（Alfa），通过对预训练滤波器中的语义模式进行重新加权来使视线模型自适应。通过 Alfa，奇异值分解（SVD）提取出捕获跨用户眼睛和面部特征的主要空间分量。通过注意力机制，我们仅需少量无标签样本即可调整并重新加权预训练结构，选择性地放大与目标用户相关的结构。Alfa 在四个跨数据集视线基准测试中实现了最低的平均视线误差，优于现有的 TTP 方法和基于低秩自适应（LoRA）的变体。我们还展示了 Alfa 的注意力低秩方法可以应用于视觉之外的应用，例如基于扩散的语言模型。

## Abstract
Pre-trained gaze models learn to identify useful patterns commonly found across users, but subtle user-specific variations (i.e., eyelid shape or facial structure) can degrade model performance. Test-time personalization (TTP) adapts pre-trained models to these user-specific domain shifts using only a few unlabeled samples. Efficient fine-tuning is critical in performing this domain adaptation: data and computation resources can be limited-especially for on-device customization. While popular parameter-efficient fine-tuning (PEFT) methods address adaptation costs by updating only a small set of weights, they may not be taking full advantage of structures encoded in pre-trained filters. To more effectively leverage existing structures learned during pre-training, we reframe personalization as a process to reweight existing features rather than learning entirely new ones.   We present Attentive Low-Rank Filter Adaptation (Alfa) to adapt gaze models by reweighting semantic patterns in pre-trained filters. With Alfa, singular value decomposition (SVD) extracts dominant spatial components that capture eye and facial characteristics across users. Via an attention mechanism, we need only a few unlabeled samples to adjust and reweight pre-trained structures, selectively amplifying those relevant to a target user. Alfa achieves the lowest average gaze errors across four cross-dataset gaze benchmarks, outperforming existing TTP methods and low-rank adaptation (LoRA)-based variants. We also show that Alfa's attentive low-rank methods can be applied to applications beyond vision, such as diffusion-based language models.

---

## 论文详细总结（自动生成）

### 论文总结：Alfa (Attentive Low-Rank Filter Adaptation)

#### 1. 核心问题与整体含义
论文关注的是**视线估计（Gaze Estimation）中的跨域个性化问题**。虽然预训练模型能识别通用的视线模式，但不同用户在眼睑形状、面部结构及相机配置上的细微差异（领域偏移）会导致模型性能显著下降。
*   **研究动机**：现有的测试时个性化（TTP）方法和参数高效微调（PEFT，如 LoRA）通常将权重视为无结构的张量，忽略了预训练滤波器中已有的空间结构信息。
*   **核心目标**：在仅有极少量（如5张）无标签样本的情况下，如何通过高效利用预训练模型已掌握的语义结构，实现低成本、高精度的个性化适配。

#### 2. 方法论
Alfa 的核心思想是将个性化过程从“学习新特征”转变为“**对现有预训练特征进行重加权**”。
*   **结构化分解（SVD）**：首先对预训练权重矩阵 $W$ 进行奇异值分解（SVD），提取出前 $d$ 个主要的奇异向量，形成“语义基字典”（Semantic Basis Dictionary）。这些基向量代表了模型在预训练阶段学到的、跨用户通用的空间模式（如虹膜位置、眼周几何结构）。
*   **注意力重加权机制**：引入多头注意力机制。将语义基字典作为 Key 和 Value，通过可学习的低秩矩阵生成 Query。通过计算注意力分数，模型能够识别并放大与当前目标用户最相关的空间模式。
*   **低秩更新与合并**：通过多头模块生成个性化更新项 $\Delta W$。由于 Alfa 保持了预训练权重的左奇异向量 $U_d$ 不变，更新后的权重可以直接合并回原模型，**在推理阶段不增加任何额外的计算开销**。
*   **对称性损失**：在微调阶段，利用人脸的左右对称性，通过无监督的对称损失函数对模型进行约束。

#### 3. 实验设计
*   **数据集**：
    *   **源域（预训练）**：ETH-XGaze (DE)、Gaze360 (DG)。
    *   **目标域（测试/适配）**：MPIIGaze (DM)、EyeDiap (DD)。
*   **Benchmark 场景**：四个跨数据集任务（DE→DM, DE→DD, DG→DM, DG→DD），采用 5-shot（每个用户仅5张无标签图像）设置。
*   **对比方法**：
    *   **TTP 方法**：PnP-GA, RUDA, TPGaze。
    *   **LoRA 变体**：LoRA, MiLoRA, DoRA, MoSLoRA, MELoRA, Spectral Adapter A, FLoRA。
*   **扩展实验**：在扩散语言模型（LLaDA-8B-Instruct）上进行零样本推理任务（GSM8K, MATH500 等）的适配对比。

#### 4. 资源与算力
*   **硬件**：
    *   视线估计实验：1 台 NVIDIA RTX 4090 GPU。
    *   LLM 实验：2 台 NVIDIA A40 GPU。
*   **训练细节**：预训练阶段使用 Adam 优化器，batch size 为 120，共 50+25 个 epoch。个性化微调阶段针对每个用户进行快速迭代。

#### 5. 实验数量与充分性
论文进行了较为充分且多维度的实验：
*   **跨域对比**：在 4 个主流视线估计基准上进行了详尽对比。
*   **消融实验**：研究了注意力头数（1 到 32）、LoRA 秩（4 到 32）、SVD 秩（32 到 128）以及样本数量（1 到 10）对性能的影响。
*   **可视化分析**：通过热力图展示了 SVD 分量捕获的语义区域，以及 Alfa 更新项与 LoRA 更新项在空间一致性上的差异。
*   **跨领域验证**：将方法迁移至 LLM 推理任务，证明了其通用性。
*   **客观性**：所有 LoRA 变体均在相同的预训练权重和微调方案下进行测试，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能领先**：Alfa 在所有四个视线估计基准上均取得了最低的平均角度误差（约 5.72°），显著优于 TPGaze 等现有 SOTA 方法。
*   **参数高效**：Alfa 在保持高性能的同时，可调参数量远少于传统的 TTP 方法，且模型体积更小。
*   **结构感知**：可视化证明 Alfa 能够精准定位到眼角、眼睑等关键解剖区域，而 LoRA 的更新分布较为分散且缺乏物理意义。
*   **通用性**：在 LLM 任务中，Alfa 以更少的参数实现了与 LoRA/DoRA 相当甚至更优的推理准确率。

#### 7. 优点
*   **推理零成本**：通过 SVD 形式的权重合并，适配后的模型在部署时不需要额外的计算资源。
*   **语义解释性**：将权重更新与面部几何结构联系起来，增强了模型在个性化过程中的可解释性。
*   **数据效率极高**：仅需 5 张无标签图像即可完成有效的领域迁移。

#### 8. 不足与局限
*   **超参数敏感性**：SVD 秩 $d$ 的选择对性能有一定影响，需要根据具体任务进行预先调优。
*   **模型架构局限**：视线估计实验主要基于 ResNet-18，尚未在更现代的 Vision Transformer (ViT) 等架构上进行大规模验证。
*   **计算初始化**：虽然推理无开销，但在微调前需要对预训练权重进行一次性的 SVD 分解，对于超大规模模型可能会带来一定的初始计算负担。

（完）
