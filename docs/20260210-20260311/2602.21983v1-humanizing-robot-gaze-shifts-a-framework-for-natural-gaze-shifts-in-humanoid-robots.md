---
title: "Humanizing Robot Gaze Shifts: A Framework for Natural Gaze Shifts in Humanoid Robots"
title_zh: 拟人化机器人视线转移：一种人形机器人自然视线转移框架
authors: "Jingchao Wei, Jingkai Qin, Yuxiao Cao, Jingcheng Huang, Xiangrui Zeng, Min Li, Zhouping Yin"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21983v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 基于视觉语言模型的多模态交互线索视线推理
tldr: 本研究针对人形机器人在社交互动中视线转移不自然的问题，提出了Robot Gaze-Shift (RGS) 框架。该框架结合了基于视觉语言模型（VLM）的认知推理机制以确定上下文相关的视线目标，以及基于条件VQ-VAE的眼头协调运动生成模型。RGS实现了从多模态线索推理到仿生运动生成的完整流程，显著提升了机器人视线转移的真实感与多样性，为实现更自然的人机交互奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决人形机器人在非受限社交场景中，难以将认知注意力机制与仿生运动生成相结合以实现自然视线转移的挑战。
method: 提出RGS框架，利用VLM推理上下文相关的视线目标，并采用条件VQ-VAE模型生成多样化的眼头协调运动。
result: 实验验证了该框架能够有效复制人类的目标选择逻辑，并生成高度逼真且多样化的视线转移轨迹。
conclusion: RGS框架通过整合认知推理与仿生运动，成功提升了人形机器人视线行为的拟人化水平和社交交互的自然度。
---

## 摘要
利用听觉和视觉反馈进行注意力重定向对于社交互动中的自然视线转移至关重要。然而，使人形机器人在非受限人机交互（HRI）中执行自然且符合语境的视线转移仍具挑战性，因为这需要将认知注意力机制与仿生运动生成相结合。在本研究中，我们提出了机器人视线转移（RGS）框架，将这两个组件集成到统一的流水线中。首先，RGS 采用基于视觉语言模型（VLM）的视线推理流水线，从多模态交互线索中推断符合语境的视线目标，确保与人类视线定向规律的一致性。其次，RGS 引入了条件向量量化变分自编码器（VQ-VAE）模型，用于眼头协调的视线转移运动生成，从而产生多样化且类人的视线转移行为。实验验证表明，RGS 能有效复制类人的目标选择，并生成逼真、多样的视线转移运动。

## Abstract
Leveraging auditory and visual feedback for attention reorientation is essential for natural gaze shifts in social interaction. However, enabling humanoid robots to perform natural and context-appropriate gaze shifts in unconstrained human--robot interaction (HRI) remains challenging, as it requires the coupling of cognitive attention mechanisms and biomimetic motion generation. In this work, we propose the Robot Gaze-Shift (RGS) framework, which integrates these two components into a unified pipeline. First, RGS employs a vision--language model (VLM)-based gaze reasoning pipeline to infer context-appropriate gaze targets from multimodal interaction cues, ensuring consistency with human gaze-orienting regularities. Second, RGS introduces a conditional Vector Quantized-Variational Autoencoder (VQ-VAE) model for eye--head coordinated gaze-shift motion generation, producing diverse and human-like gaze-shift behaviors. Experiments validate that RGS effectively replicates human-like target selection and generates realistic, diverse gaze-shift motions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Humanizing Robot Gaze Shifts: A Framework for Natural Gaze Shifts in Humanoid Robots》的结构化深度总结：

### 1. 论文的核心问题与整体含义
该研究旨在解决人形机器人在社交互动中**视线转移（Gaze Shift）不自然**的问题。在真实的人机交互（HRI）场景中，自然的视线行为不仅需要机器人知道“该看哪里”（视线推理），还需要知道“如何协调地看”（运动生成）。
*   **研究动机**：现有的方法要么依赖手工规则，缺乏对复杂环境的适应性；要么生成的运动过于机械，无法模拟人类眼头协调的多样性和随机性。
*   **核心目标**：建立一个统一的 **Robot Gaze-Shift (RGS)** 框架，将基于认知的视线推理与基于仿生的运动生成相结合。

### 2. 论文提出的方法论
RGS 框架由三个耦合阶段组成：
*   **交互场景感知**：利用 RGB-D 相机和麦克风获取多模态数据（图像流、深度流、音频流）。
*   **基于 VLM 的视线推理**：
    *   **核心思想**：利用视觉语言模型（VLM）强大的语义理解能力，结合心理学中的视线规律进行决策。
    *   **关键技术**：采用 **Set-of-Mark (SoM)** 提示技术，为场景中的每个目标分配唯一标识符；引入**交互记忆缓冲区**（存储过去 15 秒的上下文），确保视线行为的连贯性。
    *   **流程**：感知模型提取语义 -> 视觉标记与融合 -> 提示词合成 -> VLM 推理目标 ID -> 3D 空间定位。
*   **基于条件 VQ-VAE 的运动生成**：
    *   **核心思想**：将眼头协调模式量化为离散的代码库（Codebook），并根据当前姿态和目标位置进行条件生成。
    *   **两阶段训练**：
        1.  **重构阶段**：训练条件 VQ-VAE，使用旋转感知的**测地线距离（Geodesic Distance）**作为损失函数，学习将人类眼头运动压缩为离散代码。
        2.  **先验学习阶段**：训练一个条件先验网络，根据当前状态预测代码分布。
    *   **多样性机制**：推理时通过对预测概率分布进行随机采样（Stochastic Sampling），使机器人面对同一目标时能产生多种合理的动作组合。

### 3. 实验设计
*   **数据集与场景**：
    *   **视线推理评估**：设计了 80 个基于场景的视频刺激，涵盖四种人类视线规律：(H1) 指示性定向、(H2) 社交定向反射、(H3) 轮流发言、(H4) 响应性共同注意。
    *   **运动生成评估**：自建了一个包含 805 个样本的人类视线转移数据集（按 8:2 划分训练/验证集）。
*   **Benchmark 与对比**：
    *   **推理基准**：以人类视线定向规律的成功率作为衡量标准。
    *   **运动基准**：使用平均测地线距离（MGD）衡量预测动作与人类真实动作的偏差。
    *   **对比方法**：主要验证了 VQ-VAE 框架相对于确定性回归模型的优势（通过可视化多样性结果）。

### 4. 资源与算力
*   **硬件平台**：使用桌面人形机器人头部（2-DoF 眼部，3-DoF 头部）和外部工业 PC。
*   **模型配置**：VLM 采用了 **Doubao-1.5-vision-pro**。
*   **训练细节**：VQ-VAE 训练 200 个 epoch，条件先验训练 100 个 epoch；使用 Adam 优化器，学习率为 $1 \times 10^{-3}$。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和具体训练时长，但提到推理周期为 1.5 秒，以平衡感知延迟与系统响应。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种心理学规律进行了 80 组视频测试（筛选后保留 76 组有效测试）。
    *   运动生成模型在 805 个样本的数据集上进行了训练和验证。
    *   在物理机器人平台上进行了 4 种交互场景的多样性推理测试。
*   **充分性评价**：实验设计较为全面，涵盖了从高层认知推理到低层运动执行的闭环验证。通过 MGD 曲线展示了模型的收敛性，通过物理机器人实验展示了实际应用效果。但在对比实验方面，缺乏与其他 SOTA（如基于强化学习或传统优化方法）的直接量化对比。

### 6. 论文的主要结论与发现
*   **推理有效性**：VLM 能够准确识别社交线索，在 H1-H3 规律上的成功率达到 85% 以上，但在处理复杂的共同注意（H4）时因遮挡等因素表现略低（70.6%）。
*   **运动自然度**：条件 VQ-VAE 成功捕捉了人类眼头协调的离散模式，重构误差（MGD）控制在较小范围内（眼部 3.4°，头部 6.5°）。
*   **行为多样性**：随机采样机制有效利用了机器人的运动学冗余，避免了机械式的重复动作，显著提升了拟人化程度。

### 7. 优点（亮点）
*   **端到端整合**：首次将大语言模型（VLM）的认知推理与 VQ-VAE 的生成式运动控制整合进统一的视线框架。
*   **鲁棒的推理机制**：引入 SoM 提示和记忆缓冲区，解决了 VLM 在动态社交场景中定位不准和时序不一致的问题。
*   **解决运动冗余**：利用 VQ-VAE 的离散潜在空间很好地建模了人类视线转移的随机性和多样性。

### 8. 不足与局限
*   **推理频率限制**：1.5 秒的推理周期对于极高动态的交互可能显得迟钝（虽然文中解释这符合人类“扫视-固定”模式）。
*   **模块解耦评估**：视线推理和运动生成目前主要是分开评估的，缺乏大规模的端到端用户主观评价实验（User Study）。
*   **感知依赖**：推理精度高度依赖于底层感知模型（如目标检测、姿态估计），在遮挡严重的复杂场景下性能会下降。
*   **主动性不足**：目前的框架主要属于“反应式”视线，缺乏基于机器人自身意图的主动视线规划。

（完）
