---
title: "GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding"
title_zh: GazeQwen：面向流式视频理解的轻量级注视条件 LLM 调制
authors: "Trong Thang Pham, Hien Nguyen, Ngan Le"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25841v1"
tags: ["query:gaze-intent"]
score: 10.0
evidence: 基于注视点位置编码的视频理解大模型调制技术
tldr: "GazeQwen 是一种轻量级的视线调节多模态大模型，旨在解决现有模型难以有效利用视线信息进行视频理解的问题。该方法通过紧凑的视线重采样器将视线特征注入 LLM 解码器层，在 StreamGaze 基准测试中表现优异，准确率达 63.9%，显著超越了 GPT-4o 和其他开源模型，证明了在 LLM 内部注入视线信息比单纯增加模型规模或优化提示词更有效。"
selection_source: fresh_fetch
motivation: 现有的多模态大模型即使通过视觉叠加或文本描述也无法有效利用视线信息来理解视频。
method: 提出一种参数高效的隐藏状态调制方法，利用紧凑的视线重采样器将视线特征作为残差注入 LLM 解码器层。
result: "在 StreamGaze 基准测试中，GazeQwen 达到了 63.9% 的准确率，比 GPT-4o 高出 10.5 个百分点，刷新了最高纪录。"
conclusion: 研究表明，学习在 LLM 内部何处注入视线信息比扩大模型规模或设计更好的提示词更为有效。
---

## 摘要
当前的多模态大语言模型 (MLLM) 即使在通过视觉叠加或文本描述提供注视线索的情况下，也无法有效地利用眼动注视信息进行视频理解。我们提出了 GazeQwen，这是一种参数高效的方法，通过隐藏状态调制使开源 MLLM 具备注视感知能力。其核心是一个紧凑的注视重采样器（约 1-5 M 可训练参数），它将 V-JEPA 2.1 视频特征与基于注视点的位置编码相结合，并产生加性残差，通过前向钩子注入到选定的 LLM 解码器层中。一个可选的第二训练阶段向 LLM 添加了低秩自适应器 (LoRA) 以实现更紧密的集成。在 StreamGaze 基准测试的所有 10 个任务中，GazeQwen 达到了 63.9% 的准确率，比使用注视作为视觉提示的相同 Qwen2.5-VL-7B 主干网络提高了 16.1 个百分点，比 GPT-4o 提高了 10.5 个百分点，是所有测试的开源和私有模型中的最高分。这些结果表明，学习在 LLM 中的何处注入注视信息比扩展模型规模或设计更好的提示词更有效。所有代码和检查点均可在 https://github.com/phamtrongthang123/gazeqwen 获取。

## Abstract
Current multimodal large language models (MLLMs) cannot effectively utilize eye-gaze information for video understanding, even when gaze cues are supplied via visual overlays or text descriptions. We introduce GazeQwen, a parameter efficient approach that equips an open-source MLLM with gaze awareness through hidden-state modulation. At its core is a compact gaze resampler (~1-5 M trainable parameters) that encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into selected LLM decoder layers via forward hooks. An optional second training stage adds low-rank adapters (LoRA) to the LLM for tighter integration. Evaluated on all 10 tasks of the StreamGaze benchmark, GazeQwen reaches 63.9% accuracy, a +16.1 point gain over the same Qwen2.5-VL-7B backbone with gaze as visual prompts and +10.5 points over GPT-4o, the highest score among all open-source and proprietary models tested. These results suggest that learning where to inject gaze within an LLM is more effective than scaling model size or engineering better prompts. All code and checkpoints are available at https://github.com/phamtrongthang123/gazeqwen .

---

## 论文详细总结（自动生成）

这是一份关于论文《GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理第一视角（Egocentric）视频时，无法有效利用用户的**眼动注视（Eye-gaze）**信息。即使通过文本坐标、视觉覆盖（Overlays）或显著性图进行提示，模型的表现仍远低于人类水平。
*   **研究背景**：在增强现实（AR）和具身智能应用中，注视点是理解用户意图和预测下一步行动的关键。然而，现有的“表面级”提示工程（Prompt Engineering）不足以让模型建立起注视点与视频语义之间的深层关联。

### 2. 方法论
GazeQwen 提出了一种**参数高效的隐藏状态调制**方案，其核心思想是不修改 LLM 输入，而是直接干预其内部表示。
*   **核心组件：注视重采样器（Gaze Resampler）**：
    *   **输入处理**：使用冻结的 **V-JEPA 2.1** 提取视频特征（保留细粒度空间细节），并将注视坐标编码为 **DETR 风格的正弦位置编码**。
    *   **特征融合**：通过交叉注意力机制，将注视编码注入到注意力机制的 **Key** 中，从而引导模型关注注视点附近的视觉 Token，而不改变其原始语义。
    *   **瓶颈设计**：使用 32 个可学习的潜变量（Latents）作为信息瓶颈，强制模型提取与注视相关的紧凑摘要。
*   **关键技术：钩子注入（Hook-based Injection）**：
    *   利用 PyTorch 的前向钩子（Forward Hooks），将重采样器生成的加性残差注入到 LLM（Qwen2.5-VL-7B）的四个等间距解码器层中。
*   **训练策略（两阶段）**：
    *   **第一阶段**：仅训练注视重采样器（约 1-5M 参数），LLM 完全冻结。
    *   **第二阶段**：引入 Rank-8 的 LoRA 适配器（约 3.5M 参数）与重采样器联合微调，使 LLM 更好地整合注视信号。

### 3. 实验设计
*   **基准测试**：**StreamGaze**，包含 285 个第一视角视频和 8,521 个注视条件下的问答对。
*   **任务分类**：涵盖 10 类任务，分为“过去”（如物体跟踪）、“现在”（如属性识别）和“前瞻性”（如物体出现预警）三个维度。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 4 Sonnet/Opus。
    *   **开源模型**：Qwen2.5-VL, InternVL3.5, VITA 1.5 等。
    *   **流式视频模型**：ViSpeak, VideoLLM-online 等。
    *   **注视专用模型**：AssistGaze。

### 4. 资源与算力
*   **参数量**：非常轻量，可训练参数仅为 **5-9 M**（重采样器 1-5M + LoRA 3.5M）。
*   **算力需求**：论文明确提到支持**单 GPU 训练**（Single-GPU training）。
*   **具体配置**：文中未详细列出具体的 GPU 型号（如 A100 或 H100）及确切的训练时长，但强调了其低成本和模块化特性（无需重新加载权重即可挂载/卸载注视模块）。

### 5. 实验数量与充分性
*   **实验规模**：在 StreamGaze 的全部 10 个任务上进行了评估，使用了 70/15/15 的视频级数据划分以防止数据泄露。
*   **消融实验**：针对四个关键设计轴进行了充分的消融研究：
    1.  **注视编码方式**：对比了坐标正弦编码与热力图编码。
    2.  **视觉主干**：对比了 V-JEPA 2.1 与 DINOv2。
    3.  **层共享策略**：对比了独立重采样器与共享重采样器。
    4.  **适配器影响**：验证了 LoRA 的增益。
*   **客观性**：对比了当前最顶尖的闭源和开源模型，实验设计较为公平，涵盖了从感知到推理的多个维度。

### 6. 主要结论与发现
*   **性能突破**：GazeQwen 达到了 **63.9%** 的准确率，比 GPT-4o 高出 **10.5%**，比同底座的 Qwen2.5-VL（使用视觉提示）高出 **16.1%**。
*   **注视的作用**：注视信息在**消除空间歧义**的任务中提升最明显，如“物体出现预警”（+34.1%）和“物体跟踪”（+29.4%）。
*   **内部调制优于提示工程**：实验证明，在 LLM 内部学习如何注入注视信号，比单纯增加模型规模或优化 Prompt 更加有效。

### 7. 优点
*   **轻量且高效**：仅需极少量的额外参数即可实现大幅性能提升。
*   **非侵入式设计**：通过 Hook 注入，无需修改 LLM 的原始架构或前向传播代码。
*   **模块化**：注视模块可以根据需要随时挂载或拆卸，不影响模型的基础功能。
*   **空间精度**：采用正弦位置编码保留了亚像素级的空间精度，优于粗糙的视觉热力图。

### 8. 不足与局限
*   **人类差距**：尽管表现优异，但 63.9% 的准确率距离人类水平（82.7%）仍有较大差距。
*   **场景局限**：目前主要针对第一视角（Egocentric）视频，在第三视角或非交互式视频中的泛化能力尚待验证。
*   **时间推理瓶颈**：在某些需要高级时间推理的任务（如预测未来动作 FAP）中，注视信息的加入提升有限（仅 +1.7%），说明模型仍需更强的时序建模能力。

（完）
