---
title: "GLANCE: Gaze-Led Attention Network for Compressed Edge-inference"
title_zh: GLANCE：用于压缩边缘推理的视线引导注意力网络
authors: "Neeraj Solanki, Hong Ding, Sepehr Tabrizchi, Ali Shafiee Sarvestani, Shaahin Angizi, David Z. Pan, Arman Roohi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15717v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 用于AR/VR实时交互的注视引导注意力网络
tldr: 针对AR/VR设备对实时目标检测的极高算力和功耗要求，本文提出GLANCE框架。该框架受生物视觉启发，采用两阶段流程：首先利用可微分无权重神经网络通过内存查找实现超低功耗视线估计，随后根据视线引导ROI区域进行选择性目标检测。实验表明，该方法在显著降低计算量和能耗的同时，提升了检测精度，满足了边缘端亚10毫秒的延迟需求。
selection_source: fresh_fetch
motivation: AR/VR系统需要在极低功耗和内存限制下实现亚10毫秒延迟的实时目标检测。
method: 结合基于内存查找的无权重神经网络进行视线追踪，并利用视线引导的注意力机制对感兴趣区域进行选择性目标检测。
result: "在Arduino Nano上实现了48.1%的mAP，减少了40-50%的计算量和65%的能耗，通信效率提升177倍。"
conclusion: 证明了以内存为中心的架构结合显式注意力建模，在资源受限的穿戴式平台上比统一处理方式更具效率和精度优势。
---

## 摘要
AR/VR 系统中的实时目标检测面临严峻的计算限制，需要在紧凑的功耗预算内实现低于 10 毫秒的延迟。受生物中央凹视觉的启发，我们提出了一种两阶段流水线，将用于超高效视线估计的可微分无权重神经网络与注意力引导的感兴趣区域（ROI）目标检测相结合。我们的方法通过内存查找而非乘加运算进行视线追踪，消除了算术密集型操作，在每帧仅需 393 次 MAC 和 2.2 KiB 内存的情况下，实现了 8.32° 的角度误差。视线预测引导对关注区域进行选择性目标检测，将计算负担降低了 40-50%，能耗降低了 65%。在 Arduino Nano 33 BLE 上部署后，我们的系统在 COCO 数据集上实现了 48.1% 的 mAP（关注对象上为 51.8%），同时保持了低于 10 毫秒的延迟，通过将通信时间缩短 177 倍，满足了严苛的 AR/VR 需求。与全局 YOLOv12n 基准（对小、中、大目标的准确率分别为 39.2%、63.4% 和 83.1%）相比，基于 ROI 的方法在相同设置下分别达到了 51.3%、72.1% 和 88.1%。这项工作表明，对于资源受限的可穿戴平台，具有显式注意力建模的以内存为中心的架构比统一处理具有更高的效率和准确性。

## Abstract
Real-time object detection in AR/VR systems faces critical computational constraints, requiring sub-10\,ms latency within tight power budgets. Inspired by biological foveal vision, we propose a two-stage pipeline that combines differentiable weightless neural networks for ultra-efficient gaze estimation with attention-guided region-of-interest object detection. Our approach eliminates arithmetic-intensive operations by performing gaze tracking through memory lookups rather than multiply-accumulate computations, achieving an angular error of $8.32^{\circ}$ with only 393 MACs and 2.2 KiB of memory per frame. Gaze predictions guide selective object detection on attended regions, reducing computational burden by 40-50\% and energy consumption by 65\%. Deployed on the Arduino Nano 33 BLE, our system achieves 48.1\% mAP on COCO (51.8\% on attended objects) while maintaining sub-10\,ms latency, meeting stringent AR/VR requirements by improving the communication time by $\times 177$. Compared to the global YOLOv12n baseline, which achieves 39.2\%, 63.4\%, and 83.1\% accuracy for small, MEDium, and LARGE objects, respectively, the ROI-based method yields 51.3\%, 72.1\%, and 88.1\% under the same settings. This work shows that memory-centric architectures with explicit attention modeling offer better efficiency and accuracy for resource-constrained wearable platforms than uniform processing.

---

## 论文详细总结（自动生成）

### 论文总结：GLANCE - 用于压缩边缘推理的视线引导注意力网络

#### 1. 核心问题与研究动机
在 AR/VR 系统中，实时目标检测面临极高的计算挑战：设备需要在极低的功耗预算下实现亚 10 毫秒（sub-10ms）的延迟，以避免用户产生晕动症。目前的检测器（如 YOLO 系列）通常对整个视野进行均匀计算，这与人类视觉系统（中央凹视觉）高度选择性的特征相悖。论文的核心动机是利用**视线追踪（Gaze Tracking）**作为显式注意力信号，仅处理用户关注的区域，从而在资源受限的边缘设备上实现高效推理。

#### 2. 方法论核心思想与技术细节
GLANCE 提出了一个两阶段的流水线架构：
*   **超轻量级视线估计（DWN）：** 采用**可微分无权重神经网络（Differentiable Weightless Neural Networks）**。其核心在于放弃传统的乘加运算（MAC），转而使用基于内存查找表（LUT）的推理。
    *   **特征处理：** 通过温度计编码（Thermometer Encoding）将连续像素值转换为二进制模式。
    *   **内存查找：** 使用输入特征作为地址直接从 LUT 中检索学习到的值，极大地降低了算术强度。
*   **注意力引导的 ROI 形成：**
    *   根据视线估计的预测点及其不确定性（角度误差），生成多个感兴趣区域（ROI）。
    *   **ROI 联合马赛克（Union-of-ROIs Mosaic）：** 将多个 ROI 合并为一个单一的裁剪图像送入检测器，避免多次推理并减少内存流量。
*   **时空稳定机制：**
    *   **时间联合策略：** 维护一个 K 帧的滑动窗口，累积关注区域以稳定召回率。
    *   **运动补偿：** 利用惯性测量单元（IMU）数据，在检测器刷新间隔内对 ROI 进行旋转校正和膨胀处理，应对头部运动。

#### 3. 实验设计与基准测试
*   **数据集：** 
    *   **MPIIGaze：** 用于评估视线估计精度。
    *   **COCO：** 用于评估目标检测性能（按小、中、大目标分类统计）。
*   **基准方法（Benchmarks）：**
    *   **视线估计：** 对比了 iTracker、Full-Face CNN、CA-Net、SAZE 等深度学习模型。
    *   **目标检测：** 以全局扫描的 **YOLOv12n** 作为性能基准。
*   **硬件平台：** 边缘端使用 **Arduino Nano 33 BLE**（微控制器），主机端使用 **NVIDIA RTX 3090**。

#### 4. 资源与算力需求
*   **边缘端（DWN）：** 极度轻量，仅需 **393 次 MAC 运算**和 **2.2 KiB 内存**。在 MSP430、Arduino Nano 等微控制器上即可运行。
*   **主机端：** 使用 RTX 3090 进行 YOLOv12n 推理。
*   **训练细节：** 论文提到使用 Adam 优化器，但未明确说明具体的总训练时长。其优势在于推理阶段的内存占用极低（比传统 CNN 模型小几个数量级）。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了跨人的留一法（LOPO）视线估计实验，并在 COCO 验证集上针对不同 ROI 尺寸（48, 64, 80 像素）和数量（N=1 到 50）进行了详尽的消融实验。
*   **公平性：** 实验对比了相同检测器（YOLOv12n）在全局模式与 ROI 模式下的表现，确保了精度提升完全源于空间注意力的优化。实验设计较为客观，涵盖了从底层算子到系统级延迟的完整链路。

#### 6. 主要结论与发现
*   **效率提升：** 相比传输全图，GLANCE 将通信时间缩短了 **177 倍**，能耗降低了 **65%**，计算负担减轻了 **40-50%**。
*   **精度优势：** 在相同检测器下，ROI 引导的方法在小目标检测上表现尤为突出（从 39.2% 提升至 51.3%），证明了局部高分辨率处理比全局均匀处理更有效。
*   **实时性：** 系统在微控制器环境下实现了亚 10 毫秒的端到端延迟，满足 AR/VR 的严苛要求。

#### 7. 优点与亮点
*   **架构创新：** 首次将无权重神经网络（WNN）应用于视线引导的边缘视觉任务，实现了近乎“零 MAC”的注意力生成。
*   **系统级优化：** 不仅仅关注算法，还考虑了 IMU 补偿、ROI 联合马赛克等实际部署中的工程问题。
*   **资源友好：** 极小的内存占用（2.2 KiB）使其能够运行在极其廉价、低功耗的微控制器上。

#### 8. 不足与局限
*   **视线精度权衡：** 虽然 DWN 极其高效，但其 8.32° 的角度误差高于某些重型 CNN 模型（约 4°），这需要通过增加 ROI 覆盖范围来补偿。
*   **静态数据集限制：** 目标检测评估主要基于静态的 COCO 数据集，虽然通过模拟扫描路径进行了时间累积实验，但在真实动态场景下的表现仍需更多实测数据支持。
*   **应用依赖：** 该方法高度依赖于视线追踪硬件的集成，对于不具备眼动追踪功能的普通边缘视觉设备不适用。

（完）
