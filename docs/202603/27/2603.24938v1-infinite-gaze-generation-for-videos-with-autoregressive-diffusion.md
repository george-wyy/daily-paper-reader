---
title: Infinite Gaze Generation for Videos with Autoregressive Diffusion
title_zh: 基于自回归扩散模型的视频无限注视生成
authors: "Jenna Kang, Colin Groth, Tong Wu, Finley Torrens, Patsorn Sangkloy, Gordon Wetzstein, Qi Sun"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24938v1"
tags: ["query:gaze-intent"]
score: 10.0
evidence: 预测人类注视以推进多模态交互
tldr: 针对视频中人类注视点预测存在的细粒度动态缺失及短期预测限制等问题，本文提出了一种基于自回归扩散模型的无限时长原始注视点生成框架。该方法利用显著性感知视觉潜空间，能够合成具有连续空间坐标和高分辨率时间戳的长程注视轨迹。实验证明，该模型在长程时空准确性和轨迹真实感方面显著优于现有方法，为场景理解和多模态交互提供了有力支持。
selection_source: fresh_fetch
motivation: 传统注视点预测模型往往忽略细粒度时间动态，且难以处理长视频中的长程行为依赖。
method: 提出一种自回归扩散模型，结合显著性感知视觉潜空间，生成具有连续坐标和高分辨率时间戳的无限时长注视轨迹。
result: 定量与定性评估显示，该方法在长程时空准确度和轨迹真实感上均显著超越了现有技术。
conclusion: 该研究通过生成式框架实现了任意长度视频的原始注视点预测，有效提升了视频场景理解的深度。
---

## 摘要
预测视频中的人类注视是推进场景理解和多模态交互的基础。虽然传统的显著性图提供了空间概率分布，而扫描路径提供了有序的注视点，但这两种抽象方式往往会压缩原始注视数据中细粒度的时序动态。此外，现有模型通常受限于短期窗口（约 3-5 秒），无法捕捉现实内容中固有的长程行为依赖关系。我们提出了一个用于在任意长度视频中进行无限时界原始注视预测的生成框架。通过利用自回归扩散模型，我们合成了具有连续空间坐标和高分辨率时间戳特征的注视轨迹。我们的模型以显著性感知的视觉潜空间为条件。定量和定性评估表明，我们的方法在长程时空准确性和轨迹真实性方面显著优于现有方法。

## Abstract
Predicting human gaze in video is fundamental to advancing scene understanding and multimodal interaction. While traditional saliency maps provide spatial probability distributions and scanpaths offer ordered fixations, both abstractions often collapse the fine-grained temporal dynamics of raw gaze. Furthermore, existing models are typically constrained to short-term windows ($\approx$ 3-5s), failing to capture the long-range behavioral dependencies inherent in real-world content. We present a generative framework for infinite-horizon raw gaze prediction in videos of arbitrary length. By leveraging an autoregressive diffusion model, we synthesize gaze trajectories characterized by continuous spatial coordinates and high-resolution timestamps. Our model is conditioned on a saliency-aware visual latent space. Quantitative and qualitative evaluations demonstrate that our approach significantly outperforms existing approaches in long-range spatio-temporal accuracy and trajectory realism.

---

## 论文详细总结（自动生成）

这是一份关于论文《Infinite Gaze Generation for Videos with Autoregressive Diffusion》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在任意长度的视频中生成连续、高频且符合人类行为逻辑的原始注视轨迹（Raw Gaze Trajectories）。
*   **研究动机**：
    *   **现有抽象的局限性**：传统的“显著性图”（空间概率）和“扫描路径”（离散固定点序列）压缩了人类注视中细粒度的时间动态（如平滑追踪运动）。
    *   **时长限制**：现有模型通常只能预测 3-5 秒的短期窗口，无法处理长视频中的长程行为依赖。
    *   **数据稀缺性**：高精度眼动数据采集成本高、校准复杂且涉及隐私，因此需要生成式模型来合成逼真的注视数据。

### 2. 方法论
*   **核心思想**：结合**自回归（Autoregressive）机制**与**扩散模型（Diffusion Model）**，将视频注视生成建模为一个条件序列生成任务，实现无限时长的轨迹合成。
*   **关键技术细节**：
    *   **自回归扩散框架**：基于 DDPM 架构，模型在生成当前窗口的注视轨迹时，会以之前生成的 $k$ 个坐标作为历史缓存（Prefix），确保时间上的连续性和平滑过渡。
    *   **显著性感知视觉条件（Saliency-aware Conditioning）**：
        *   作者认为显著性特征比原始 RGB 特征更能引导眼动。
        *   使用修改后的 **UNISAL** 模型作为编码器，将 RNN 通道从 256 压缩至 64，并通过 2D 平均池化进一步压缩空间维度，形成轻量化的视觉潜变量。
    *   **U-Net 骨干网络**：采用一维卷积（1D Convolutional）块和多尺度自注意力机制处理坐标序列，通过交叉注意力（Cross-attention）注入视觉条件。
*   **算法流程**：输入视频帧序列和历史注视坐标 $\rightarrow$ 提取显著性潜变量 $\rightarrow$ 扩散模型迭代去噪 $\rightarrow$ 输出未来一段窗口的连续坐标 $\rightarrow$ 将输出作为下一轮的历史输入。

### 3. 实验设计
*   **数据集**：
    *   **DIEM**：包含 84 个长视频（30-209秒），用于模型训练和域内测试。
    *   **DHF1K**：包含 1000 个视频，选取其中不同运动模式的子集用于评估模型的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 6 种最先进的方法：**DeepGaze III**（卷积网络）、**DiffEye**（基于图像的扩散模型）、**GazeFormer** 与 **HAT**（Transformer 架构）、**TPP-Gaze**（神经时间点过程）以及 **Chen et al.**（强化学习）。
*   **评估指标**：Levenshtein 距离、离散 Fréchet 距离、动态时间规整（DTW）、最大时间相关性（Maximum Temporal Correlation）。

### 4. 资源与算力
*   **训练细节**：模型训练了 70 个 Epoch，使用 Adam 优化器，学习率为 $1 \times 10^{-4}$。
*   **扩散参数**：训练时使用 1000 步线性噪声调度，推理时使用 DDIM 采样缩减至 50 步以提高效率。
*   **算力说明**：**论文未明确提及具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了详尽的定量对比。
    *   **消融实验**：测试了不同预测长度（10、45、90帧）、不同初始化策略（有无历史记录）以及不同视觉条件（显著性 vs. MAGVIT2 视觉 Tokenizer）的影响。
    *   **用户研究**：邀请 12 名参与者对生成轨迹的真实感进行主观盲测。
*   **充分性评价**：实验设计较为全面，不仅涵盖了客观几何指标，还通过用户研究验证了指标（如 Fréchet 距离）在视频任务中的局限性，体现了研究的客观性。

### 6. 主要结论与发现
*   **性能领先**：该方法在 Levenshtein、DTW 和时间相关性指标上显著优于所有基准模型，证明了其捕捉长程时空动态的能力。
*   **显著性条件的有效性**：实验证明，相比于通用的视觉 Token（如 MAGVIT2），显著性潜变量能提供更稳定的训练收敛和更高的预测精度。
*   **预测窗口平衡**：发现 45 帧（约 1.5 秒）的预测长度在短期准确性和长期稳定性之间达到了最佳平衡。
*   **主观偏好**：用户研究显示，85% 的情况下人类认为该模型生成的轨迹比目前表现最好的基准模型更接近真实人类。

### 7. 优点与亮点
*   **无限时长生成**：通过自回归机制打破了传统模型只能预测固定短时窗口的限制。
*   **高频连续性**：不同于离散的注视点预测，该模型能生成平滑的原始轨迹，更符合视频观看时的生理特征（如平滑追踪）。
*   **高效条件压缩**：创新的显著性感知瓶颈设计，既保留了关键视觉引导信息，又大幅降低了视频处理的计算开销。

### 8. 不足与局限
*   **长程依赖挑战**：虽然是自回归结构，但模型仍受限于固定的滑动窗口，可能无法完全模拟人类基于长期记忆（如对几分钟前出现的情节的记忆）产生的注视行为。
*   **头动缺失**：训练数据基于固定头部的眼动仪，未考虑现实中头部运动与眼动的耦合关系。
*   **指标敏感性**：离散 Fréchet 距离对局部离群点过于敏感，在评估视频连续轨迹时可能存在偏差。
*   **应用限制**：目前主要针对常规 RGB 视频，在 VR/AR 等需要 360 度全景或深度信息的场景下尚需进一步扩展。

（完）
