---
title: Semantic Neighborhood Density and Eye Gaze Time in Human Programmer Attention
title_zh: 人类程序员注意力中的语义邻域密度与眼动注视时间
authors: "Robert Wallace, Emory Michaels, Yu Huang, Collin McMillan"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03566v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 编程中的眼动注视时间与人类注意力
tldr: 本研究探讨了程序员阅读源代码时，单词的语义邻域密度（SND）与眼动注视时间之间的关系。研究者利用以往 C 和 Java 语言的眼动追踪数据，通过统计分析和预测建模，发现 SND 与注视时间显著相关。研究证明，高 SND 的单词（尤其是低频词）会吸引更多注意力，这表明 SND 是衡量软件工程中认知负荷和代码理解质量的重要指标。
selection_source: fresh_fetch
motivation: 旨在研究语义邻域密度（SND）这一心理学指标在软件工程领域中如何影响程序员的注意力分配。
method: 对 C 和 Java 的眼动追踪实验数据进行二次分析，计算代码标记的 SND 并结合频率进行统计建模。
result: 发现高 SND 单词（特别是罕见词）的注视时间更长，且 SND 与词频对注视时间具有一定的预测作用。
conclusion: SND 是影响程序员认知负荷的关键因素，为优化代码可读性和理解工具提供了新的理论依据。
---

## 摘要
本文研究了源代码中单词的人类眼动注视时间与其语义邻域密度（SND）之间的关系。人类眼动注视时间是量化人类注意力的一种常用方法，例如人们阅读单词的重要性以及所付出的认知努力。同时，SND 是衡量一个单词在相同语境中与其他单词意思相似程度的指标。SND 在心理学研究中有着悠久的历史，在多个领域中它都与眼动注视时间相关联，并有助于解释人类的认知因素，如困惑感和阅读理解质量。但在软件工程领域，SND 具有未知且可能独特的含义。在本文中，我们计算了两次先前眼动追踪实验（一次针对 C 语言，一次针对 Java 语言）中受试者查看的源代码标记（tokens）的 SND。我们进行了无模型分析以研究 SND 与注视时间之间的统计关系，并进行了基于模型的分析以研究 SND 对注视时间的预测能力。我们发现，高 SND 的单词往往比低 SND 的单词具有更长的注视时间，特别是对于那些不常用（即低频）的单词。我们还发现，尽管眼动追踪数据中普遍存在高水平的噪声，但 SND 和频率对注视时间仍具有一定的预测能力。

## Abstract
This paper studies the relationship between human eye gaze time on words in source code and the Semantic Neighborhood Density (SND) of those words. Human eye gaze time is a popular way to quantify human attention such as the importance of words people read and the cognitive effort people exert. Meanwhile, SND is a measure of how similar a word is in meaning to other words in the same context. SND has a long history in Psychology research where it has been connected to eye gaze time in various domains and helps explain human cognitive factors such as confusion and quality of reading comprehension. But SND carries an unknown and potentially unique meaning in software engineering. In this paper, we compute SND for tokens in source code that people viewed in two previous eye-tracking experiments, one in C and one in Java. We conduct a model-free analysis for statistical relationships between SND and gaze time, and a model-based analysis for predictive power of SND to gaze time. We found that words with high SND tend to have higher gaze time then low SND words, especially for words that are uncommon (i.e., have low frequency). We also found SND and frequency to have a minor predictive power on gaze time, despite high levels of noise common in eye tracking data

---

## 论文详细总结（自动生成）

以下是对论文《Semantic Neighborhood Density and Eye Gaze Time in Human Programmer Attention》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：理解程序员在阅读代码时的注意力分配对于优化用户界面设计、人员配置以及设计更符合人类逻辑的 AI 注意力系统至关重要。
*   **核心问题**：探讨源代码中单词的**语义邻域密度（Semantic Neighborhood Density, SND）**与人类**眼动注视时间（Eye Gaze Time）**之间是否存在关联。
*   **背景**：SND 衡量一个词在语境中与多少个意思相近的词“相邻”。在心理学中，SND 已被证明会影响阅读理解，但在软件工程（SE）领域，这种效应尚未被深入研究。

### 2. 方法论
*   **核心思想**：通过计算代码标记（Tokens）的向量表示，量化其在语义空间中的拥挤程度（SND），并将其与已有的程序员眼动追踪数据进行关联分析。
*   **关键技术细节**：
    *   **预处理**：使用 srcML 提取 Token。Java 语言需进行驼峰命名和下划线分割，C 语言则直接使用原始 Token。
    *   **词嵌入（Word Embeddings）**：采用 GPT2（针对 Java 和 C 专门训练的版本）和 CodeLLaMA（7B 参数版）将单词映射为高维向量。
    *   **SND 计算（ARC 算法）**：使用“平均共现半径（Average Radius of Co-occurrence）”。
        1.  计算全局距离阈值 $\tau$（均值减去 1.5 倍标准差）。
        2.  定义语义邻域：在向量空间中距离小于 $\tau$ 的所有单词集合。
        3.  计算 SND：目标词与其邻域内所有词的平均余弦相似度。
    *   **词频（TF）计算**：计算语料库级别的词频，作为控制变量。
    *   **眼动指标**：计算单次注视时长（SFD）、首次注视时长（FFD）、注视总时长（GD）和回归路径时长（RPD）。

### 3. 实验设计
*   **数据集**：
    *   **Smith 数据集（C 语言）**：21 名程序员，执行错误定位任务，共 31 小时眼动数据。
    *   **Wallace 数据集（Java 语言）**：10 名程序员，执行代码总结任务，共 60 小时眼动数据。
*   **分析方法**：
    *   **无模型分析（Model-Free）**：使用置换检验（Permutation Test）和 Hedges' g 效应量，比较高/低 SND 组和高/低词频组的注视时间差异。
    *   **基于模型的分析（Model-Based）**：使用广义线性模型（GLM）和留一交叉验证（LOO），尝试根据眼动指标预测单词是否属于高 SND 类别。
*   **对比维度**：对比了不同编程语言（C vs Java）、不同任务类型以及不同词嵌入模型（GPT2 vs CodeLLaMA）。

### 4. 资源与算力
*   论文中提到使用了 **GPT2 (350M 参数)** 和 **CodeLLaMA (7B 参数)** 模型。
*   **算力说明**：文中未明确给出具体的 GPU 型号、数量或训练时长。但作者指出，350M 参数的模型大小适中，足以在消费级硬件上运行而无需精度损失。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流编程语言，总计 91 小时的眼动追踪数据，涉及 31 名受试者。
*   **充分性**：实验设计较为充分。作者不仅做了统计显著性检验，还引入了词频作为交互变量，并使用了两种不同的词嵌入模型来验证结果的稳健性。
*   **客观性**：采用了置换检验（不依赖分布假设）和 Winsorization 处理异常值，确保了统计分析的客观性和公平性。

### 6. 主要结论与发现
*   **SND 的抑制效应**：高 SND 的单词通常会导致更长的注视时间。这表明语义空间越“拥挤”，程序员处理该词时的认知负担越重。
*   **交互作用**：**高 SND 且低频（罕见）的单词**吸引的视觉注意力显著更多。这是本文最强有力的发现。
*   **预测能力**：SND 和词频对注视时间具有一定的预测能力（AUC 在 0.55 到 0.67 之间），其中对“回归路径时长（RPD）”的预测效果最好。
*   **模型差异**：在计算 SND 时，专门针对特定语言训练的 GPT2 模型通常比通用的 CodeLLaMA 表现出更强的统计关联性。

### 7. 优点
*   **跨学科创新**：首次将心理学中的 SND 概念引入软件工程的程序理解研究中。
*   **方法严谨**：控制了词频和可预测性等潜在干扰因素，使用了稳健的统计测试方法。
*   **多维度验证**：在两种语言和两种完全不同的编程任务（找 Bug vs 写文档）中均发现了相似的趋势。

### 8. 不足与局限
*   **数据噪声**：眼动追踪数据本身具有高度噪声，受试者的个人背景和生理状态可能影响结果。
*   **词嵌入依赖**：SND 的计算高度依赖于词嵌入模型的质量，不同的向量空间可能会产生不同的 SND 值。
*   **任务局限性**：实验仅覆盖了错误定位和代码总结，尚不清楚在代码重构或系统设计等其他任务中是否存在相同规律。
*   **忽略结构**：目前的分析是单词级别的，忽略了代码的语法结构和上下文逻辑流对注意力的影响。

（完）
