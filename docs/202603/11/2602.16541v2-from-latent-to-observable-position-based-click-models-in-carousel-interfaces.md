---
title: From Latent to Observable Position-Based Click Models in Carousel Interfaces
title_zh: 从隐变量到可观测：轮播界面中基于位置的点击模型
authors: "Santiago de Leon-Martinez, Robert Moro, Maria Bielikova"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16541v2"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 源自眼动追踪数据的观察到的检查信号
tldr: 本研究针对现代推荐系统中常见的轮播图界面，探讨了其点击模型的优化与结构。由于传统单列表模型无法捕捉复杂的滑动浏览行为，作者提出了三种新型位置点击模型，包括首个利用眼动追踪数据作为显式检查信号的 OEPBM 模型。通过对比梯度优化与传统 EM/MLE 方法，研究发现梯度法表现更优，且 OEPBM 在点击预测和行为对齐上效果显著。研究强调了在复杂界面中引入额外行为信号对提升模型真实性的重要性。
selection_source: carryover_cache
motivation: 传统的单列表点击模型无法有效处理现代推荐系统中轮播图界面的复杂交互和用户浏览行为。
method: 提出了三种针对轮播图的点击模型，特别是引入眼动追踪数据作为观察信号的 OEPBM，并采用梯度下降进行优化。
result: 实验表明梯度优化在点击似然度上优于传统方法，且 OEPBM 在预测准确性和行为对齐方面表现最佳。
conclusion: 仅靠点击数据难以在复杂界面中真实建模用户行为，必须结合额外的行为信号来提升推荐系统的建模能力。
---

## 摘要
点击模型是推荐系统学习与评估的核心组件，但现有的大多数模型是针对单一排序列表界面设计的。相比之下，现代推荐平台越来越多地使用复杂的界面，例如轮播图（carousels），它由多个可滑动的列表组成，支持复杂的用户浏览行为。在本文中，我们研究了轮播界面中基于位置的点击模型，并探讨了优化方法、模型结构以及与用户行为的一致性。我们提出了三种专门为轮播界面设计的基于位置的新型模型，其中包括第一个不含隐变量、并结合了源自眼动追踪数据的观测考察信号的基于位置的模型，称为观测考察位置模型（OEPBM）。我们开发了这些轮播点击模型的通用实现，支持多种优化技术，并进行了实验，将基于梯度的方法与经典方法（即期望最大化和极大似然估计）进行了比较。我们的结果表明，基于梯度的优化始终能获得更好的点击似然度。在评估的模型中，OEPBM 在点击预测方面表现最强，并产生了与用户行为最一致的考察模式。然而，我们也证明了强大的点击拟合并不意味着对用户考察和浏览模式的真实建模。这揭示了在复杂界面中仅依赖点击的模型的根本局限性，以及在为基于轮播图的推荐系统设计点击模型时，引入额外行为信号的必要性。

## Abstract
Click models are a central component of learning and evaluation in recommender systems, yet most existing models are designed for single ranked-list interfaces. In contrast, modern recommender platforms increasingly use complex interfaces such as carousels, which consist of multiple swipeable lists that enable complex user browsing behaviors.   In this paper, we study position-based click models in carousel interfaces and examine optimization methods, model structure, and alignment with user behavior. We propose three novel position-based models tailored to carousels, including the first position-based model without latent variables that incorporates observed examination signals derived from eye tracking data, called the Observed Examination Position-Based Model (OEPBM). We develop a general implementation of these carousel click models, supporting multiple optimization techniques and conduct experiments comparing gradient-based methods with classical approaches, namely expectation-maximization and maximum likelihood estimation.   Our results show that gradient-based optimization consistently achieve better click likelihoods. Among the evaluated models, the OEPBM achieves the strongest performance in click prediction and produces examination patterns that most closely align to user behavior. However, we also demonstrate that strong click fit does not imply realistic modeling of user examination and browsing patterns. This reveals a fundamental limitation of click-only models in complex interfaces and the need for incorporating additional behavioral signals when designing click models for carousel-based recommender systems.

---

## 论文详细总结（自动生成）

### 论文总结：从隐变量到可观测：轮播界面中基于位置的点击模型

#### 1. 核心问题与整体含义（研究动机和背景）
传统的点击模型（如级联模型 CM 或位置模型 PBM）主要针对单一的垂直排列列表设计。然而，现代推荐系统（如 Netflix、Spotify）广泛采用**轮播界面（Carousel Interfaces）**，用户可以纵向滚动行，也可以横向滑动列表。
*   **研究动机**：现有的轮播点击模型（如 CCM）多基于级联假设，认为用户是顺序浏览的，但这与眼动追踪研究发现的“F型”浏览模式不符。
*   **核心问题**：如何设计能准确捕捉轮播界面复杂浏览行为的模型？基于位置的模型（PBM）是否优于级联模型？引入眼动追踪数据能否解决点击模型中“考察（Examination）”这一隐变量难以估计的问题？

#### 2. 方法论
论文提出了三种新型的轮播位置点击模型，其核心公式遵循 $P(click) = P(examination) \times P(attraction)$：
*   **CPBM (Carousel Position-Based Model)**：为轮播图中的每个行列位置 $(i, j)$ 独立学习一个考察概率 $w_{i,j}$。
*   **RCPBM (Row-Column PBM)**：为了减少参数量，将位置考察概率分解为行权重和列权重的乘积 $w_i \times w_j$，捕捉行列间的依赖关系。
*   **OEPBM (Observed Examination PBM)**：**本文核心创新**。利用眼动追踪数据中的“注视点（Fixations）”作为显式的考察信号，将原本不可见的隐变量 $w$ 变为可观测变量。
*   **优化技术**：除了传统的期望最大化（EM）和极大似然估计（MLE），论文引入了**梯度上升（Gradient Ascent, GA）**优化方法，并推导了相应的更新规则。

#### 3. 实验设计
*   **数据集**：使用 **RecGaze 数据集**，这是目前唯一包含轮播界面眼动追踪和点击交互的数据集（包含 87 名用户、30 个电影选择屏幕、共 2,432 个有效会话）。
*   **对比方法（Benchmarks）**：
    *   **TCM (Terminating Cascade Model)**：带终止概率的级联模型。
    *   **CCM (Carousel Click Model)**：现有的专门针对轮播图的级联点击模型。
*   **实验场景**：
    1.  **标准场景**：同时学习吸引力和考察概率。
    2.  **固定吸引力场景**：将吸引力固定为点击率（CTR），仅优化考察参数，以测试模型对浏览行为的建模能力。
*   **评估指标**：点击对数似然（Test LL）和观测考察对数似然（Test OELL）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件型号（如 GPU 类型）或训练时长。考虑到点击模型的参数量相对较小（主要是位置权重和物品吸引力权重），且数据集规模在数千条记录级别，该实验通常在普通的 CPU 或单块消费级 GPU 上即可快速完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文对比了 5 种主要模型（TCM, CCM, CPBM, RCPBM, OEPBM），涵盖了 3 种优化算法（MLE, EM, GA）以及 4 种不同的参数初始化策略（如基于 CTR 或基于眼动数据的初始化）。
*   **充分性评价**：实验设计较为全面，通过“标准”与“固定吸引力”两种设置对比，有效地分离了吸引力和位置偏差的影响。然而，受限于 RecGaze 数据集的规模（仅 1,307 个物品），实验在处理长尾物品和深度滑动位置的稀疏性方面仍面临挑战。

#### 6. 主要结论与发现
*   **优化方法**：基于梯度的优化（GA）在拟合点击似然度方面始终优于传统的 EM 和 MLE 方法。
*   **模型表现**：PBM 变体在点击预测上普遍优于现有的级联模型（CCM/TCM）。
*   **OEPBM 的优越性**：引入眼动信号的 OEPBM 在点击预测和真实行为对齐（OELL）上均表现最佳。
*   **核心警示**：**高点击似然度并不等同于真实的浏览建模**。仅优化点击似然度的模型可能会产生不符合实际的考察概率分布，这证明了在复杂界面中引入额外行为信号（如眼动）的必要性。

#### 7. 优点
*   **理论创新**：首次将 PBM 扩展到轮播界面，并提出了首个完全可观测的点击模型（OEPBM）。
*   **方法论严谨**：不仅关注预测准确率，还通过眼动数据验证了模型内部变量（考察概率）的真实性。
*   **实用性**：证明了即使没有实时眼动数据，使用少量眼动数据进行“热启动”初始化也能显著提升传统点击模型的表现。

#### 8. 不足与局限
*   **数据稀疏性**：RecGaze 数据集规模较小，测试集中深度滑动位置（如第 6 行以后）的点击数据极少，限制了模型在深层浏览场景下的验证。
*   **应用门槛**：OEPBM 依赖昂贵的眼动追踪数据，这在大多数商业大规模推荐场景中难以实时获取。
*   **界面单一**：实验仅基于桌面端的轮播界面，未探讨移动端（手势滑动）是否存在不同的行为模式。

（完）
