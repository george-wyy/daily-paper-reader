---
title: "Beyond Scanpaths: Graph-Based Gaze Simulation in Dynamic Scenes"
title_zh: 超越扫描路径：动态场景中基于图的注视模拟
authors: "Luke Palmer, Petar Palasek, Hazem Abdelkawy"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28319v1"
tags: ["query:gaze-intent"]
score: 9.0
evidence: 用于眼动建模的自回归动力系统及下一步眼动分布预测
tldr: 本研究针对自动驾驶安全中的人类注意力建模问题，提出了一种基于自回归动力系统的原始注视轨迹建模方法。不同于传统的显著性图或扫描路径，该方法利用异构图变换器（ART）处理注视中心图，捕捉驾驶员、交通对象与道路结构间的交互，并结合对象密度网络（ODN）预测注视分布。此外，研究发布了Focus100数据集，包含30名受试者的原始注视数据。实验证明，该方法在生成自然注视轨迹和显著性图方面优于现有模型。
selection_source: fresh_fetch
motivation: 现有的注意力建模方法通常将注视简化为显著性图或扫描路径，难以捕捉复杂动态驾驶场景中原始注视轨迹的显式时间动态。
method: 提出了一种基于异构图变换器（ART）和对象密度网络（ODN）的自回归框架，通过注视中心图建模驾驶员与环境的交互。
result: 该方法在无需固定点过滤的情况下，生成的注视轨迹、扫描路径动态和显著性图比现有模型更符合人类自然表现。
conclusion: 通过将注视建模为动态环境下的图动力系统，本研究为动态场景中人类注意力的时间建模提供了更有效的方案。
---

## 摘要
准确建模人类注意力对于众多计算机视觉应用至关重要，特别是在汽车安全领域。现有方法通常将注视点折叠为显著性图或扫描路径，仅隐式地处理注视动力学。相反，我们将注视建模表述为一个自回归动力系统，并根据注视历史和不断演变的场景环境，显式地随时间展开原始注视轨迹。驾驶场景被表示为以注视为中心的图，并由亲和关系变换器（Affinity Relation Transformer, ART）进行处理。ART 是一种异构图变换器，用于建模驾驶员注视、交通目标和道路结构之间的相互作用。我们进一步引入了目标密度网络（Object Density Network, ODN）来预测下一步的注视分布，从而捕捉复杂环境中注意力转移的随机性和以目标为中心的特性。我们还发布了 Focus100，这是一个包含 30 名参与者观看第一视角驾驶视频的原始注视数据的新数据集。我们的统一方法直接在原始注视数据上进行训练，无需注视过滤，与现有的注意力模型相比，能够生成更自然的注视轨迹、扫描路径动力学和显著性图，为动态环境中人类注意力的时间建模提供了宝贵的见解。

## Abstract
Accurately modelling human attention is essential for numerous computer vision applications, particularly in the domain of automotive safety. Existing methods typically collapse gaze into saliency maps or scanpaths, treating gaze dynamics only implicitly. We instead formulate gaze modelling as an autoregressive dynamical system and explicitly unroll raw gaze trajectories over time, conditioned on both gaze history and the evolving environment. Driving scenes are represented as gaze-centric graphs processed by the Affinity Relation Transformer (ART), a heterogeneous graph transformer that models interactions between driver gaze, traffic objects, and road structure. We further introduce the Object Density Network (ODN) to predict next-step gaze distributions, capturing the stochastic and object-centric nature of attentional shifts in complex environments. We also release Focus100, a new dataset of raw gaze data from 30 participants viewing egocentric driving footage. Trained directly on raw gaze, without fixation filtering, our unified approach produces more natural gaze trajectories, scanpath dynamics, and saliency maps than existing attention models, offering valuable insights for the temporal modelling of human attention in dynamic environments.

---

## 论文详细总结（自动生成）

这篇论文提出了一种在动态驾驶场景中模拟人类注视（Gaze）的新框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的注意力建模通常将注视点简化为静态的“显著性图”（Saliency Maps）或离散的“扫描路径”（Scanpaths）。这种做法忽略了注视的连续动力学特性（如平滑追踪运动），且依赖于不可靠的固定点过滤算法。
*   **核心问题**：如何在不进行固定点过滤的情况下，直接从原始注视序列中学习，并建立一个能够同时生成自然注视轨迹、扫描路径和显著性图的统一动力系统模型。

### 2. 方法论
该方法将注视预测视为一种**基于图的模拟（Graph-Based Simulation, GBS）**，核心组件包括：
*   **时空异构场景图**：将驾驶场景表示为图。节点包括交通参与者（车辆、行人）、静态目标（红绿灯）、道路结构以及一个特殊的“注视节点”（代表驾驶员的中央凹视野）。边连接空间邻近节点和时间序列节点。
*   **亲和关系变换器（Affinity Relation Transformer, ART）**：一种改进的异构图变换器。它将节点间的相对位置、时间差和外观相似度等“亲和特征”直接注入注意力机制，从而更好地建模注视与环境目标间的交互。
*   **对象密度网络（Object Density Network, ODN）**：这是一个自适应的混合密度输出头。不同于固定高斯组件数量的传统方法，ODN 将图中的每个节点视为一个高斯组件。模型预测注视落在某个节点上的概率，从而实现以目标为中心的注意力建模。
*   **自回归模拟**：模型通过采样预测下一步注视位置，将其反馈回图中，实现长程注视轨迹的滚动生成。

### 3. 实验设计
*   **数据集**：
    *   **Focus100（新发布）**：包含100个60秒的驾驶视频，30名受试者，提供60Hz原始注视数据和危险标注。其交通密度和多样性显著高于现有数据集。
    *   **MAAD**：现有的同步原始注视驾驶数据集。
*   **对比方法（Baselines）**：包括显著性预测模型（GLC, SCOUT, ViNet, DReyeVENet）和传统模型（Itti, GBVS）。
*   **评估指标**：
    *   **序列指标**：动态时间规整（DTW）、时间相关性（TC）、编辑距离（LEV）。
    *   **动力学指标**：固定点持续时间、固定点频率。
    *   **显著性指标**：NSS、IG、AUC。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA L40S GPU**。
*   **训练细节**：采用 float16 半精度训练，Focus100 训练 50 个 epoch，Batch Size 为 128。
*   **推理效率**：在单张 L40S 上，包含感知、建图和推理的全流程耗时约 68ms/帧（约 15 fps）。

### 5. 实验数量与充分性
*   **实验规模**：在两个数据集（Focus100 和 MAAD）上进行了全面对比。
*   **消融实验**：针对时间窗口大小、图处理器类型（对比 HGT 和 HEAT）、输出头类型（对比标准 MDN）进行了详细消融。
*   **充分性评价**：实验设计较为充分。通过跨数据集验证和多种维度的指标评估，证明了模型在生成原始轨迹和聚合显著性图方面的通用性。

### 6. 主要结论与发现
*   **统一性**：单一模型在无需针对性训练的情况下，通过后处理即可在轨迹生成、扫描路径和显著性图三个任务上达到 SOTA。
*   **自然度**：ART 生成的注视序列在时间动力学（如固定点频率和持续时间）上与人类高度相似，远超基于显著性图采样的基准模型。
*   **目标驱动**：ODN 证明了人类驾驶时的注意力转移具有强烈的“对象导向”特性。

### 7. 优点与亮点
*   **无需过滤**：直接学习原始注视数据，避免了固定点/扫视分类算法带来的信息损失和伪影。
*   **关系建模**：ART 模块通过显式编码节点间的亲和关系，提升了图神经网络处理复杂空间交互的能力。
*   **高质量数据集**：Focus100 数据集的发布填补了高多样性、原始注视驾驶数据的空白。

### 8. 不足与局限
*   **依赖感知栈**：模型性能受限于上游目标检测和深度估计的准确性，感知错误会传播至注视预测。
*   **缺乏意图建模**：目前未显式考虑驾驶员的具体意图（如左转、变道），而意图会显著改变注意力分配。
*   **实验室偏差**：Focus100 数据是在实验室环境下通过屏幕观看录像收集的，与真实驾驶中的前庭感觉和生理压力可能存在差异。

（完）
