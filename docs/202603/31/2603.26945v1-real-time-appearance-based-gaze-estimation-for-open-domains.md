---
title: Real-time Appearance-based Gaze Estimation for Open Domains
title_zh: 面向开放域的实时基于外观的视线估计
authors: "Zhenhao Li, Zheng Liu, Seunghyun Lee, Amin Fadaeinejad, Yuanhao Yu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26945v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 基于外观的实时视线估计
tldr: "针对基于外观的视线估计在复杂现实场景（如佩戴物、弱光）中泛化性差的问题，本文提出一种鲁棒的AGE框架。通过数据增强（合成眼镜、口罩及光照）扩展图像流形，并结合多任务学习（对比学习、分类、分割）解决标签不一致问题。该轻量化模型在保持高性能的同时，参数量仅为SOTA的1%，实现了移动端实时高精度视线追踪。"
selection_source: fresh_fetch
motivation: 现有视线估计模型在处理面部遮挡和极端光照等开放域场景时，由于训练数据多样性不足和标签偏差，泛化能力显著下降。
method: 采用集成增强技术合成多样化场景，并利用多视图监督对比学习、离散标签分类及眼部分割等多任务学习策略提升特征鲁棒性。
result: "提出的轻量化MobileNet模型在极具挑战性的新基准测试中表现优异，性能媲美SOTA模型UniGaze-H，但参数量减少了99%以上。"
conclusion: 该研究证明了通过增强数据多样性和优化多任务学习目标，可以在极低计算开销下实现跨域鲁棒且实时的视线估计。
---

## 摘要
基于外观的视线估计（AGE）在受限场景下已取得显著性能，但我们揭示了一个显著的泛化差距，即现有的 AGE 模型在实际的非受限场景中往往失效，特别是在涉及面部佩戴物和光照条件较差的情况下。我们将这种失效归因于两个核心因素：图像多样性有限以及不同数据集之间标签保真度的不一致，特别是在俯仰角（pitch）轴上。为了解决这些问题，我们提出了一个鲁棒的 AGE 框架，在不需要额外人工标注数据的情况下增强了泛化能力。首先，我们通过集成增强技术（包括合成眼镜、口罩和多变的光照）来扩展图像流形。其次，为了减轻跨数据集标签偏差各向异性的影响，我们将视线回归重新表述为一个多任务学习问题，并结合了多视图有监督对比学习（SupCon）、离散化标签分类和眼部区域分割作为辅助目标。为了严格验证我们的方法，我们构建了新的基准数据集，旨在评估挑战性条件下的视线鲁棒性，而这一维度在现有的评估协议中很大程度上被忽视了。我们基于 MobileNet 的轻量级模型实现了与当前最先进（SOTA）的 UniGaze-H 竞争力的泛化性能，同时仅使用了不到 1% 的参数，从而在移动设备上实现了高保真、实时的视线追踪。

## Abstract
Appearance-based gaze estimation (AGE) has achieved remarkable performance in constrained settings, yet we reveal a significant generalization gap where existing AGE models often fail in practical, unconstrained scenarios, particularly those involving facial wearables and poor lighting conditions. We attribute this failure to two core factors: limited image diversity and inconsistent label fidelity across different datasets, especially along the pitch axis. To address these, we propose a robust AGE framework that enhances generalization without requiring additional human-annotated data. First, we expand the image manifold via an ensemble of augmentation techniques, including synthesis of eyeglasses, masks, and varied lighting. Second, to mitigate the impact of anisotropic inter-dataset label deviation, we reformulate gaze regression as a multi-task learning problem, incorporating multi-view supervised contrastive (SupCon) learning, discretized label classification, and eye-region segmentation as auxiliary objectives. To rigorously validate our approach, we curate new benchmark datasets designed to evaluate gaze robustness under challenging conditions, a dimension largely overlooked by existing evaluation protocols. Our MobileNet-based lightweight model achieves generalization performance competitive with the state-of-the-art (SOTA) UniGaze-H, while utilizing less than 1\% of its parameters, enabling high-fidelity, real-time gaze tracking on mobile devices.

---

## 论文详细总结（自动生成）

### 论文结构化总结：Real-time Appearance-based Gaze Estimation for Open Domains

#### 1. 核心问题与整体含义（研究动机和背景）
基于外观的视线估计（AGE）虽然在受限的实验室环境下表现优异，但在现实开放域场景（如佩戴眼镜、口罩、极端光照）中存在严重的**泛化性崩溃**。论文指出两个核心痛点：
*   **图像多样性不足**：现有数据集缺乏真实世界的遮挡物和复杂光照。
*   **跨数据集标签偏差的各向异性**：不同数据集在俯仰角（Pitch）轴上的标注不一致性远高于水平角（Yaw）轴，导致简单合并数据集训练反而会损害模型性能。
*   **效率与精度的权衡**：现有的 SOTA 模型（如基于 ViT 的模型）计算开销巨大，难以在移动端实时运行。

#### 2. 方法论：核心思想与关键技术
论文提出了一个无需额外人工标注的鲁棒 AGE 框架，核心思想是通过**自动化数据增强**和**多任务正则化**来构建鲁棒的特征流形。
*   **自动化增强流水线**：
    *   **佩戴物合成**：利用 GlassesGAN 生成姿态一致的眼镜模板，并合成面部口罩。
    *   **环境模拟**：背景替换（MIT Indoor）、传感器噪声注入、定向光照扰动。
*   **多任务学习架构**：
    *   **离散化标签分类**：将连续视线回归转化为分类任务，通过期望值计算输出，有效缓解“均值崩溃”问题。
    *   **多视图有监督对比学习（SupCon）**：通过 4 种对比损失（数据集不变性、视线一致性、眼镜/口罩不变性）强制模型学习遮挡无关的特征。
    *   **眼部/虹膜分割**：引入 4 个二值分割头作为辅助任务，引导模型关注关键的眼部几何结构。
*   **各向异性标签处理**：针对 Pitch 轴标签不准的问题，对噪声较大的数据集（DN, DC）选择性丢弃 Pitch 回归损失，仅保留对比学习约束。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：ETH-XGaze (DX)、GazeGene (DN)、GazeCapture (DC)。
    *   **新基准 RealGaze**：包含 20 名志愿者，涵盖 9 种组合场景（眼镜、口罩、侧光等），模拟真实移动端使用。
    *   **新基准 ZeroGaze**：利用 Flux.1 生成的 7.6 万组“零视线”三元组，专门用于量化遮挡带来的视觉偏差。
*   **对比方法**：包括 PureGaze、ETH-XGaze (ResNet-50) 以及目前最强的 UniGaze (ViT-B/H)。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA V100 GPU**。
*   **训练细节**：
    *   **MobileNet 版本**：从 ImageNet 预训练开始，训练 50 个 epoch，Batch Size 为 160/GPU。
    *   **ViT 版本**：基于 MAE 预训练权重，训练 15 个 epoch，Batch Size 为 96/GPU。
    *   **优化器**：Adam 优化器，学习率在 $10^{-3}$ 到 $3 \times 10^{-3}$ 之间。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **跨域评估**：在 MPIIFaceGaze 和 EYEDIAP 上进行了标准跨数据集测试。
*   **鲁棒性分析**：在 RealGaze 的 9 种细分场景下对比了各模型的误差分量（dX, dY）。
*   **消融实验**：涵盖了骨干网络选择（MobileNet vs ResNet vs ViT）、数据增强模块、各项损失函数（Lclf, Lseg, LSϕ等）的贡献分析。
*   **可视化分析**：通过 t-SNE 展示了特征流形在遮挡下的稳定性。
*   **结论**：实验设计科学、对比公平（统一了视线范围限制），样本量大且覆盖了边缘场景，具有很高的说服力。

#### 6. 主要结论与发现
*   **轻量化优势**：基于 MobileNet-v2 的模型在参数量不到 UniGaze-H 的 **1%** 的情况下，实现了与之媲美甚至更优的泛化性能。
*   **Pitch 轴敏感性**：证实了 Pitch 轴是泛化失败的主要来源，通过弱化噪声数据集的 Pitch 监督可显著提升稳定性。
*   **遮挡不变性**：通过合成增强和对比学习，模型能够“看穿”眼镜和口罩，关注眼部核心区域。
*   **校准潜力**：提出的训练无关（Training-free）个性化校准方法，仅需 1-5 个点即可大幅降低绝对误差。

#### 7. 优点（亮点）
*   **实用性极强**：针对移动端实时需求设计，解决了实际部署中最头疼的遮挡和光照问题。
*   **理论洞察深刻**：首次量化并分析了跨数据集标签偏差的“各向异性”特征。
*   **高质量基准**：贡献了 RealGaze 和 ZeroGaze 两个极具价值的测试集，填补了 AGE 领域缺乏遮挡评估基准的空白。
*   **多任务融合**：巧妙地将分割、分类和对比学习结合，无需额外人工标注即可提升特征质量。

#### 8. 不足与局限
*   **极端场景覆盖**：虽然考虑了眼镜和口罩，但对于重度眼妆、眼部皱纹（老年人）或极端的室外太阳光直射场景讨论较少。
*   **伪标签依赖**：眼部和虹膜分割依赖于 MediaPipe 生成的伪标签，若 MediaPipe 在某些极端角度失效，辅助任务的质量可能受损。
*   **硬件依赖**：虽然模型轻量，但训练过程仍依赖 8 卡 V100 等高性能算力进行大规模对比学习。

（完）
