---
title: "Foveated Diffusion: Efficient Spatially Adaptive Image and Video Generation"
title_zh: 视网膜中央凹扩散：高效的空间自适应图像与视频生成
authors: "Brian Chao, Lior Yariv, Howard Xiao, Gordon Wetzstein"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23491v1"
tags: ["query:gaze-intent"]
score: 8.0
evidence: 基于视线的自适应图像生成交互
tldr: 针对扩散模型在高分辨率图像和视频生成中计算开销巨大的问题，本文提出了一种受人类视觉系统启发的“注视点扩散”方法。该方法利用人眼在注视中心分辨率高、边缘分辨率低的特性，通过非均匀分配Token密度，在保证注视区域高清晰度的同时大幅减少外围计算量。通过从预训练模型进行后训练，该方法在显著提升生成效率的同时，实现了与全分辨率生成在感知上无差异的效果。
selection_source: fresh_fetch
motivation: 扩散模型生成高分辨率内容的计算复杂度随Token数量呈二次方增长，亟需在保持视觉质量的同时降低计算成本。
method: 利用注视点追踪技术，通过掩码对不同区域分配非均匀密度的Token，并开发了一种从高分辨率数据构建混合分辨率Token的机制进行模型后训练。
result: 该方法在大幅减少Token数量和生成时间的情况下，生成的图像和视频在感知上与全分辨率结果几乎没有区别。
conclusion: 注视点生成是提高扩散模型效率的一种实用且可扩展的方案，为实时交互式内容创作提供了新思路。
---

## 摘要
扩散模型和流匹配模型为创意内容创作（如交互式图像和流式视频生成）开启了前所未有的能力。然而，对更高分辨率、帧率和上下文长度日益增长的需求，使得高效生成变得愈发具有挑战性，因为计算复杂度随生成 token 数量呈二次方增长。本研究旨在优化用户注视位置已知或可估计（例如通过眼动追踪）场景下的生成过程效率。在这些场景中，我们利用了人类视觉中依赖于离心率的敏锐度特性：虽然用户在注视位置周围的小区域（中央凹区域）内能感知到极高分辨率的视觉信息，但在视野外周，分辨细节的能力会迅速下降。我们的方法首先通过一个模拟中央凹分辨率的掩码来非均匀地分配 token，在中央凹区域分配较高的 token 密度，而在外周区域分配较低的密度。图像或视频在混合分辨率的 token 设置下生成，其结果在感知上与全分辨率生成无异，同时大幅减少了 token 数量和生成时间。为此，我们开发了一种从高分辨率数据直接构建混合分辨率 token 的原则性机制，使得视网膜中央凹扩散模型能够基于现有基础模型进行后训练，同时保持跨分辨率的内容一致性。我们通过广泛的分析和精心设计的用户研究验证了我们的方法，证明了视网膜中央凹化（foveation）作为一种实用且可扩展的维度，在实现高效生成方面的有效性。

## Abstract
Diffusion and flow matching models have unlocked unprecedented capabilities for creative content creation, such as interactive image and streaming video generation. The growing demand for higher resolutions, frame rates, and context lengths, however, makes efficient generation increasingly challenging, as computational complexity grows quadratically with the number of generated tokens. Our work seeks to optimize the efficiency of the generation process in settings where the user's gaze location is known or can be estimated, for example, by using eye tracking. In these settings, we leverage the eccentricity-dependent acuity of human vision: while a user perceives very high-resolution visual information in a small region around their gaze location (the foveal region), the ability to resolve detail quickly degrades in the periphery of the visual field. Our approach starts with a mask modeling the foveated resolution to allocate tokens non-uniformly, assigning higher token density to foveal regions and lower density to peripheral regions. An image or video is generated in a mixed-resolution token setting, yielding results perceptually indistinguishable from full-resolution generation, while drastically reducing the token count and generation time. To this end, we develop a principled mechanism for constructing mixed-resolution tokens directly from high-resolution data, allowing a foveated diffusion model to be post-trained from an existing base model while maintaining content consistency across resolutions. We validate our approach through extensive analysis and a carefully designed user study, demonstrating the efficacy of foveation as a practical and scalable axis for efficient generation.

---

## 论文详细总结（自动生成）

以下是对论文《Foveated Diffusion: Efficient Spatially Adaptive Image and Video Generation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代扩散模型（如 DiT）的计算复杂度随 Token 数量呈**二次方增长**。随着用户对高分辨率、高帧率和长上下文的需求增加，实时生成（如流式视频、交互式环境）面临巨大的算力瓶颈。
*   **研究动机**：受人类视觉系统启发，人眼仅在注视点中心（中央凹区域）具有极高的分辨力，而外周区域的分辨力随离心率增加而迅速下降。
*   **整体含义**：本文提出 **Foveated Diffusion（注视点扩散）**，旨在通过眼动追踪或显著性预测，在生成过程中非均匀地分配计算资源，从而在不损失感知质量的前提下，显著提升图像和视频的生成效率。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：在 Denoising（去噪）过程中直接操作一组**混合分辨率的 Token**。在注视区域保持高 Token 密度，在外周区域使用低密度 Token（1 个低分辨率 Token 代表 $2 \times 2$ 个高分辨率 Token 的空间区域）。
*   **关键技术细节**：
    *   **注视点 Token 化（Foveated Tokenization）**：根据二值掩码 $M$ 将潜在空间划分为高/低分辨率区域。总序列长度 $L$ 远小于全分辨率长度。
    *   **混合分辨率 RoPE（Mixed-Resolution RoPE）**：针对非均匀网格修改旋转位置嵌入。通过相位对齐（Phase-Aligned）机制，确保高分辨率查询（Query）能正确关注到低分辨率的键（Key），反之亦然。
    *   **注视点后训练（Foveated Training）**：为了解决直接推理导致的结构不一致（如物体重复），提出一种原则性的后训练方案。利用预训练 VAE 分别编码原始图像及其下采样版本，按掩码合并成“干净”的混合分辨率目标，通过流匹配（Flow Matching）和 LoRA 进行微调。
    *   **推理与合成**：在缩减后的 Token 空间进行迭代去噪，最后将结果拆分为高/低分辨率块，分别通过 VAE 解码后进行空间上采样和 Alpha 混合。

### 3. 实验设计
*   **数据集**：
    *   **图像生成**：使用 Aesthetic-Train-V2 数据集（90k 训练，10k 测试）。
    *   **视频生成**：使用 Vchitect-T2V-Dataverse 数据集（微调 Wan2.1 模型）。
*   **Benchmark 与指标**：
    *   **图像**：FID（分布质量）、HPSv2.1（人类偏好）、Precision（精度）、CLIP Score（文本对齐度）。
    *   **视频**：VBench（综合视频生成指标，包括一致性、动感等）。
    *   **用户研究**：采用 2AFC（双选强制选择）范式，在模拟眼动追踪环境下对比感知质量。
*   **对比方法**：
    *   **Full high-res**：全分辨率生成（基准上限）。
    *   **Naïve mixed-res**：未经后训练、直接应用混合分辨率推理的原始模型。

### 4. 资源与算力
*   **GPU 型号与数量**：实验在 **8 台 NVIDIA H100 GPU** 集群上完成。
*   **训练时长**：图像和视频模型均使用 LoRA（Rank 32）进行了 **10,000 步** 的微调。
*   **推理效率**：图像生成实现约 **2倍** 加速；视频生成实现约 **4倍** 加速（因为 3D 注意力机制对 Token 缩减更敏感）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   定量分析涵盖了 10,000 张测试图像和 200 个测试视频。
    *   用户研究涉及 11 名参与者，共收集了 660 次投票数据。
    *   展示了多种掩码策略（随机圆、显著性引导、边界框引导）。
*   **充分性评价**：实验设计较为充分。作者不仅对比了标准指标，还通过用户研究验证了“感知无差异”的核心主张。消融实验展示了后训练对消除结构伪影（如双头、比例失调）的必要性。

### 6. 主要结论与发现
*   **效率提升**：在仅使用 25% Token 的情况下，视频生成速度提升了 4 倍，且感知质量几乎未下降。
*   **感知一致性**：经过后训练的 Foveated Diffusion 能够产生跨分辨率边界一致的内容，解决了朴素混合分辨率方法中常见的结构断裂问题。
*   **应用潜力**：该方法在 VR 游戏、机器人仿真和自动驾驶模拟等显著性区域明确的场景中具有极高的实用价值。

### 7. 优点
*   **感知驱动**：紧密结合人类视觉特性，将计算资源花在“刀刃”上。
*   **原则性设计**：通过修改 RoPE 和构建一致性训练目标，从理论上解决了混合分辨率生成的对齐问题。
*   **即插即用**：作为一种后训练框架，可以适配现有的 DiT 基础模型（如 FLUX 或 Wan2.1），无需从头训练。

### 8. 不足与局限
*   **边界伪影**：在注视点边缘偶尔会出现颜色不连续或混合痕迹，这主要是由于 VAE 分别解码后再混合导致的，而非扩散模型本身的问题。
*   **VAE 限制**：目前的 VAE 仍需对高/低分辨率部分进行两次解码，未来若能开发原生支持混合分辨率 Token 的 VAE，效率将进一步提升。
*   **硬件依赖**：该方法的优势依赖于准确的注视点输入（如眼动仪），在缺乏此类输入的通用场景下，需依赖显著性预测模型，这会引入额外的计算开销。

（完）
