---
title: Gaze-Regularized VLMs for Ego-Centric Behavior Understanding
title_zh: 用于第一人称视角行为理解的视线正则化视觉语言模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23190v1"
tags: ["query:gaze-intent"]
score: 10.0
evidence: 针对人类意图和未来行动的眼动正则化框架
tldr: "本研究针对第一视角行为理解，提出了一种注视正则化视觉语言模型（VLM）框架。传统方法往往忽略了能反映人类意图的眼动注视信息，而该方法通过生成基于注视的查询，使模型动态关注注视区域，并利用正则化机制对齐模型与人类的注意力。实验表明，该方法在预测未来事件的动作描述方面表现优异，语义得分比基准模型提高了约13%，为在VLM中利用人类注视信息奠定了基础。"
selection_source: fresh_fetch
motivation: 现有的第一视角行为理解方法主要依赖视觉数据，忽略了眼动注视中蕴含的关键意图和未来行动线索。
method: 在VLM架构中引入注视正则化机制和基于注视的动态查询，使模型注意力与人类注视模式保持一致。
result: "实验结果显示，该方法在语义得分上比不使用注视数据的基准模型提升了近13%。"
conclusion: 将人类注视信息整合进VLM能显著增强模型对未来事件的预测能力，为行为理解提供了有效的正则化手段。
---

## 摘要
视线（包括注视和扫视）为人类意图和未来行动提供了关键见解。本研究引入了一个视线正则化框架，旨在增强用于第一人称视角行为理解的视觉语言模型（VLMs）。与仅依赖视觉数据而忽视视线信息的现有方法不同，我们的方法在训练过程中直接将视线信息整合到 VLM 架构中。通过生成基于视线的查询，模型能够动态关注视线高亮的区域，同时视线正则化机制确保模型注意力与人类注意力模式保持一致。为了更好地理解如何将视线有效地整合到 VLMs 中，我们进行了广泛的实验，探索了多种整合视线数据的策略。这些创新使得能够通过详细的动作描述来预测未来事件。实验结果表明，与不利用视线数据的基准模型相比，语义得分提高了近 13%，突显了我们方法的有效性。这项工作为在 VLMs 中利用人类视线奠定了基础，显著提升了其在需要准确且鲁棒的未来事件预测应用中的预测能力。

## Abstract
Eye gaze, encompassing fixations and saccades, provides critical insights into human intentions and future actions. This study introduces a gaze-regularized framework that enhances Vision Language Models (VLMs) for egocentric behavior understanding. Unlike existing methods that rely solely on visual data and overlook gaze information, our approach directly incorporates gaze information into the VLM architecture during training. By generating gaze-based queries, the model dynamically focuses on gaze-highlighted regions, while a gaze-regularization mechanism ensures the alignment of model attention with human attention patterns. To better understand how gaze can be effectively integrated into VLMs, we conducted extensive experiments exploring various strategies for incorporating gaze data. These innovations enable the prediction of future events with detailed action descriptions. Experimental results demonstrate a nearly 13 % improvement in semantic scores compared to baseline models not leveraging gaze data, highlighting the effectiveness of our approach. This work establishes a foundation for leveraging the human gaze in VLMs, significantly boosting their predictive capabilities in applications requiring accurate and robust future event prediction.

---

## 论文详细总结（自动生成）

以下是对论文《Gaze-Regularized VLMs for Ego-Centric Behavior Understanding》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在第一人称视角（Ego-centric）视频中，人类的眼动注视（Gaze）包含了关于意图、短期目标和未来行动的关键线索。然而，现有的视觉语言模型（VLMs）主要依赖视觉特征进行静态描述，往往忽略了这种能揭示“人接下来要做什么”的生物学信号。
*   **核心问题**：如何有效地将人类视线信息整合进 VLM 架构中，以提升模型对第一人称行为的细粒度理解和未来事件的预测能力？

### 2. 论文提出的方法论
该论文提出了一种**视线正则化框架（Gaze-Regularized Framework）**，其核心思想是通过视线引导模型注意力，并强制模型注意力分布与人类真实视线分布对齐。
*   **视线正则化注意力块（Gaze-Regularized Attention Block）**：在视觉编码器（ViT）和感知重采样器（Perceiver Resampler）之间插入。它将“视线覆盖图像”（Gaze-overlaid images）作为查询（Query），将原始 RGB 图像特征作为键（Key）和值（Value）。
*   **视线正则化机制（Gaze Regularizer）**：利用 **KL 散度（Kullback-Leibler Divergence）** 作为辅助损失函数。在训练过程中，计算模型内部的注意力权重分布与基于真实视线生成的二值热图分布之间的差异，强制模型“看”人类所看的地方。
*   **视线聚合与遮挡检测**：
    *   **聚合（Aggregation）**：考虑到单点视线存在噪声，模型收集 200ms 内的视线点进行平滑处理。
    *   **遮挡检测（Occlusion Check）**：利用前后向光流一致性（Optical Flow Consistency）检测物体是否被遮挡，剔除无效的视线点。
*   **伪视线生成（Pseudo-Gaze Generation）**：针对测试阶段可能无法获取实时视线数据的情况，设计了一个轻量级的编码器-解码器模块，根据 RGB 图像预测视线热图，从而生成“伪视线查询”。

### 3. 实验设计
*   **数据集**：主要使用 **Ego4D** 数据集（包含约 33.3 小时带视线轨迹的视频）。此外，使用 **EGTEA+ Gaze** 数据集进行分布外（OOD）泛化性测试。
*   **任务设置**：
    1.  **未来活动预测**：观察前 $\tau_o$ 秒，预测未来 2 秒的细粒度动作描述。
    2.  **当前活动理解**：描述当前帧正在发生的动作。
*   **Benchmark 与对比方法**：
    *   **基准模型**：基于 OpenFlamingo 架构的 Base 模型（不含视线信息）。
    *   **对比对象**：包括 InternVL、Open-LLaVA-NeXT 等主流 VLM 在零样本（Zero-shot）下的表现，以及不同视线输入策略（单点视线 vs 聚合视线、文本形式视线 vs 图像形式视线）。
*   **评估指标**：语义得分（Semantic Score，基于 SBERT）、ROUGE-L、METEOR。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 张 NVIDIA A800 (40GB)** GPU。
*   **训练时长**：
    *   基准模型（Base Model）：约 36-38 小时。
    *   视线正则化模型（Gaze-Regularized Model）：约 50 小时。
*   **模型规模**：参数量在 9.44 亿至 9.96 亿之间（约 1B 规模）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    *   **消融实验**：正则化系数 $\lambda$ 的敏感度、注意力块的数量、视线点覆盖大小、遮挡检测的有效性。
    *   **标注质量对比**：对比了 GPT-4V 和 ShareCaptioner 生成的不同粒度标注对模型性能的影响。
    *   **鲁棒性测试**：测试了视线数据缺失（随机损坏）时的模型表现。
*   **充分性评价**：实验设计较为全面，不仅验证了视线的有效性，还考虑了实际应用中视线缺失的补救方案（伪视线），并进行了跨数据集验证，客观性较强。

### 6. 论文的主要结论与发现
*   **视线显著增益**：引入视线正则化后，未来活动预测的语义得分比基准模型提升了 **13%**，当前活动理解提升了 **2%**。
*   **正则化的必要性**：单纯输入视线图像而不进行 KL 散度正则化，性能提升有限，证明了“对齐注意力分布”是关键。
*   **标注粒度的影响**：当文本标注越细粒度（如描述具体的物体位置和手部动作）时，视线信息带来的性能提升越显著（额外提升 12%）。
*   **预测局限性**：现有的通用 VLM（如 InternVL）在没有针对性训练的情况下，即便给予视线提示，也难以准确预测未来事件。

### 7. 优点（亮点）
*   **生物启发式设计**：成功将人类心理学/生理学中的视线特性转化为深度学习中的正则化约束。
*   **实用性考虑**：提出的“伪视线生成”模块解决了该技术在缺乏眼动追踪硬件的设备上部署的难题。
*   **细粒度理解**：通过 GPT-4V 增强了数据集的标注质量，使模型能够生成比传统“动词+名词”更丰富的描述。

### 8. 不足与局限
*   **标注偏差风险**：细粒度标注依赖于 GPT-4V 的自动生成，虽然经过人工筛选，但仍可能存在模型固有的幻觉或偏见。
*   **模态单一**：目前仅整合了视觉和视线，未考虑第一人称视角中同样重要的音频（Audio）或触觉信号。
*   **计算开销**：引入光流遮挡检测和额外的注意力块增加了推理延迟（每序列增加约 2-3 秒），对实时性要求极高的场景（如辅助驾驶）可能存在挑战。
*   **超参数依赖**：遮挡检测中的阈值（$\epsilon, \eta$）需要人工反馈调整，缺乏自适应性。

（完）
